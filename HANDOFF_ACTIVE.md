# Active Claude/Codex handoff

Last compacted: 2026-08-09 17:25 EDT. This is the executable mailbox, not a
history. Terminal conclusions live in `AI_POLICIES.md`, live compute in
`JOBS.md`, queue order in `BACKLOG.md`, and durable review records in
`HANDOFF_REVIEW.md`.

## Current truth

| area | status | next legal action |
|---|---|---|
| Production | **LIVE / CONFIRMED** | Fly release 17 runs compiled `mc-s0-report-lcb`; RLCB-C1 measured `+0.338 +/- 0.068` signed levels versus `mc-strong`. |
| S3a structured bury | **TERMINAL SELECT NONE** | Exact verifier accepted all 2,048 clusters; aggregate `20609613…271f`, final `32156d79…c9ff`. Close this recipe: no retry, tuning, confirmation or pooled inference. |
| S4 point banking | **2,048-CLUSTER MINI SCREEN RUNNING / SEALED** | Exact `cad3992`, packet `17036e63…1385`, admission `1d99bb55…bdbf`, receipt `20a420d2…5cc`. Count/status monitoring only, then one terminal verify. |
| Human H0 | **CONTROLLER FROZEN / REVIEW OPEN / ZERO OUTCOMES** | Producer `931f504`, asset `ff277b4`, packet `13d9a97f…61fc`. Review the controller below. PASS may authorize one diagnostic execution receipt; it does not itself execute, train or promote. |
| Teacher Stage C | **T1 COMPLETE / V2 HELD** | Rebind the score-free design to passed H0-v3/controller semantics only after the controller review closes. No state capture, labels, compute or training. |
| S3c small endgames | **768-ROOT CURRICULUM FROZEN / REVIEW QUEUED** | Producer `0b96fae`, asset `4fb90a1`, packet `df102428…9eca`. Resume after the active H0 review; no solver outcome exists. |
| S5 point protection | **REPLAY HYPOTHESIS ONLY** | Reconstruct bot-seat losing follows and prove an avoidable lower-point legal action still exists before designing a treatment. |
| HUMAN-C1 | **PARKED / NO TRAFFIC** | Preserve the inert seam. Per strategy review, defer further infrastructure until a challenger beats report-LCB in confirmation. |

## Active milestone — T3 human-witness challenger readiness

Plain-English objective: turn human/model ideas and observed loss modes into
one honestly tested challenger while the already reviewed S4 policy receives a
real whole-game test.

| output | why it can improve strength | progress and what remains |
|---|---|---|
| **T3.1 S3a verdict** | Test whether broader point/void/trump kitty choices improve the whole bot. | **Closed SELECT NONE.** The state-level signal did not survive the fresh full-game gate. Do not tune on this screen. |
| **T3.2 S4 whole-game screen** | Correct a rollout blind spot: a player who is already winning may bank a 5/10/K while retaining higher control. | Exact-state mechanism passed; one reviewed 2,048-cluster Mini screen is now running. Seal outcomes until terminal verify. |
| **T3.3 H0 bounded design** | Let human and V11 actions expand the bot's ideas, but judge them fairly instead of copying them. | **Closed design PASS** at `239f13c`; exact v3 packet `4d3f0a35…8cc3c`. |
| **T3.4 H0 controller** | Identify which human/V11/random proposals survive fresh rollout evaluation and can seed better ballots or Teacher examples. | **Implementation and immutable packet complete.** External review is the only remaining T3.4 gate; no world or outcome has been sampled. |
| **T3.5 Stage-C-v3 contract** | Spend expensive labels on uncertainty, disagreement, point play, bury and late states rather than ordinary heuristic self-play. | V2 binds obsolete H0. Rebind/freeze v3 after H0 controller review; no dataset exists yet. |
| **T3.6 S3c feasibility** | Start exact search at tiny natural endgames, then grow one card at a time instead of retrying the failed four-card jump. | Census/curriculum frozen and reproduced. External review remains; no solver run. |
| **Human evaluation boundary** | Ensure a bot that beats bots can ultimately be tested honestly against people. | `human_v8` is provenance-verified and training-excluded as declared. HUMAN-C1 traffic work is parked until a real challenger exists. |

Active `/goal`:

> Complete T3 human-witness challenger readiness; terminally preserve S3a;
> run only the reviewed S4 screen; freeze/review H0, Stage-C and S3c contracts;
> define the leakage-safe human ladder; stop before unreviewed strength
> compute, training, promotion or production change.

## Sole active review — H0-v3 score-free execution controller

### Plain-English review question

Does this program faithfully turn the passed H0 design into one finite future
experiment? It must replay the exact 557 human decisions, build the reviewed
production+human+V11+random ballots, account for every sampled world and
candidate rollout, suppress all utility if any row is incomplete, and make a
second execution impossible without a new packet. This review must not run the
counterfactual experiment.

### Exact assets

- producer source: `931f50416d1d42f73b8933a526ebfafa6999aec9`;
- asset commit: `ff277b448aa14ff98c7dc1edc3b6ea3f7fdbf917`;
- packet path: `server/runs/logs/human-v8-h0-counterfactual-controller-v1/controller_packet.json`;
- external packet SHA-256: `13d9a97f9adf26860b9f5e0d4889960c75baa5bf979b939206d2399472ba61fc`;
- internal packet SHA-256: `a11d9737845d2633283d1c89d20afdeb6301d3131d6eb1a1f14b4e90dc6abb1e`;
- controller/runtime SHA-256: `08bab9f0…6670` / `9e5ed146…130d`;
- candidate geometry: `876ed56b…ff2b`; schedule: `f54ce374…1793`;
- frozen maximum: 1,329,210 candidate-world rollouts;
- clean producer root: `/Users/jerryyu/Projects/shengji-h0-controller-931-review`;
- clean asset root: `/Users/jerryyu/Projects/shengji-h0-controller-ff2-review`.

The producer root has ignored, hash-exact compiled/RLCB evidence copied in for
portable reopening; `git status --porcelain` remains empty. The packet root is
the immutable tracked asset. Verify from the producer root while pointing to
the packet root—running the producer at the asset commit should fail producer
identity by design.

### Measured score-free facts

- 557/557 rows replay: 512 play and 45 bury;
- 452 human plays already occur in the production ballot; 305 rows have no
  novel analysis action; V11 and matched-random each propose on 207 rows and
  coincide on 30;
- humans match 18/45 structured bury ballots;
- eight deal-clustered shards each carry 69–70 rows; maximum planned shard
  work is 166,860 candidate-world rollouts;
- focused tests pass 30/30; freeze and independent full recomputation both
  return `VERIFIED_FOR_CONTROLLER_REVIEW`;
- preflight sampled **0 worlds**, ran **0 candidate rollouts**, computed no
  utility/outcome, and grants no execution authority.

### Load-bearing checks

1. Recompute all design/corpus/source-log/V11/live-parent/runtime identities,
   every candidate union, source attribution, schedule and both packet hashes.
2. Inspect acting-team utility: it derives from raw integral attacker points,
   includes the deal half-level and flips sign only for defenders.
3. Check the production root is exact `mc-s0-report-lcb`; pilot selection uses
   all deduplicated candidates on 30 common worlds; report fixes only
   reference/human/selected actions on 300 fresh common worlds; continuation
   is exact `HeuristicBot`.
4. Check exact source survival, candidate caps, seeds, sampler counters,
   reference work, report action population and candidate-world ceilings.
5. Force an underfilled sampler and a mid-row protocol refusal. The row must
   retain exact outcome-free attempted-work counters while exposing no cards,
   raw scores, utilities or estimands; any refused row suppresses aggregate
   utility.
6. Check the one-shot review claim, canonical receipt/namespace, clean-tree
   requirement, exclusive shard/aggregate paths, TOCTOU reopens and terminal
   replay. Refused rows are identity-verified and never retried.
7. Confirm DESIGN/AUDIT and play/bury estimands stay separate and remain
   diagnostic only. No label, training, strength, promotion or deployment flag
   may become true.
8. **Adjudicate this explicit wording clarification:** v3 names three
   “pairwise-disjoint world folds.” The implementation domain-separates their
   deterministic RNG streams but does not reject coincident realized hidden
   deals; the packet says so explicitly. PASS means this is the intended
   statistical independence. HOLD if the reviewed design instead required
   mutually exclusive realized world support, because that would require a
   new design rather than a silent controller change.

Pinned focused command from the producer root:

```sh
SHENGJI_FAST=1 SHENGJI_REQUIRE_VOIDS=1 PYTHONPATH=server /Users/jerryyu/Projects/shengji/server/.venv/bin/python -m pytest -q server/tests/test_h0_human_counterfactual_controller.py server/tests/test_h0_human_counterfactual_packet.py
```

The score-free verify command is the controller's `verify` invocation with
producer Git `931f504…aec9`, packet SHA `13d9a97f…61fc`, corpus manifest
`b9699790…16553`, source manifest `07ff18fb…2aa5e` and V11 checkpoint
`cd89d6ed…c003`. It must report 557 rows and zero worlds.

Requested marker:

`H0_HUMAN_COUNTERFACTUAL_CONTROLLER_V1_REVIEW {"candidate_geometry_sha256":"876ed56bd8f436d58cb6f3d58774a0f06756afb4d8c98ffdb49d9424b545ff2b","controller_script_sha256":"08bab9f03928eb57ac5bb5e58ae3e7c7389ced02320c22e3bd1d94a7406c6670","corpus_manifest_sha256":"b9699790bdfe1c217922c9f9c72b237c1856174fa64c11753329a8ff11e16553","design_packet_sha256":"4d3f0a35082c6957f2a468686b8eedbd6d7cbbf9540503fcea08cccf27c8cc3c","design_review_git":"239f13ce52a8be81108fdebf9bd0e96742e60133","git":"931f50416d1d42f73b8933a526ebfafa6999aec9","independent_review":true,"labels_authorized":false,"max_candidate_worlds":1329210,"one_counterfactual_execution_authorized":true,"outcomes_computed_before_review":false,"packet_sha256":"13d9a97f9adf26860b9f5e0d4889960c75baa5bf979b939206d2399472ba61fc","production_deployment":false,"production_promotion":false,"schedule_sha256":"f54ce37425707dfeea3563bbc5d635617943152166a82825a74e55ad00131793","schema":"human-h0-counterfactual-controller-review-v1","score_free_preflight_verified":true,"selected_bury_rows_sha256":"cdfe77dfbec0e97fb8935c5822239acd6db60c644c433c32a4445913459aa1e8","selected_play_rows_sha256":"18673b20ca0a5b1a8e476f3bcf45cf9d08f90f4244f9c5ee07cb8bd8cd47711d","source_manifest_sha256":"07ff18fb35f2fb987f18b37b5100172e2751681fbfed17285ce7d7035232aa5e","strength_claim":false,"training_authorized":false,"v11_checkpoint_sha256":"cd89d6ed7e9d5f798d69ce546107c4dfbef682c5385de39af527026e39e1c003","verdict":"PASS","worlds_sampled_before_review":0}`

Append PASS/HOLD and measured findings only to `HANDOFF_REVIEW.md`. A PASS
authorizes creation of one execution receipt under the exact runtime; it does
not itself run H0 or authorize labels, training, a strength claim, promotion or
production.

## Queued after H0 review

1. Rebind/freeze Stage-C-v3 without capturing states or labels.
2. Resume the S3c curriculum review (`0b96fae` / `4fb90a1` /
   `df102428…9eca`); PASS may authorize one-card controller implementation only.
3. Build the S5 score-free bot-decision replay census if coding capacity
   remains. Do not infer a treatment from aggregate human-loss forensics.

## Fleet and safety

- Mini: S4 screen only; read count/status heartbeats, never partial outcomes.
- Air: free for exact tests/reviews or another already reviewed job; idle
  compute does not authorize a new estimand.
- Fly: production release 17; passive monitoring only.
- Never retry, resume, extend, tune from or pool a consumed one-shot stream.
- Never promote from DESIGN/AUDIT diagnostics; fresh paired confirmation owns
  policy strength and the future blinded human ladder owns product value.
