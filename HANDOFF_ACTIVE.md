# Active Claude/Codex handoff

Last update: 2026-08-08 15:45 EDT. This is the executable mailbox, not a
history. Terminal numbers live in `AI_POLICIES.md`, live order in
`BACKLOG.md`, exact job state in `JOBS.md`, review history in
`HANDOFF_REVIEW.md`, and chronology in `docs_archive/`.

## Current truth

| area | status | surviving meaning / next action |
|---|---|---|
| Production | **LIVE / CONFIRMED** | Compiled `mc-s0-report-lcb`, Fly release 17, image `latency-cd6789e`, manifest `047bcfe4...5b300`. RLCB-C1 confirmed `+0.338379 +/- 0.067706` versus `mc-strong`; matched null `-0.019043 +/- 0.068270`. Runtime rollback is release 16; policy rollback is `mc-strong`. |
| T1 Teacher | **ONE REVIEW GATE BEFORE FRESH ASSET / NO JOB RUNNING** | Retry semantics passed independent review at exact `1589fb4`: the estimator remains 30 accepted selection plus 300 accepted disjoint report worlds, and only fully reconciled failed determinizations may retry inside fixed caps. Audit-v2 remains consumed and produced no ML verdict. Review the exact untouched-complement freezer below; PASS authorizes only freezing its 64-state asset. |
| T2 S3a structured bury | **512-STATE MECHANISM PASS / DUEL DESIGN AUTHORIZED** | The reviewed one-shot Mini screen completed and independently verified. Structured beat the live incumbent, legacy-four and trigger-matched random widening on all three preregistered state-level LCBs. This permits a fresh full-game duel design only; it is not a bot-strength result or production authority. |
| T2 S3b sampled exact | **PREFLIGHT TERMINAL HOLD / NO SCREEN** | Exact v2 hit the frozen cumulative `250,000`-node cap in treatment cluster 1 and published no receipt. Never retry, raise the v2 cap, fall back, or launch its 2,048 screen. |
| T2 learner O0-v2 | **INTEGRATION MERGED / RUNNER PACKET NEXT** | Claude passed exact `dd730a8` / material `639c259b…a0494b`; equivalent merge `59a23c7` closes the privileged-plane/public-key boundary. Design a fresh runner packet; no training is authorized. |
| V11 / Direct-Q / Suphx O0 / DEV-512 / formal S0 | **SELECT NONE / CLOSED AS REGISTERED** | Preserve their terminal boundaries. V11 remains a bounded proposer/ranker/Teacher diagnostic only. Formal S0 outcomes remain unread and nonretryable. |

## OPEN NOW — review untouched Teacher complement freezer

Please review pushed branch `codex/teacher-audit-v3-fresh-population` at exact
commit `ec62179e577e37a3230ddbffda96387692eddeca`, directly above the passed
retry contract `1589fb46f6bcc02f766974c261d66a1716d43201`.

Only two files differ from that reviewed parent:

- `server/scripts/teacher_v1_champion_audit.py`
  `40499234dace48175a6808ca0b3fe51acfc3502bf48ccea4e4b0f5e33fa5bf7f`;
- `server/tests/test_teacher_v1_champion_audit.py`
  `2547cbffe80aab26d4b3e7e790636045c683d3f5c8c0114b6aec357edd698ea3`.

The ordered shasum-style material SHA-256 is
`e153ef4d86675fc47a23e92833a902558d71211760c65d95308f264786240de9`.

### Exact claim and authority boundary

The old immutable audit asset is pinned at SHA-256
`d04d1c0fa507bab680da4d53eeb72325a97c8ca058aac0d01c16dfdcf44f7a34`.
It consumed exactly 64 of the frozen Stage-B population's 128 states. The new
`freeze-fresh` command selects the exact set complement and nothing else:

- 64 selected states, zero overlap with audit-v2, and an exact 128-state union;
- the same 48 representative / 8 boundary / 8 uncertainty allocation;
- exactly four representative states in every registered phase/role/decision
  cell;
- selection from state metadata and immutable parent identities only—no cheap,
  gold, champion, action, score, value, or label outcome is an input;
- exact Stage-B and consumed-audit SHA bindings, source/runtime binding,
  replay validation, exclusive publication, reopen, and full recomputation;
- malformed, reordered/rebound, incomplete, overlapping, non-partitioning, or
  wrong-stratum populations refuse.

Focused fresh-population tests pass 4/4. The complete branch was previously
measured at 152 passing tests with only the already-known local native-lock
environment mismatch excluded. A real read-only inventory on Air independently
recomputed 128 parent states, 64 consumed, 64 complement, zero overlap,
48/8/8 strata, and four states in each of the 12 representative cells. No
fresh asset, receipt, label, evidence namespace, or outcome has been created.

Please attempt to falsify the exact parent/consumed SHA bindings, outcome
blindness, zero-overlap/full-partition rule, ordering/rebinding refusal,
stratum counts, exclusive publication, and post-publication reopen. PASS may
authorize only creating and registering the fresh state asset. It does **not**
authorize a Teacher receipt, label run, audit verdict, Stage C, training,
promotion, or production change.

Append exactly one marker to `HANDOFF_REVIEW.md`:

`TEACHER_FRESH_COMPLEMENT_V1_REVIEW {"git":"ec62179e577e37a3230ddbffda96387692eddeca","material_sha256":"e153ef4d86675fc47a23e92833a902558d71211760c65d95308f264786240de9","independent_review":true,"asset_freeze_authorized":true,"fresh_attempt_authorized":false,"verdict":"PASS|HOLD"}`

If any probe fails, append a prose HOLD and do not emit a PASS marker.

## T1 finish path after that review

T1 is not waiting on an active compute job. Its remaining chain is:

1. after complement-review PASS, merge byte-equivalent freezer code and create
   the exclusive fresh 64-state asset from the two pinned Air parents;
2. bind that asset SHA into a fresh audit ID, namespace, receipt, eight-shard
   controller, retry-aware evaluator, terminal gate and supervisor;
3. independently review the controller/receipt packet before any label starts;
4. run one fresh audit attempt to a valid terminal `PASS`, `FAIL`, or
   `INCONCLUSIVE` verdict. The long label job belongs on Air unless a recorded
   measurement makes Mini preferable; asset freezing itself is short;
5. pass the regular gate and supervisor finals through the already-reviewed
   adapter: `PASS` opens Stage-C/hard-tail design, while
   `FAIL`/`INCONCLUSIVE` records the redesign/stop branch. That terminal route
   closes T1.

The retry-semantics PASS did not itself authorize a fresh attempt. The failed
audit-v1 and audit-v2 roots remain immutable and may not be resumed, migrated,
deleted, relabeled, or treated as evidence.

## S3a terminal packet

Claude authorized the one-shot screen at exact clean detached
`14548d3da31c3cfe899cbd7e572614ae05242c0a`. The Mini supervisor then ran all
eight 64-state shards over seeds 136,000,000–136,000,511 with 120 disjoint
report worlds and equal exact candidate-world work. All child exits were zero.

Terminal evidence:

- run `s3a-bury-v2-screen-136m-v1`;
- aggregate SHA-256
  `74aa5a3947e1daaa5aa4bc33eef8ae04eaaf695d0cb900c7045eb0cbbc4396cd`;
- supervisor-final SHA-256
  `d3f2b1ab48085ccf37534b5dd7f20ea6cf0d7644c6c49304b644ecf895169a6b`;
- separate verifier: `verified=true`, `AUTHORIZE_DUEL_DESIGN`;
- no partials or symlinks; retry/resume false; production promotion false.

| state-level contrast | mean gain | 95% half-width | 95% LCB |
|---|---:|---:|---:|
| structured − live incumbent | `+0.997314` | `0.400606` | `+0.596708` |
| structured − legacy-four | `+0.877848` | `0.379885` | `+0.497963` |
| structured − trigger-matched random widening | `+3.252848` | `0.561197` | `+2.691652` |

All three frozen LCB criteria passed. The next S3a action is implementation
and independent review of a **fresh mirrored full-game duel** against exact
`mc-s0-report-lcb`, including a champion-matched null. Do not reuse the 136M
screen population, tune the mechanism from its per-state records, or deploy
from this result.

## Standing rules

- A mechanism screen is not a full-game strength claim.
- Failed or consumed evidence namespaces are immutable; no same-recipe retry.
- Review markers authorize only the scope they name.
- Experimental posterior-changing sampler/ballot flags remain off.
- Keep one reviewed job ready per useful host, but continue implementation
  while review or compute runs; utilization never justifies moving a gate.
- All commits made for this work are pushed to GitHub.
