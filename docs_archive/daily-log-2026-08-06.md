# Daily log — 2026-08-06

**IN PROGRESS.** This log records evidence only through the timestamped updates
below. Production remains compiled `mc-strong` (N=30); no S0 policy has been
deployed.

## Overnight: S0a completed and selected one mechanism

At 06:58 EDT all eight authoritative Mini S0a shards completed cleanly from the
detached frozen worktree at
`be1e39cd9281f752d610ff770f6a280098024388`. The run covered the exact 2,048
fresh mirrored deal clusters on seeds 132,000,000–132,002,047. Every label has
4,096 seat/team-flipped records; all sampler attempts reconcile, with zero
failed worlds, short searches, zero-world decisions or void-relaxed fallbacks.
All manifests agree on Mini, Python 3.14.6, strict voids and compiled binary
SHA-256 `9c9e77fbdc4c6caceec195465155f37ec6369e409462fd838bc142bf8a0be4c1`.

The registered frozen-source aggregate selected `mc-s0-report-lcb`:

| paired signed level utility | result |
|---|---:|
| report-LCB minus current | `+0.352539 +/- 0.068893` |
| report-LCB minus equal-work uniform | `+0.293457 +/- 0.065645` |
| report-mean minus current | `+0.272461 +/- 0.070863` |
| report-mean minus equal-work uniform | `+0.213379 +/- 0.072245` |
| equal-work uniform minus current | `+0.059082 +/- 0.066679` |
| true null minus current | `+0.008301 +/- 0.070050` |

Aggregate SHA-256 is
`0fcd53d4f782a705bfef9ea8ec6155c49db45d76ec71ce25891a9f864413de49`.
An independent invocation reproduced the same JSON; the only byte-level
difference was the stored file's absent final newline.

The main inference is narrower and more useful than “more MC wins.” Equal-work
uniform did not establish a gain, while both policies using a disjoint R=300
report fold did. The report/validation rule—not extra work by itself—explains
the S0a improvement. S0a is a mechanism screen and cannot promote anything to
production.

## Runtime and audit provenance hardened

The overnight review closed a real cross-phase audit gap before any child result
could be accepted:

- child manifests and aggregates now bind the exact parent aggregate and its
  runtime identity;
- the terminal packet refuses within-phase, cross-phase or frozen-runtime
  drift;
- the supervisor hashes the runner, aggregator and packet verifier and refuses
  audit-tool drift;
- aggregation and packet verification import policies/evaluation from the
  detached frozen source tree, not whichever main checkout happens to invoke
  them;
- the frozen-source reproduction command and hashes are durable in the active
  handoff.

These repairs landed in `a114716`, `6fe5f44`, `a9c33c3` and `a8cdb7a`. They did
not alter the frozen S0 policy, work allocation, seed blocks or evidence already
running.

## S0b-LCB launched from the exact S0a parent

At 06:58 EDT the singleton supervisor admitted and launched exactly eight
`s0b-lcb` shards on Mini. Their contiguous seed blocks cover
134,000,000–134,002,047; every partial manifest binds the exact S0a aggregate
hash and survivor and repeats the frozen SHA/runtime/native identity. Air is
fallback only, remains unreachable, and may never be pooled into Mini.

S0b isolates allocation under the already-selected report-LCB rule. It compares
deterministic adaptive allocation with uniform report allocation and a matched
random-allocation attribution control at exact work. Adaptive survives only if
both registered paired point estimates—adaptive minus uniform and adaptive
minus random—are positive. Otherwise report-LCB advances unchanged.

At 09:18 EDT all eight workers were live with zero stderr, had completed the
first adaptive label and had entered the uniform-report label together. Random
allocation follows. There are no final S0b manifests and partial game scores
are intentionally unscored.

## Parallel terminal closeout work

While S0b runs, terminal-only operations are being made fail-closed without
touching the pinned runner, aggregator, packet generator or live supervisor.
`server/scripts/s0_closeout.py` independently regenerates the terminal packet,
requires byte identity with the durable copy, checks every handoff field and
defaults to read-only verification. Its explicit cleanup mode refuses a
nonterminal state or a live worker and removes only the exact reached Mini S0
launch labels, keepawake process and terminal supervisor. A follow-up audit
caught that the submitted supervisor and workers are keep-alive: the first
cleanup guard could therefore refuse forever while launchd restarted processes
whose immutable final outputs had already sealed. Cleanup now derives the exact
reached worker labels from the independently verified terminal packet and may
remove only those completed workers even if a collision-restart PID is live;
any unproved live worker still refuses. Keepawake and the running terminal
supervisor are removed last. Unit tests cover
packet drift, missing fields, nonterminal refusal, active-worker refusal and
exact service scope. At 08:58 EDT the focused S0 closeout/protocol/search suite
passed 31 tests; the later cleanup regression passed 18 focused tests, and
bytecode compilation plus `git diff --check` were clean.

Completed S0a launch services were removed after their artifacts sealed so they
could not repeatedly restart and collide with immutable outputs. The evidence
files and aggregate were untouched.

## Parallel V11 protected-anchor implementation

The V11 lane is deliberately separate from S0, but its bounded code path was
completed while Mini computed. Two experimental policies now preserve the
complete production MC ballot, all N=30 common worlds, the five-point anchor
margin and `TRACTOR_LOCK`:

- `mc-v11anchor` moves frozen v11pair's thresholded proposal to protected
  candidate 0 while retaining Smart's action; and
- `mc-v11anchor-random` fires on the same states but protects a separately
  seeded random non-Smart action, isolating learned proposal quality from the
  generic effect of changing candidate 0.

The frozen NPZ is SHA-256 pinned and a missing or changed checkpoint refuses
rather than falling back. Shared numpy weights are cached read-only. Decision
records bind the action-set hash, Smart/v11/protected actions and indices,
threshold/delta/trigger, MC deltas, actual played action and fallback reason.
Ballot provenance now records that override/racing/anchor nets score the MC
root ballot—not the unrelated RL leaf enumeration.

`server/scripts/v11_revalidate.py` implements the already-predeclared direct
compatibility block: eight 256-cluster shards on fresh seeds
121,000,000–121,002,047, frozen v11pair/current/true-null, strict counters and
a two-superiority-LCB plus clean-null gate. This block may authorize only a
later protected-anchor experiment; it cannot promote production. The later
anchor-strength reference and seed block remain intentionally unfrozen until
S0 names the terminal champion.

At 11:56 EDT the full direct-current prerequisite launched 8/8 shards on Air
from a new detached clean worktree at exact commit
`e66b90bc3a50d514472670ea99909add5ea30d19`. The existing Air compute checkout
and its untracked user swap file were left untouched. A fresh compiled extension
was built in the frozen worktree; the two-cluster smoke passed before launch.
All eight partial manifests then agreed on shards 0–7, contiguous 256-cluster
seed ranges spanning 121,000,000–121,002,047, Python 3.14.6, clean tree,
compiled+strict execution, identical ballots/policy/digest contracts and frozen
checkpoint SHA-256
`cd89d6ed7e9d5f798d69ce546107c4dfbef682c5385de39af527026e39e1c003`.
Eight workers were live at roughly 94–97% CPU. Partial effects remain sealed.

## Parallel teacher-v1 entry-gate implementation

The bounded code through Teacher-v1 Stage B is also ready; no teacher evidence
has run. It provides:

- fresh one-state-per-deal replay capture from seeds >=120M, with pre-play
  phase/decision/pool targeting and digest-pinned exclusion of original, late
  and deep-lead DEV/CALIB/REPORT identities;
- exact declaration, bury, lead and follow replay under a pinned production
  N=30 actor;
- a 64-state Stage-A freeze (four representative rows per phase/role/decision
  cell plus eight boundary and eight uncertainty challenges), 256 selection +
  256 report common worlds, and a second deterministic execution gate;
- a disjoint 128-state Stage-B freeze (eight representative rows per cell plus
  16 boundary and 16 uncertainty challenges), followed by 64+64 gold worlds
  whose downstream decisions use separately seeded production `mc-strong`
  N=30; and
- a one-sided clustered gold-report regret gate at <=0.10 signed levels before
  Stage C can even be implemented or launched.

Labels retain the dense world-by-candidate tensors for attacker points, acting-
team signed points, uncapped house-rule bracket and signed possession utility.
Representative inclusion probabilities are honest reservoir probabilities;
deterministically chosen challenge rows are explicitly not deployment-
weightable. Real runs fail on dirty/non-compiled/non-strict provenance, input
or source drift, bad ballots, incomplete work, any sampler failure, duplicate
deals, wrong stratum composition or a changed target schema.

At 09:34 EDT the focused V11/teacher/evaluator/pilot suite passed 138 tests with
two optional skips; the complete server suite then passed 405 tests with the
same two skips. All new entry points byte-compiled, and a fresh one-deal
capture+selector-diagnostic smoke completed. A compiled+strict two-cluster V11
direct-revalidation smoke also completed all arm/null/reference records with no
manifest problems; it is explicitly `promotable:false`. This is mechanics
evidence only, not a data-quality or strength result.

A 09:59 pre-run review found and closed two additional identity gaps before
either lane spent evidence compute. Frozen v11 registry construction no longer
prefers a caller-cwd relative lookalike; the policy exposes its actual absolute
loaded path/SHA and `v11_revalidate.py` requires those exact bytes to equal the
manifest's pinned NPZ. Teacher Stage-B freezing no longer accepts a Stage-A
file merely because its schema matches: it validates experiment, stage,
completion, internal state digest, exact 64-state composition, unique deals,
runtime/actor/diagnostic population identity and every replay. It also requires
the exact Stage-A mechanics PASS artifact bound to that state-set SHA. The
focused V11/teacher regression now passes 54 tests.

## S2 self-play correctness boundary advanced

The next bounded backlog audit repaired three implementation defects without
restarting the invalid historical DMC2 experiment:

- learner ingestion now applies one acting-team sign to both the terminal
  return and attacker-perspective oracle baseline, so defender targets are the
  exact negative of attacker targets;
- actor, opponent, gate and evaluation workers receive immutable checkpoint
  paths plus SHA-256 identities instead of mutable `generator.pt` /
  `candidate.pt` filenames; and
- a passing gate promotes the exact snapshot that played the duel. Previously
  it evaluated one candidate, let training continue, then wrote the newer and
  unevaluated learner weights into the generator path.

The retained legacy clipped reward is now explicitly named
`dmc2-clipped-level-possession-v1`. Actor-batch seeds derive from experiment,
root seed, purpose, sequence and actor digest rather than wall clock; every
batch records its seed, actor and opponent-pool identities. Snapshots publish
atomically and never overwrite, and the run manifest declares
`promotion_evidence:false`, `faithful_suphx_or_douzero:false` and
`resume_supported:false`.

Seven focused falsification tests and the complete 415-test server suite pass
(two optional skips). A six-second bounded smoke completed 18
one-round actor batches with 18 unique named seeds and one immutable actor
generation. No optimizer step or strength gate ran. The remaining S2 launch
blocker is exact interrupted/resumed learner, optimizer, replay-ring, RNG and
pending-job reproduction; only after that should the independent faithful
Suphx- and DouZero-style microbaselines be implemented and run.

## First teacher entry packet predeclared

Before any teacher evidence capture, the first bounded packet was frozen as
`teacher-v1-entry-120m-v1`: exactly 1,024 fresh deals at seeds
120,000,000–120,001,023 in eight interleaved shards. The same eight immutable
diagnostic artifacts feed 64-state Stage A and disjoint 128-state Stage B.
Stage A uses eight primary plus eight deterministic-rerun label shards; Stage B
uses eight cheap shards and one exact-parent gold shard per cheap shard. A
pre-label supply deficit closes the packet inconclusive rather than authorizing
an append, replacement seed or moved quota. No capture or label job has started.

## Documentation and naming cleanup

The August 5 daily log was expanded to cover the entire day's correctness,
evaluation, sampler, teacher/RL, high-N and S0 work through its honest 23:59
running state. The protected v11 anchor work was renamed everywhere as a
separate strength lane: it is valuable, but it is not “S0b” and is not required
to close this milestone.

A deployment audit also resolved stale documentation around the real policy
control point. Local source launches fall back to `mc` (N=10), but Fly
explicitly pins compiled `mc-strong` (N=30) through `SHENGJI_BOT`. A terminal
S0 SELECT NONE leaves that setting unchanged; PROMOTE requires a reviewed
config commit to the exact confirmed survivor plus a `/healthz` policy/compiled
engine check. The live deploy remains a separate quiet-room operation because
restarting the single stateful process disconnects active games.

## 10:50 independent review and closeout repair

An independent review found that terminal cleanup's documented unexpected-
worker refusal was not implemented in its real CLI path. The cleanup set was
derived only from the terminal packet, so the old active-service check could
not see a live worker from an unreached S0 branch. `s0_closeout.py` now scans
the complete loaded S0-worker namespace before any removal and refuses every
phase/shard label outside the packet-proved chain. A regression exercises
`main()` with a terminal packet plus an injected live
`com.shengji.s0mini.s0c_adaptive_lcb.0`; cleanup refuses before mutation. The
focused closeout/protocol/search suite passes 33 tests; bytecode compilation
and `git diff --check` are clean. The pinned runner, aggregator, packet
generator, supervisor and live S0b files were not changed.

The same review correctly placed teacher capture on HOLD: its predeclared 120M
packet exists in prose, but current diagnostic/state/gate artifacts do not bind
the exact eight capture shards and 1,024-deal seed range. That separate lane
must add exact packet/coverage provenance and wrong-range, missing-shard and
repeated-shard falsification tests before any evidence capture. This does not
block S0.

At 10:54 EDT S0b remained healthy and sealed from inspection: all eight
original workers were on `report_uniform` in the 300–399/512 bucket, output
bytes had advanced, every stderr was empty, and a PID-scoped sample showed all
eight workers at 99.6–100% CPU with `runs=1`.

At 11:44 EDT the supervisor confirmed all eight shards had completed the exact
512-round uniform-report arm and entered `mc-s0-random` versus `mc-strong`, the
third and final S0b arm. All eight original services remained active, no final
manifest existed and partial effects remained sealed.

At 11:59 EDT the S0b workers remained live together on that final random arm
and record bytes continued advancing. Mini therefore remained fully assigned
to the authoritative S0 chain while Air ran the independent V11 compatibility
block. The next local work was made explicit in the backlog instead of idling:
evaluator cutoff semantics, the bounded frontend soak, S3a structured bury
search and then separately S3b sampled exact endgame.

## Frontend ship gate closed

At 12:10 EDT the bounded multiplayer/frontend gate passed. The real ASGI
multi-WebSocket suite passed 33 tests spanning join and seat races,
disconnect-to-bot grace, reconnect/takeover, stale and displaced sockets,
second absence, token rotation, exactly-once bot actions and private-hand
continuity/visibility. The browser connection/intent suite passed 14 tests
covering chat before initial state, reconnect deduplication, cross-room
isolation and invite-over-saved-session precedence.

The one missing declared boundary was added without changing behavior: after
60 real-wire chat posts, a late socket received exactly the most recent 50 with
contiguous monotonic ids, while the client correctly replaced the reconnect
snapshot and did not duplicate the next live event. Lint retained only existing
React fast-refresh warnings and the TypeScript/Vite production build passed.

## Legacy full-game cutoff repaired

The old `ai.env.play_game` behavior compared partial levels after 200 rounds
and awarded the tie to team 0. That path is now fail-closed: every unfinished
max-round exhaustion raises typed `FullGameCutoff`, whether levels tie or one
team happens to lead. Seed, partial levels, game and logs remain attached only
for diagnosis; no winner is exposed. Legacy mirrored evaluation propagates the
refusal and returns no partial score, including when its first flip completed.

Completed-game tuple results remain compatible, and the registered evaluator
continues to use one-round `play_round`, so current S0/V11 evidence is
unaffected. Engine house-rule level progression and the separately versioned
clipped RL target were untouched. Independent focused verification passed 25
tests; the broader game/invariant/evaluator selection passed 46.

## What remains for S0

1. Complete all eight frozen S0b-LCB shards and independently verify their exact
   aggregate. Do not pool Air, inspect partial effects or extend the block.
2. Let the registered survivor select exactly one S0c phase. Run its eight
   1,024-cluster shards on independent seeds 135,000,000–135,008,191.
3. Promote only if survivor-current and survivor-null paired 95% lower bounds
   are both above zero while null-current does not clear. Otherwise close
   **SELECT NONE** and keep production on `mc-strong`.
4. Regenerate and byte-check the terminal packet, verify every artifact/hash,
   counter, coverage and provenance field, clean the exact completed services,
   update the durable docs, run the final tests, commit and push.

No terminal S0 conclusion has been reached yet.
