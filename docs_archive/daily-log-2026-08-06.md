# Daily log — 2026-08-06

**IN PROGRESS.** This log records evidence only through the timestamped updates
below. Production remains compiled `mc-strong` (N=30); no S0 policy has been
deployed.

## Overnight: S0a completed and selected one mechanism

At 06:58 EDT all eight authoritative Mini S0a shards completed cleanly from the
detached frozen worktree at
`be1e39cd9281f752d610ff770f6a280098024388`. The run covered the exact 2,048
fresh mirrored deal clusters on seeds 132,000,000–132,002,047. Every label has
4,096 seat/team-flipped records; all sampler attempts reconcile, with zero
failed worlds, short searches, zero-world decisions or void-relaxed fallbacks.
All manifests agree on Mini, Python 3.14.6, strict voids and compiled binary
SHA-256 `9c9e77fbdc4c6caceec195465155f37ec6369e409462fd838bc142bf8a0be4c1`.

The registered frozen-source aggregate selected `mc-s0-report-lcb`:

| paired signed level utility | result |
|---|---:|
| report-LCB minus current | `+0.352539 +/- 0.068893` |
| report-LCB minus equal-work uniform | `+0.293457 +/- 0.065645` |
| report-mean minus current | `+0.272461 +/- 0.070863` |
| report-mean minus equal-work uniform | `+0.213379 +/- 0.072245` |
| equal-work uniform minus current | `+0.059082 +/- 0.066679` |
| true null minus current | `+0.008301 +/- 0.070050` |

Aggregate SHA-256 is
`0fcd53d4f782a705bfef9ea8ec6155c49db45d76ec71ce25891a9f864413de49`.
An independent invocation reproduced the same JSON; the only byte-level
difference was the stored file's absent final newline.

The main inference is narrower and more useful than “more MC wins.” Equal-work
uniform did not establish a gain, while both policies using a disjoint R=300
report fold did. The report/validation rule—not extra work by itself—explains
the S0a improvement. S0a is a mechanism screen and cannot promote anything to
production.

## Runtime and audit provenance hardened

The overnight review closed a real cross-phase audit gap before any child result
could be accepted:

- child manifests and aggregates now bind the exact parent aggregate and its
  runtime identity;
- the terminal packet refuses within-phase, cross-phase or frozen-runtime
  drift;
- the supervisor hashes the runner, aggregator and packet verifier and refuses
  audit-tool drift;
- aggregation and packet verification import policies/evaluation from the
  detached frozen source tree, not whichever main checkout happens to invoke
  them;
- the frozen-source reproduction command and hashes are durable in the active
  handoff.

These repairs landed in `a114716`, `6fe5f44`, `a9c33c3` and `a8cdb7a`. They did
not alter the frozen S0 policy, work allocation, seed blocks or evidence already
running.

## S0b-LCB launched from the exact S0a parent

At 06:58 EDT the singleton supervisor admitted and launched exactly eight
`s0b-lcb` shards on Mini. Their contiguous seed blocks cover
134,000,000–134,002,047; every partial manifest binds the exact S0a aggregate
hash and survivor and repeats the frozen SHA/runtime/native identity. Air is
fallback only, remains unreachable, and may never be pooled into Mini.

S0b isolates allocation under the already-selected report-LCB rule. It compares
deterministic adaptive allocation with uniform report allocation and a matched
random-allocation attribution control at exact work. Adaptive survives only if
both registered paired point estimates—adaptive minus uniform and adaptive
minus random—are positive. Otherwise report-LCB advances unchanged.

At 09:18 EDT all eight workers were live with zero stderr, had completed the
first adaptive label and had entered the uniform-report label together. Random
allocation follows. There are no final S0b manifests and partial game scores
are intentionally unscored.

## Parallel terminal closeout work

While S0b runs, terminal-only operations are being made fail-closed without
touching the pinned runner, aggregator, packet generator or live supervisor.
`server/scripts/s0_closeout.py` independently regenerates the terminal packet,
requires byte identity with the durable copy, checks every handoff field and
defaults to read-only verification. Its explicit cleanup mode refuses a
nonterminal state or a live worker and removes only the exact reached Mini S0
launch labels, keepawake process and terminal supervisor. A follow-up audit
caught that the submitted supervisor and workers are keep-alive: the first
cleanup guard could therefore refuse forever while launchd restarted processes
whose immutable final outputs had already sealed. Cleanup now derives the exact
reached worker labels from the independently verified terminal packet and may
remove only those completed workers even if a collision-restart PID is live;
any unproved live worker still refuses. Keepawake and the running terminal
supervisor are removed last. Unit tests cover
packet drift, missing fields, nonterminal refusal, active-worker refusal and
exact service scope. At 08:58 EDT the focused S0 closeout/protocol/search suite
passed 31 tests; the later cleanup regression passed 18 focused tests, and
bytecode compilation plus `git diff --check` were clean.

Completed S0a launch services were removed after their artifacts sealed so they
could not repeatedly restart and collide with immutable outputs. The evidence
files and aggregate were untouched.

## Parallel V11 protected-anchor implementation

The V11 lane is deliberately separate from S0, but its bounded code path was
completed while Mini computed. Two experimental policies now preserve the
complete production MC ballot, all N=30 common worlds, the five-point anchor
margin and `TRACTOR_LOCK`:

- `mc-v11anchor` moves frozen v11pair's thresholded proposal to protected
  candidate 0 while retaining Smart's action; and
- `mc-v11anchor-random` fires on the same states but protects a separately
  seeded random non-Smart action, isolating learned proposal quality from the
  generic effect of changing candidate 0.

The frozen NPZ is SHA-256 pinned and a missing or changed checkpoint refuses
rather than falling back. Shared numpy weights are cached read-only. Decision
records bind the action-set hash, Smart/v11/protected actions and indices,
threshold/delta/trigger, MC deltas, actual played action and fallback reason.
Ballot provenance now records that override/racing/anchor nets score the MC
root ballot—not the unrelated RL leaf enumeration.

`server/scripts/v11_revalidate.py` implements the already-predeclared direct
compatibility block: eight 256-cluster shards on fresh seeds
121,000,000–121,002,047, frozen v11pair/current/true-null, strict counters and
a two-superiority-LCB plus clean-null gate. This block may authorize only a
later protected-anchor experiment; it cannot promote production. The later
anchor-strength reference and seed block remain intentionally unfrozen until
S0 names the terminal champion.

At 11:56 EDT the full direct-current prerequisite launched 8/8 shards on Air
from a new detached clean worktree at exact commit
`e66b90bc3a50d514472670ea99909add5ea30d19`. The existing Air compute checkout
and its untracked user swap file were left untouched. A fresh compiled extension
was built in the frozen worktree; the two-cluster smoke passed before launch.
All eight partial manifests then agreed on shards 0–7, contiguous 256-cluster
seed ranges spanning 121,000,000–121,002,047, Python 3.14.6, clean tree,
compiled+strict execution, identical ballots/policy/digest contracts and frozen
checkpoint SHA-256
`cd89d6ed7e9d5f798d69ce546107c4dfbef682c5385de39af527026e39e1c003`.
Eight workers were live at roughly 94–97% CPU. Partial effects remain sealed.

## Parallel teacher-v1 entry-gate implementation

The bounded code through Teacher-v1 Stage B is also ready; no teacher evidence
has run. It provides:

- fresh one-state-per-deal replay capture from seeds >=120M, with pre-play
  phase/decision/pool targeting and digest-pinned exclusion of original, late
  and deep-lead DEV/CALIB/REPORT identities;
- exact declaration, bury, lead and follow replay under a pinned production
  N=30 actor;
- a 64-state Stage-A freeze (four representative rows per phase/role/decision
  cell plus eight boundary and eight uncertainty challenges), 256 selection +
  256 report common worlds, and a second deterministic execution gate;
- a disjoint 128-state Stage-B freeze (eight representative rows per cell plus
  16 boundary and 16 uncertainty challenges), followed by 64+64 gold worlds
  whose downstream decisions use separately seeded production `mc-strong`
  N=30; and
- a one-sided clustered gold-report regret gate at <=0.10 signed levels before
  Stage C can even be implemented or launched.

Labels retain the dense world-by-candidate tensors for attacker points, acting-
team signed points, uncapped house-rule bracket and signed possession utility.
Representative inclusion probabilities are honest reservoir probabilities;
deterministically chosen challenge rows are explicitly not deployment-
weightable. Real runs fail on dirty/non-compiled/non-strict provenance, input
or source drift, bad ballots, incomplete work, any sampler failure, duplicate
deals, wrong stratum composition or a changed target schema.

At 09:34 EDT the focused V11/teacher/evaluator/pilot suite passed 138 tests with
two optional skips; the complete server suite then passed 405 tests with the
same two skips. All new entry points byte-compiled, and a fresh one-deal
capture+selector-diagnostic smoke completed. A compiled+strict two-cluster V11
direct-revalidation smoke also completed all arm/null/reference records with no
manifest problems; it is explicitly `promotable:false`. This is mechanics
evidence only, not a data-quality or strength result.

A 09:59 pre-run review found and closed two additional identity gaps before
either lane spent evidence compute. Frozen v11 registry construction no longer
prefers a caller-cwd relative lookalike; the policy exposes its actual absolute
loaded path/SHA and `v11_revalidate.py` requires those exact bytes to equal the
manifest's pinned NPZ. Teacher Stage-B freezing no longer accepts a Stage-A
file merely because its schema matches: it validates experiment, stage,
completion, internal state digest, exact 64-state composition, unique deals,
runtime/actor/diagnostic population identity and every replay. It also requires
the exact Stage-A mechanics PASS artifact bound to that state-set SHA. The
focused V11/teacher regression now passes 54 tests.

## S2 self-play correctness boundary advanced

The next bounded backlog audit repaired three implementation defects without
restarting the invalid historical DMC2 experiment:

- learner ingestion now applies one acting-team sign to both the terminal
  return and attacker-perspective oracle baseline, so defender targets are the
  exact negative of attacker targets;
- actor, opponent, gate and evaluation workers receive immutable checkpoint
  paths plus SHA-256 identities instead of mutable `generator.pt` /
  `candidate.pt` filenames; and
- a passing gate promotes the exact snapshot that played the duel. Previously
  it evaluated one candidate, let training continue, then wrote the newer and
  unevaluated learner weights into the generator path.

The retained legacy clipped reward is now explicitly named
`dmc2-clipped-level-possession-v1`. Actor-batch seeds derive from experiment,
root seed, purpose, sequence and actor digest rather than wall clock; every
batch records its seed, actor and opponent-pool identities. Snapshots publish
atomically and never overwrite, and the run manifest declares
`promotion_evidence:false`, `faithful_suphx_or_douzero:false` and
`resume_supported:false`.

Seven focused falsification tests and the complete 415-test server suite pass
(two optional skips). A six-second bounded smoke completed 18
one-round actor batches with 18 unique named seeds and one immutable actor
generation. No optimizer step or strength gate ran. The remaining S2 launch
blocker is exact interrupted/resumed learner, optimizer, replay-ring, RNG and
pending-job reproduction; only after that should the independent faithful
Suphx- and DouZero-style microbaselines be implemented and run.

## First teacher entry packet predeclared

Before any teacher evidence capture, the first bounded packet was frozen as
`teacher-v1-entry-120m-v1`: exactly 1,024 fresh deals at seeds
120,000,000–120,001,023 in eight interleaved shards. The same eight immutable
diagnostic artifacts feed 64-state Stage A and disjoint 128-state Stage B.
Stage A uses eight primary plus eight deterministic-rerun label shards; Stage B
uses eight cheap shards and one exact-parent gold shard per cheap shard. A
pre-label supply deficit closes the packet inconclusive rather than authorizing
an append, replacement seed or moved quota. No capture or label job has started.

## Documentation and naming cleanup

The August 5 daily log was expanded to cover the entire day's correctness,
evaluation, sampler, teacher/RL, high-N and S0 work through its honest 23:59
running state. The protected v11 anchor work was renamed everywhere as a
separate strength lane: it is valuable, but it is not “S0b” and is not required
to close this milestone.

A deployment audit also resolved stale documentation around the real policy
control point. Local source launches fall back to `mc` (N=10), but Fly
explicitly pins compiled `mc-strong` (N=30) through `SHENGJI_BOT`. A terminal
S0 SELECT NONE leaves that setting unchanged; PROMOTE requires a reviewed
config commit to the exact confirmed survivor plus a `/healthz` policy/compiled
engine check. The live deploy remains a separate quiet-room operation because
restarting the single stateful process disconnects active games.

## 10:50 independent review and closeout repair

An independent review found that terminal cleanup's documented unexpected-
worker refusal was not implemented in its real CLI path. The cleanup set was
derived only from the terminal packet, so the old active-service check could
not see a live worker from an unreached S0 branch. `s0_closeout.py` now scans
the complete loaded S0-worker namespace before any removal and refuses every
phase/shard label outside the packet-proved chain. A regression exercises
`main()` with a terminal packet plus an injected live
`com.shengji.s0mini.s0c_adaptive_lcb.0`; cleanup refuses before mutation. The
focused closeout/protocol/search suite passes 33 tests; bytecode compilation
and `git diff --check` are clean. The pinned runner, aggregator, packet
generator, supervisor and live S0b files were not changed.

The same review correctly placed teacher capture on HOLD: its predeclared 120M
packet exists in prose, but current diagnostic/state/gate artifacts do not bind
the exact eight capture shards and 1,024-deal seed range. That separate lane
must add exact packet/coverage provenance and wrong-range, missing-shard and
repeated-shard falsification tests before any evidence capture. This does not
block S0.

At 10:54 EDT S0b remained healthy and sealed from inspection: all eight
original workers were on `report_uniform` in the 300–399/512 bucket, output
bytes had advanced, every stderr was empty, and a PID-scoped sample showed all
eight workers at 99.6–100% CPU with `runs=1`.

At 11:44 EDT the supervisor confirmed all eight shards had completed the exact
512-round uniform-report arm and entered `mc-s0-random` versus `mc-strong`, the
third and final S0b arm. All eight original services remained active, no final
manifest existed and partial effects remained sealed.

At 11:59 EDT the S0b workers remained live together on that final random arm
and record bytes continued advancing. Mini therefore remained fully assigned
to the authoritative S0 chain while Air ran the independent V11 compatibility
block. The next local work was made explicit in the backlog instead of idling:
evaluator cutoff semantics, the bounded frontend soak, S3a structured bury
search and then separately S3b sampled exact endgame.

## Frontend ship gate closed

At 12:10 EDT the bounded multiplayer/frontend gate passed. The real ASGI
multi-WebSocket suite passed 33 tests spanning join and seat races,
disconnect-to-bot grace, reconnect/takeover, stale and displaced sockets,
second absence, token rotation, exactly-once bot actions and private-hand
continuity/visibility. The browser connection/intent suite passed 14 tests
covering chat before initial state, reconnect deduplication, cross-room
isolation and invite-over-saved-session precedence.

The one missing declared boundary was added without changing behavior: after
60 real-wire chat posts, a late socket received exactly the most recent 50 with
contiguous monotonic ids, while the client correctly replaced the reconnect
snapshot and did not duplicate the next live event. Lint retained only existing
React fast-refresh warnings and the TypeScript/Vite production build passed.

## Legacy full-game cutoff repaired

The old `ai.env.play_game` behavior compared partial levels after 200 rounds
and awarded the tie to team 0. That path is now fail-closed: every unfinished
max-round exhaustion raises typed `FullGameCutoff`, whether levels tie or one
team happens to lead. Seed, partial levels, game and logs remain attached only
for diagnosis; no winner is exposed. Legacy mirrored evaluation propagates the
refusal and returns no partial score, including when its first flip completed.

Completed-game tuple results remain compatible, and the registered evaluator
continues to use one-round `play_round`, so current S0/V11 evidence is
unaffected. Engine house-rule level progression and the separately versioned
clipped RL target were untouched. Independent focused verification passed 25
tests; the broader game/invariant/evaluator selection passed 46.

## What remains for S0

1. Complete all eight frozen S0b-LCB shards and independently verify their exact
   aggregate. Do not pool Air, inspect partial effects or extend the block.
2. Let the registered survivor select exactly one S0c phase. Run its eight
   1,024-cluster shards on independent seeds 135,000,000–135,008,191.
3. Promote only if survivor-current and survivor-null paired 95% lower bounds
   are both above zero while null-current does not clear. Otherwise close
   **SELECT NONE** and keep production on `mc-strong`.
4. Regenerate and byte-check the terminal packet, verify every artifact/hash,
   counter, coverage and provenance field, clean the exact completed services,
   update the durable docs, run the final tests, commit and push.

No terminal S0 conclusion has been reached yet.

## S3 structured decisions: bounded cores landed behind flags

While Mini and Air continued their sealed evidence blocks, two orthogonal
strength mechanisms reached a code-only boundary. Structured bury now expands
the old four hand-authored variants into a deterministic capped ballot of up to
32 banker-visible point-, trump-, pair- and void-oriented buries. It keeps the
incumbent as candidate zero, uses common sampled worlds, records exact work and
replay state, and falls back loudly if its dose underfills. This finally tests
the combinatorial burial hypothesis the old four-variant tie did not test.

The sampled exact-endgame core exhaustively enumerates legal submitted actions
when no hand has more than four cards, resolves failed throws through the real
engine and computes partnership minimax final attacker points. Its MC seam can
run only inside a marked determinized world and refuses live-state use, states
outside the bound or an exhausted node cap. Five real four-card mechanics
states solved in 0.26–0.69 seconds with 9k–24k nodes. The combined focused
teacher/pilot/S3/evaluator selection passed 124 tests.

Both mechanisms remain feature-off and have zero strength evidence. Structured
bury next needs a disjoint report fold and equal-work legacy/random controls;
exact endgame needs one shared solver/cache per common world because repeating
a fresh solve for every candidate is too slow. Their eventual challenge and
paired duel references wait for S0's terminal champion, so this work did not
compete with or contaminate either live evidence block.

At 12:30 EDT both evidence machines remained productively assigned. Mini's
eight original S0b workers were live at roughly 80–82% CPU on the final random-
allocation arm, with eight partial manifests and no final. Air's eight V11
direct-compatibility workers were live at roughly 88–92% CPU, likewise with
eight partial manifests and no final. No partial effect was opened.

## 12:59 — S3 core and teacher-entry provenance gate pushed

The S3 review found and closed the last compatibility/performance boundary
before landing `b807ad1`. Exact endgame now passes a world-scoped
`ExactWorldSession` through every registered rollout override, so the feature-
off seam does not break the existing value-leaf policies. The session is shared
across candidate frontiers only within one ordinary-play determinization and is
keyed by full banker/trump/kitty context; malformed hidden-hand sizes or card
conservation refuse. Independent review passed 428 server tests with 25 skips,
30 compiled-rules checks, 58 candidate-frontier parity comparisons on five
real late states, and measured roughly 1.9–6.1x work / 1.9–5.7x time savings
from sharing. S3b therefore moved from performance-code HOLD to protocol next:
freeze a cumulative 250k-node challenge, require zero refusals, and expose exact
nodes/cache hits. It remains a sampled perfect-information oracle, not an exact
imperfect-information Shengji solver.

Teacher-v1's entry gate landed separately as `23a9e0b`. The exact 1,024-deal
packet and all eight shard/range identities now survive capture, diagnostics,
state freezing, canonical label partitioning and both gates. A new exclusive
pre-label receipt binds each population's role, run id, nonce, exact state-set
bytes and executable digests. Stage A refuses reused receipt SHA/nonces before
PASS; Stage-B freeze reopens and rehashes all 16 Stage-A artifacts and receipts
and repeats partition, record, deterministic and runtime/source checks. Direct
falsification covers wrong range/hash, missing/repeated/swapped shards, copied
metadata, mixed receipts, contradictory stage/mode, source/state drift and
receipt mutation. Independent review returned PASS under the explicit
accidental/malformed-artifact threat model, and py-compile plus teacher/pilot
acceptance passed 134 tests with 23 optional skips. This closes the code-entry
gate only; no capture or teacher label exists yet.

At 12:54 Mini still had eight active S0b workers, eight partial manifests and no
final on the third arm, with reporting tails near 200/512 where present. At
12:57 Air still had all eight V11 process trees plus growing JSONL partials,
eight partial manifests and no final. No partial effect was opened. Both
machines were therefore occupied; teacher capture waits for a whole machine
rather than competing with sealed evidence.

## 13:55 — mechanics challenge closed, V11 composition blinded, cross-audits found real holds

Three additional implementation slices landed while the two evidence machines
continued working. `2370a27` froze the S3b mechanics challenge at four real
late states and four named production-sampler worlds per state. Every one of
140 candidate-frontier exact evaluations completed in 16 shared world sessions
with zero refusal or budget overflow, 130,989 nodes and 97,834 cache hits under
the cumulative 250,000-node/session cap. This closes bounded mechanics only;
it does not establish imperfect-information exactness or full-game strength.

`29c8cc1` added a transactional exact-resume primitive for synchronous RL. It
binds numerical runtime, learner and optimizer topology/state, physical and
logical replay-ring state/cursor, named RNG streams, progress and immutable
actor/candidate bytes, with digest-proved rollback. `5d0c5a1` made every
reachable v11 protected anchor copy the literal terminal search champion rather
than silently falling back to ordinary N=30. These are infrastructure changes,
not learned-policy evidence.

Before any direct-V11 effect was opened, commit `7ecffd5` froze the missing
composition estimand. At that moment a filename-pattern check appeared to show
one final manifest; the 14:37 reconciliation proved it was the predeclared
two-cluster `_SMOKE`, not an evidence shard. Codex had inspected only completion
names, not its body, records or effects, so the blind predeclaration remains
valid.
The original direct-superiority verdict remains unchanged. Separately, a
protocol-valid direct block with a sane null and terminal S0 may admit a
non-promotable 2,048-cluster protected-anchor screen on exact 137M seeds even
if standalone v11 is neutral, because MC can reject bad proposals. The anchor
must beat champion, same-trigger random and champion-matched null by positive
95% lower bounds while the null interval contains zero. Only PASS admits an
independent 8,192-cluster 138M confirmation.

Independent adversarial reviews then found two important reasons not to commit
the next code prematurely. The S2 synchronous coordinator let
`KeyboardInterrupt` escape without poisoning after mutation, did not detect
implicit process-global RNG use, and did not bind mutable learner Python state
outside `state_dict`; each could make resume silently non-exact. The S3a
512-state protocol correctly added disjoint reporting and exact equal-work
legacy/random controls, but its re-opener did not yet redraw named sampled
worlds or replay every persisted scorer value, so self-consistent fabricated
raw evidence could pass. Both repairs and falsification tests are in progress;
no RL or S3 evidence run is authorized until they close.

Air's clean teacher-v1 worktree at `dc6dc2f` passed exact preflight and is
`READY_AFTER_V11`: all split/checkpoint hashes, Python 3.14.6, compiled engine,
capacity and unused output namespace agree. It will launch the staged 8x128
capture immediately after all V11 workers exit and the singleton aggregate
seals. At 13:44 Mini still had eight active S0b workers on the final arm, eight
partials, zero finals and 400/512 visible progress on every shard. At 13:55 Air
had one V11 final manifest and live remaining workers. No partial effect from
either block was inspected.

## 14:17 — three falsification holds closed and pushed

The earlier HOLDs produced concrete correctness improvements rather than being
waived after ordinary tests:

- `e946696` closes the S3a structured-bury evidence re-opener. The 512-state
  protocol reconstructs each named deal, redraws the exact selection and report
  folds, compares ordered draw IDs/world digests/counters/transcripts, and
  replays every raw candidate score with a fresh scorer in frozen call order.
  Valid-format digest tampering and a self-consistent wholesale rewrite of raw
  values now fail. The focused acceptance is 27/27. No state was generated;
  the run remains blocked on terminal S0 and a PASS can authorize only a fresh
  full-game duel design.
- `d2229d0` closes the blind V11 protected-composition runner. It binds the
  direct prerequisite to exact commit `e66b90bc3a50d514472670ea99909add5ea30d19`
  and its source hashes, preserves the direct aggregate's original standalone
  authorization bit, and uses only its protocol validity plus sane matched
  null for admission. Confirmation reopens and rehashes every screen manifest
  and JSONL and recomputes coverage, counters, statistics and authorization
  from raw rows. Champion-matched null policies and score-blind progress were
  added; long artifacts publish by exclusive hard link. Root acceptance is
  44/44; independent broader acceptance is 78/78, and the full server suite
  was 552 passed with two optional skips. No V11 composition evidence ran.
- `e49cf60` closes the generic synchronous RL infrastructure gate. Every
  failed `BaseException` transition poisons the runner; restore rolls back
  transactionally; process-global Python/NumPy/Torch RNG use is forbidden;
  supported learner/optimizer Python configuration outside `state_dict` is
  bound; collector-visible mutable state and algorithm batch identity are
  frozen; and candidate checkpoints use persistent exclusive ownership plus
  no-clobber publication. The focused acceptance is 46/46. This remains an
  algorithm-neutral chassis, not a faithful Suphx/DouZero implementation or a
  strength result. A concrete learner still needs verified actor loading and
  its own uninterrupted-versus-resumed output identity test.

At 14:16 both machines remained fully occupied. Mini had 8/8 healthy S0b
workers, eight partial manifests and no final/failure marker near 100% CPU.
Air had 8/8 healthy direct-V11 workers, eight partial manifests and no
final/failure marker near 91–93% CPU; sanitized flushed rows were about 78.6%.
No partial score or effect from either block was opened. Teacher capture remains
the immediate Air successor after 8/8 V11 finals and one valid singleton
aggregate; S3b's terminal-champion-matched strength protocol is the active
parallel code slice.

## 14:37 — six-hour autonomous execution window begins

Jerry authorized Codex to own the next six hours of execution, keep both hosts
productive, code bounded strength work while jobs run, push every accepted
commit and keep the active handoff, review, backlog, job ledger and daily log
current. The explicit north star remains beating production `mc-strong` N=30.
Closed correctness/code gates count only insofar as they enable trustworthy
strength evidence; they are not substitutes for that result.

The first reconciled checkpoint found Mini's original eight S0b workers healthy
near 98–99% CPU under the singleton supervisor, eight partial manifests and no
final/failure artifact. Air's eight V11 workers were healthy near 88–90% CPU,
with exactly 1,260/1,536 durable rows flushed per shard, eight partial manifests
and zero real finals/failures. The lone final-named file was the old non-evidence
`_SMOKE`, correcting the earlier filename-pattern false positive. No partial
outcome or effect was opened. Codex owns the eventual one-time V11 aggregation
and immediate teacher capture transition; Mini's supervisor remains the sole
owner of S0b -> S0c.

Parallel local work is deliberately bounded: adversarial review of S3b's
terminal-champion-matched sampled-exact strength protocol and score-free
throughput refusal, a concrete role-conditioned direct-Q microbaseline on the
exact synchronous chassis, and an audit of the V11 -> teacher transition. No S3
evidence can launch before terminal S0, and no code-only result changes
production.

## 14:44–14:57 — encoder contract repaired, three code gates pushed, V11 sealed, teacher launched

The transition audit found a high-impact representation bug before reusing the
V11 result. `encode_obs` constructed `Memory(rnd, seat)` while Memory's default
had changed on August 3 to `own_kitty=True`; encoder version stayed 1. Banker
inference therefore placed the banker's private buried cards outside the
`unseen` plane, even though the historical v11 training contract treated kitty
as hidden. The prior encoder test rebuilt the same seed twice and could not
detect this. Commit `66aad44` restores explicit `own_kitty=False`, names schema
`rl-observation-v1-public-no-private-kitty`, binds hashes of both encode and
Memory source, and adds a true counterfactual test that changes hidden kitty
identity without changing banker-visible public state. Focused banker/RL/V11
acceptance passed 29/29.

Three independently reviewed, non-strength code slices then landed and were
pushed:

- `0183cdd` adds a fail-closed singleton teacher entry supervisor. It owns only
  eight captures -> eight literal-parent-hash diagnostics -> exact 64-state
  freeze, with fresh exclusive namespace, clean compiled+strict identity,
  heartbeats, peer termination and no receipt/label/gate code path. Root tests
  passed 44/44.
- `868b6d8` adds a bounded Shengji-specific DouZero-style direct-Q chassis:
  independent attacker/defender GRU action-Q networks, chronological public
  history, direct signed terminal bracket return, immutable actor, narrow
  ordinary-play ballot and explicit Smart setup controls. Interrupted/resumed
  execution matches canonical batches, ordered actions, candidate bytes and all
  learner/optimizer/replay/RNG/progress state. Root matrix passed 73/73. This is
  a one-round code gate, not paper faithfulness or strength.
- `79985a2` adds the S3b terminal-champion-matched strength protocol. A
  score-free two-cluster same-host throughput receipt gates a non-promotable
  2,048-cluster 139M complete-round screen; only both efficacy LCBs, a sane
  matched null, nonzero exact use and zero refusal/overflow admit the disjoint
  8,192-cluster 140M confirmation. Confirmation reopens raw screen evidence.
  The claim is one-round signed level utility only; multi-round progression
  remains a later deployment gate. The root protocol/mechanics matrix passed
  82 tests.

At 14:54 the frozen V11 block reached exactly 8/8 real finals, zero partials,
zero workers and no failure artifact. The singleton aggregate ran once from
exact `e66b90b`, excluding `_SMOKE`. Over 2,048 paired seed clusters,
v11-current was `-0.132324 +/- 0.069737`, v11-null was
`-0.159180 +/- 0.069282`, and null-current was
`+0.026855 +/- 0.067875`. Both efficacy criteria failed, the true null was
sane and `anchor_test_authorized=false`. Aggregate SHA-256 is
`112f2c756235d69ac60efbd0f263ef096d311145d0151931ce2a2b8b0099eaec`.
This is a decisive rejection of that exact as-run composition, whose recorded
encoder digest includes the newly proved banker-private-kitty drift. It is not
a clean negative result for the checkpoint under its trained encoder contract.
The artifact and verdict stay immutable; a corrected version must use disjoint
seeds and may not pool results.

The freed Air was immediately repointed to minimal clean corrected-encoder
commit `0183cdd105ca6074d3824fe294f39a2986b15bb8`. Read-only preflight confirmed
Python 3.14.6, compiled+strict execution, exact exam hashes and v11 checkpoint
SHA. At 14:55 the supervisor admitted packet `teacher-v1-entry-120m-v1` and
launched all eight 128-deal capture workers on exact seeds
120,000,000–120,001,023. Mini's eight S0b workers remained live under their
original singleton supervisor. No live partial effect was opened.

## 15:04–15:16 — teacher entry refused safely, encoder audit completed, two review HOLDs closed

Air's eight teacher capture workers all completed, but the supervisor refused
the population before launching any diagnostic. The apparent "actor drift" was
a validator representation bug: `actor_identity()` retained
`BallotSpec.config` as tuples in memory, while JSON artifacts necessarily
round-tripped them as lists. Source hashes, wide-ballot values, checkpoint and
the canonical semantic payload were identical. The direct dict comparison was
therefore stricter than the serialization contract. This is a successful
fail-closed stop, not teacher evidence: the v1 namespace contains eight capture
finals, zero diagnostics, zero state set, zero receipt and zero label. It will
never be resumed or overwritten. A fresh disjoint 143M-v2 packet is being
versioned with JSON-canonical actor identity, exact Python 3.14.6 admission and
explicit refusal of every experimental sampler/ballot flag.

The encoder audit classified every retained 531-feature shard by bytes, not
file timestamps. Clean public-v1 assets are `bc` (1,728,960 rows), `distill`
(1,658,910), `distill_n30` (1,059,802), `gen_v3_all` (2,325,168) and
`gen_v4_all` (1,963,493). In particular, all 503,354 banker rows in gen-v4 use
public-v1 semantics, independently proving compatibility of v11pair's training
inputs with the restored encoder. Contaminated derived assets are:

- `highn_enc`: 5,923 private-only banker rows, 14,922 invariant nonbanker rows;
- `human_v4`: 509 private-only banker rows, 1,341 invariant nonbanker rows;
- `human_v5`: 551 private-only banker rows, 1,510 invariant nonbanker rows;
- `human_v6`: 551 private-only banker rows, 1,618 invariant nonbanker rows.

All four are quarantined and rebuildable at least in large part from raw logs;
v5/v6 and high-N have complete raw sources. `ckpt_v13abs.pt` is itself
incompatible because its trainer consumed `highn_enc`; regenerating the cache
does not rehabilitate trained weights, so any v13 successor needs retraining
and fresh evaluation. This proves again that a version integer and vector width
are insufficient encoder provenance.

Two bounded code packets were reviewed and pushed:

- `cde0fec` freezes corrected-encoder V11 direct v2 on eight 256-cluster shards
  at exact fresh seeds 142,000,000–142,002,047. It binds the combined encoder
  implementation plus encode/Memory source hashes, exact ep07 bytes, common
  ballot and true null; requires every N=30 cell to reconcile exactly 30
  accepted worlds per search with zero failure/refusal/short/zero/exact-feature
  counters; keeps shard progress score-blind; and reopens raw records before
  aggregation. Root focused checks pass 49/49 and compiled+strict preflight is
  clean. No evidence has launched.
- Claude correctly noted that `868b6d8` omitted imported execution-contract
  modules from the direct-Q algorithm identity. Commit `f5ff2f9` binds those
  plus the material heuristic, MC-ballot and legal-play transitive sources.
  The exact 16-key contract and one mutation per dependency are tested; root's
  direct-Q/synchronous/resume/checkpoint matrix passes 77/77. This closes a code
  gate, not a learning or strength result.

At 15:04 Mini's authoritative S0b singleton still reported all eight workers
active, eight partial manifests, no final or FAILED marker and unchanged clean
compiled+strict identity. No partial S0 effect was used.

## 15:25–15:32 — corrected V11 uses Air; adversarial teacher review prevents a false PASS

The clean Air V11 worktree was moved from preserved historical `e66b90b` to
exact detached `cde0fecf4151685e7174be8a7aa64b0ee6478edd`. Score-free
compiled+strict preflight under Python 3.14.6 returned no protocol problem and
reproduced the exact public/no-private-kitty encoder schema, combined digest and
encode/Memory source hashes. At 15:25 all eight fresh 142M shards launched in
detached sessions. Immediate liveness showed eight workers, eight record
partials, eight manifest partials, zero finals/failures and no namespace
collision. No partial outcome was opened.

The first teacher 143M-v2 repair passed 56 focused tests and root's broader
teacher/pilot/S3a matrix passed 211/211, but independent adversarial review
still found three real blockers:

1. The frozen-state validator checked diagnostic file metadata without proving
   the 64 selected states were exact embedded diagnostic states—or even that
   their seeds belonged to 143M. Its positive fixture had empty diagnostic rows
   and invented 64 unrelated states, so that test passed vacuously. Diagnostic
   validation also failed to byte-bind embedded states back to capture records.
2. State capture/diagnostic/freeze output used an existence precheck followed by
   `os.replace`; a concurrently created final could be overwritten and a
   dangling symlink could bypass `exists`. Evidence publication must be hard-
   link-exclusive and collision-falsified.
3. `TEACHER_V1_SPEC.md` still instructed the refused 120M-v1 packet.

Teacher v2 therefore remains HOLD before commit or launch. The repair must
recompute the full capture -> diagnostic -> selection chain, enforce 64 unique
in-range exact states and actual coverage maps, replace overwrite-capable
publication, add invented/swapped/altered/collision falsifications and update
the operational spec. The passing test count did not override those missing
semantics.

## 15:36–15:47 — two review findings closed while both strength blocks stayed live

The S0 runtime-provenance defect was repaired on main without touching the
frozen Mini worktree. Commit `4dc5302` makes future smoke and full runs refuse
all four posterior-changing sampler/ballot environment keys even when set to an
empty value, records an explicit empty flag list in every manifest and requires
the exact empty identity within and across phases. Missing, nonempty and drifted
identities are falsified; the focused S0 protocol/closeout/search matrix passes
39/39. The live S0a/S0b chain predates the field and remains admissible because
its environment was independently measured flags-off. If its historical
supervisor launches S0c, that external environment check must be repeated and
carried into closeout.

Commit `d5d71d2` separately repairs DMC2's promotion identity. Completed gate
resolution cannot see the mutable learner or republish it: PASS re-verifies and
returns the exact candidate reference that was evaluated, FAIL keeps the exact
incumbent and any generator drift refuses. The test introduces distinct newer
learner bytes and forbids publication, directly catching the old failure mode.
The focused DMC2/self-play matrix passes 32/32. This is enabling correctness;
the historical DMC2 runs remain invalid and it is not a strength result.

At 15:43 Mini's eight S0b workers remained live at roughly 82–85% CPU with
eight partial manifests and no final/failure artifact. At 15:45 Air's eight
corrected-V11 workers and detached sessions likewise remained live with eight
record partials, eight manifest partials and no final/failure artifact. No
partial outcome was inspected. Parallel code work moved to the actual next
strength blockers: close teacher-v2's evidence chain, add explicit exact-
candidate actor refresh so direct-Q can learn from updated policies, and
version the already-blind protected-anchor protocol for the corrected V11
parent without looking at its live result.

## 15:47–16:04 — three waiting-time strength gates closed and independently reviewed

Commit `2038b31` closes the fresh teacher 143M-v2 entry chain through its exact
64-state freeze. It canonicalizes actor identity through JSON; pins Python
3.14.6 and refuses posterior-changing keys by presence, including empty values;
admits exactly eight capture shards and 1,024 deals; byte-binds capture rows to
diagnostics and selected states; recomputes coverage; enforces unique in-range
selection and Stage-A/B disjointness; publishes artifacts exclusively; and
reopens the full semantic parent chain after freeze. Invented, altered,
reordered, out-of-range, collision, dangling-partial and nonzero-uncertainty
falsifications are present. The broader teacher matrix passed 204/204. This is
an entry-code PASS, not a teacher-quality result: Air is occupied, no 143M-v2
namespace exists, and receipt/label/gate publication remains a later separate
gate.

The corrected-parent version of the blind V11 protected-anchor experiment was
frozen at `b361836`. It binds exact corrected commit `cde0fec`, encoder and
checkpoint identity, the accepted direct dose/null, terminal S0 champion and
the original disjoint 137M/138M estimand. Claude found that an empty-valued
sampler environment key escaped the first check; commit `1354cac` changed the
new consumer to presence-based refusal and added empty-value falsification.
The combined v1/v2/direct matrix passes 56/56. Its explicit
`DIRECT_AGGREGATE_SHA256=None` lock stays engaged until the live corrected-V11
aggregate seals; terminal S0 is independently required.

Commit `b27be23` adds Direct-Q's explicit actor refresh. A caller cannot supply
an artifact or learner snapshot: only the runner's already-published current
candidate can become the actor for the next batch. Exact fixed-actor and
adopted-actor resume both match, while candidate bytes, live learner drift and
shared adopted actor/candidate corruption poison the runner until checkpoint
restore. In-flight rotation remains an ordinary refusal. Root's broad
synchronization/direct-Q/resume matrix passed 84/84; independent re-review
passed 57/57 after reproducing and closing the last shared-artifact poison gap.
This repairs a material learning-loop omission—future batches can actually use
new policy bytes—but it is not evidence that the learner improves.

At 16:02 Mini still had eight authoritative S0b workers at roughly 86–96% CPU,
eight partial manifests, no final/failure artifact, and live singleton
supervisor/keepawake services. Air still had all eight corrected-V11 worker
groups, eight record partials, eight manifest partials and no final/failure
artifact. No blinded partial effect was opened. Code work moved to the bounded
Direct-Q learning/held-out evidence protocol and the post-freeze teacher
receipt/label publication gate while both hosts remained saturated.
