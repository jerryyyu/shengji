# Daily log — 2026-08-07

## S0 compute finished; the evidence pipeline did not

All eight 1,024-cluster S0c shards and the frozen aggregate completed on Mini.
The numerical outcome remains intentionally unread. The historical null reused
two evaluator opponent RNG streams at lag 17, so the ordinary iid interval was
not terminal authority. Before any corrected outcome parsing, the one-shot
audit successfully sealed exactly the packet, aggregate, eight manifests and
eight raw JSONLs:

- input seal SHA-256
  `b6a48e9dbabad008a15e3ace0b19fecff9304849435b5d9c4f69da30ddc29d10`;
- seal-attempt SHA-256
  `3da45785a7b7032785573bae4f1ba2e3b740f726d29e5e6efb46021511e3c1f8`;
- exact 18-input-set SHA-256
  `14a74a76b14bc6fd731f3de5cf332ee50060c18f7baba1ad77614766e35b1361`;
- evaluation-attempt SHA-256
  `97d3b22f656f9b43a8b34acf4085896706bb40b48c88f3b095cb08592725f9c5`.

The corrected evaluator then refused before decoding an outcome. A keepalive
supervisor restart had regenerated against moving repository provenance and
overwrote the terminal state with `BLOCKED`; the independently regenerated
packet no longer byte-matched the durable one. Because the evaluation attempt
was already durable and explicitly nonretryable, the old population could no
longer authorize either PROMOTE or a corrected negative result. This is an
evidence-chain failure, not evidence that adaptive or report-LCB lost.

## Report-LCB shipped as a separate product decision

Jerry chose the simpler replicated policy for production while keeping the
formal caveat. `mc-s0-report-lcb` adds a disjoint R=300 paired report fold to
ordinary N=30 selection and overrides candidate 0 only when the one-sided
Student-t lower bound is positive. Its two development results were:

- S0a: `+0.353 +/- 0.069` versus `mc-strong`;
- S0b: `+0.357 +/- 0.066` versus `mc-strong`.

The deployment preflight passed 84 search/protocol/ballot/websocket tests with
the compiled engine active. On the named difficult lead state, local median
decision time was 0.390s versus 0.127s for `mc-strong`; this was acceptable for
the strength-first product goal. Commit `74be565` documented the manual
decision and rollback, and Fly version 16 passed machine checks. Repeated live
health checks reported:

```json
{"ok":true,"rooms":0,"bot":"mc-s0-report-lcb","fast":true}
```

This deployment is not a retroactive formal S0 promotion. `mc-strong` remains
the immediate rollback, and a fresh collision-free report-LCB/current/null
confirmation remains the clean way to put the product choice on formal footing.

## Formal S0 closed outcome-blind

Commit `17f4085` introduced a structurally non-promoting closeout. It reopens
only the score-blind seal/attempt receipts, hashes the canonical input bytes
twice without decoding them, binds the exact supervisor refusal, writes its own
nonretryable attempt first, and can emit only `S0_COMPLETE_SELECT_NONE` with
`outcomes_parsed=false`, `promotion_admissible=false` and no deployment or
retry authority. The focused adversarial matrix passed 41 tests, including
mutated inputs and attempt-consumption on failure.

The one-shot closeout published successfully:

- closeout-attempt SHA-256
  `6104cbab343e29fcf1cc20ec0280b14374c88cb9a78bef169cf37e458a044ca5`;
- closeout SHA-256
  `ef0a3659859b38d0b9362376e5e403fecb625f59c475600ed09906ce695fde9a`.

Commit `ca556c2` made the already-frozen S0e-v2 parent lock terminal with
`authorized=false`. The independent verifier reproduced the exact lock and 51
focused S0 closeout/dependency/parent tests passed. The lock's historical
formal phrase “production remains mc-strong” describes what S0 was allowed to
authorize; it does not undo the separately approved production deployment.

After terminal verification, Codex required the loaded launch-service
namespace to equal exactly the eight inert S0c labels and required every PID to
be absent. It then booted out those eight labels and verified the complete
`com.shengji.s0mini.*` namespace empty. No evidence file, failed namespace or
attempt receipt was deleted. Mini is free.

## Other overnight results and next strength work

- Corrected V11 direct-v2 spent its game compute, but its consumer falsely
  rejected uncapped house-rule utility outside `+/-1..3`. Preserve the raw and
  failed artifacts and repair validation without replaying games. Do not infer
  network activation because the historical runner did not record it.
- Teacher 143M-v2 completed capture and refused before diagnostics when the v11
  actor returned an action outside its canonical ballot. Preserve that packet.
  Version the actor/gate fix, prove zero off-ballot actions, and rerun only
  through a fresh 64-state freeze.
- Mini's next admitted compute is the six Direct-Q 32-iteration score-redacted
  preflights. They authorize a full bounded learning screen only if measured
  wall/storage and semantic receipts pass.
- Protected V11, structured bury and sampled-exact endgame code gates predate
  the manual production change. Before running them, explicitly name whether
  the reference is formal `mc-strong` or live report-LCB. For the product goal,
  report-LCB is the relevant bar; changing that parent must be versioned rather
  than implied.

## Synthesized lesson

S0 bought a real product mechanism and a painful measurement lesson. A fresh
disjoint report fold replicated a large gain twice; adaptive allocation did not
show a separable increment. The terminal confirmation did not answer the game
question because provenance and dependency repair failed before admissible
outcome parsing. The right response is neither “everything failed” nor “pretend
S0c passed”: ship the simpler replicated candidate with an explicit rollback,
close the burned evidence conservatively, and run one new collision-free
confirmation whose authority cannot be changed by a moving supervisor.

## T1 learned-lane gates closed

The rest of the day converted three ambiguous learned-policy branches into
bounded dispositions rather than launching more compute on hope.

### V11-v2 repaired without replay

The corrected-encoder direct-v2 games were complete, but shard 5 had published
a `.FAILED` consumer artifact because the validator still assumed the obsolete
`+/-1..3` level range. The repair reopened all seven normal shards plus the
failed shard's immutable raw bytes, checked exact accepted N=30 work and source
identity, and aggregated the existing 2,048 clusters without replaying a game.
The sealed aggregate SHA-256 is
`b7c90ba4c1a9bb421a4cfcc788dbf1eb44365868f65ee0eb58257b38205d21ad`:

- V11 minus current: `-0.141113 +/- 0.069823`;
- V11 minus matched null: `-0.109863 +/- 0.070111`;
- null minus current: `-0.031250 +/- 0.067878`.

The null was sane and the direct superiority gates failed. The frozen protected
composition rule therefore records `protected_composition_authorized=false`.
This closes direct deployment and the current protected-anchor lane. It does
not erase the earlier 57.7% SmartBot win: V11 remains a useful exact-ballot
proposal/ranking and disagreement signal whose expensive tail must be judged by
fresh teacher gold. Its relative action deltas are not a scalar leaf value.

### Teacher-v3 entry succeeded through its exact stop

Teacher-v2 had exposed a real canonical-action defect: the V11 actor could
return a legal physical action outside the canonical MC ballot. The repair
proved the named `143000001:44:0` witness and 1,452 additional lead/follow
decisions, preserving physical action semantics while mapping the actor choice
to the canonical ballot.

Fresh `teacher-v1-entry-149m-v3` then ran from clean commit `be25b4d` on Air:

- eight interleaved 128-deal captures, 1,024 total;
- eight exact diagnostics;
- 64 unique frozen states: 48 representative, 8 boundary and 8 uncertainty;
- state-set SHA-256
  `e016373e8ecb9b6c7b6f3c14f8f4b14d9845f76478137f7a2c07249628cb4648`;
- terminal progress SHA-256
  `b7e3995e64012819e1df0b5024d13e30d5c5c1943ed801cba0898c975fc4511e`.

The supervisor stopped before receipts or labels exactly as specified. These
64 states are a mechanics parent, not a cheap-versus-gold dataset. Next are two
separately receipted Stage-A executions over the same canonical eight 8-state
shards, each with 256 selection and 256 report worlds. Exact deterministic
equality earns the separate 128-state Stage-B continuation-quality gate.

### Direct-Q produced a useful split result

All six score-redacted preflights admitted the bounded three-seed screen, and
the full 512-iteration treatment/no-step experiment completed on Mini. Aggregate
SHA-256 is
`1fa6789eded784e03778f5ede841e45039579625477dbaa249d63c5ccc8ce791`.
The learned actor changed decisions and improved paired gameplay by
`+0.162760 +/- 0.058755`, with all three gameplay seeds positive. It still
failed the learning gate: seed 1 regressed on the frozen Q probe and the pooled
attacker and defender MSE-reduction lower bounds were negative.

The correct verdict is `SELECT NONE`: no extension, promotion or claim that
Direct-Q learned the intended value function. The positive gameplay result is
worth diagnosing rather than discarding. The next bounded work is seed-1 and
role/target calibration plus a larger disjoint held-out probe and a Suphx-style
supervised microbaseline; Direct-Q v2 must earn target learning before more
self-play compute.

## Fresh report-LCB confirmation frozen and launched

RLCB-C1 was designed as new evidence rather than an S0c repair. Commit
`ced1033` freezes eight 256-cluster shards over seeds
`150000000..150002047`, comparing exactly live report-LCB, `mc-strong`, and a
collision-free `mc-strong-null-rlcb-c1`. Receipt SHA-256 is
`02c286ed6e431ec807c4fe4040244e11c790c4a5b0ac5dd8f2ba186d275d39d0`;
selection digest is
`e0f758bba627860225debc9a43493f5c7e51ded90256c15961435c6376a33d31`.

The only superiority estimand is the paired one-sided 95% lower bound for
report-LCB minus current. The null interval containing zero is a calibration
check, not a second superiority hurdle. All eight Mini workers launched from
clean exact `ced1033`; monitoring remains heartbeat/progress only until every
terminal shard is present.

## Production latency was reproduced and isolated

The live CAXI log established that the complaint was real: 109 report-LCB
searches had p50 1.143s, p95 16.413s and max 20.499s on Fly's shared CPU, before
the old fixed 0.7s post-decision delay. A sanitized 100-decision replay asset
preserves exact states, ballots, work, semantic receipts and full pre-decision
RNG state without room/player/token/timestamp identity. Its payload SHA-256 is
`91354e3391a1ccbd65d758d011416f135ba8b212aacec40195c7a03fc3e70a40`.

On an uncontended Air run, 100/100 decisions reproduced and every latency gate
passed: search p50 0.172s, p95 0.357s and max 0.422s. The pushed scheduler lane
now runs CPU-bound bot decisions off the event loop, keeps the join/claim grace
window, treats the configured delay as a minimum total turn time rather than an
additive tax, and defers cancellation until the worker finishes so a room lock
cannot be released during mutation. Cancellation, pacing, multi-room and a
genuine CPU-busy-loop event-loop fairness test pass repeatedly.

The local worst-state profile attributes most cost to complete rollouts and
heuristic continuation, with smaller round-play and sampler components. That
makes shared-host CPU contention the immediate production problem and compiled
whole-rollout work a later optimization surface. Same-image Fly evidence,
production-safe deployment during an empty-room window and post-deploy timing
remain open; N=30, R=300, ballot and LCB semantics are not performance knobs.

## End-of-day synthesis

T1 did not produce a deployable learned policy, but it substantially reduced
uncertainty. V11 direct use is negative while proposal use remains plausible;
Direct-Q's actor can improve play without learning its registered held-out
target; Teacher-v3 now has a clean, fresh and reproducible entry population;
and report-LCB has both a practical latency repair and a fresh formal
confirmation in flight. The next strength-producing sequence is therefore
Teacher Stage A -> versioned Stage-B gold -> V11 proposal precision -> a
champion-preserving search integration, with Direct-Q target repair proceeding
as a separate bounded lane.
