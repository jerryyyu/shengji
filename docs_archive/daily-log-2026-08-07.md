# Daily log — 2026-08-07

## S0 compute finished; the evidence pipeline did not

All eight 1,024-cluster S0c shards and the frozen aggregate completed on Mini.
The numerical outcome remains intentionally unread. The historical null reused
two evaluator opponent RNG streams at lag 17, so the ordinary iid interval was
not terminal authority. Before any corrected outcome parsing, the one-shot
audit successfully sealed exactly the packet, aggregate, eight manifests and
eight raw JSONLs:

- input seal SHA-256
  `b6a48e9dbabad008a15e3ace0b19fecff9304849435b5d9c4f69da30ddc29d10`;
- seal-attempt SHA-256
  `3da45785a7b7032785573bae4f1ba2e3b740f726d29e5e6efb46021511e3c1f8`;
- exact 18-input-set SHA-256
  `14a74a76b14bc6fd731f3de5cf332ee50060c18f7baba1ad77614766e35b1361`;
- evaluation-attempt SHA-256
  `97d3b22f656f9b43a8b34acf4085896706bb40b48c88f3b095cb08592725f9c5`.

The corrected evaluator then refused before decoding an outcome. A keepalive
supervisor restart had regenerated against moving repository provenance and
overwrote the terminal state with `BLOCKED`; the independently regenerated
packet no longer byte-matched the durable one. Because the evaluation attempt
was already durable and explicitly nonretryable, the old population could no
longer authorize either PROMOTE or a corrected negative result. This is an
evidence-chain failure, not evidence that adaptive or report-LCB lost.

## Report-LCB shipped as a separate product decision

Jerry chose the simpler replicated policy for production while keeping the
formal caveat. `mc-s0-report-lcb` adds a disjoint R=300 paired report fold to
ordinary N=30 selection and overrides candidate 0 only when the one-sided
Student-t lower bound is positive. Its two development results were:

- S0a: `+0.353 +/- 0.069` versus `mc-strong`;
- S0b: `+0.357 +/- 0.066` versus `mc-strong`.

The deployment preflight passed 84 search/protocol/ballot/websocket tests with
the compiled engine active. On the named difficult lead state, local median
decision time was 0.390s versus 0.127s for `mc-strong`; this was acceptable for
the strength-first product goal. Commit `74be565` documented the manual
decision and rollback, and Fly version 16 passed machine checks. Repeated live
health checks reported:

```json
{"ok":true,"rooms":0,"bot":"mc-s0-report-lcb","fast":true}
```

This deployment is not a retroactive formal S0 promotion. `mc-strong` remains
the immediate rollback, and a fresh collision-free report-LCB/current/null
confirmation remains the clean way to put the product choice on formal footing.

## Formal S0 closed outcome-blind

Commit `17f4085` introduced a structurally non-promoting closeout. It reopens
only the score-blind seal/attempt receipts, hashes the canonical input bytes
twice without decoding them, binds the exact supervisor refusal, writes its own
nonretryable attempt first, and can emit only `S0_COMPLETE_SELECT_NONE` with
`outcomes_parsed=false`, `promotion_admissible=false` and no deployment or
retry authority. The focused adversarial matrix passed 41 tests, including
mutated inputs and attempt-consumption on failure.

The one-shot closeout published successfully:

- closeout-attempt SHA-256
  `6104cbab343e29fcf1cc20ec0280b14374c88cb9a78bef169cf37e458a044ca5`;
- closeout SHA-256
  `ef0a3659859b38d0b9362376e5e403fecb625f59c475600ed09906ce695fde9a`.

Commit `ca556c2` made the already-frozen S0e-v2 parent lock terminal with
`authorized=false`. The independent verifier reproduced the exact lock and 51
focused S0 closeout/dependency/parent tests passed. The lock's historical
formal phrase “production remains mc-strong” describes what S0 was allowed to
authorize; it does not undo the separately approved production deployment.

After terminal verification, Codex required the loaded launch-service
namespace to equal exactly the eight inert S0c labels and required every PID to
be absent. It then booted out those eight labels and verified the complete
`com.shengji.s0mini.*` namespace empty. No evidence file, failed namespace or
attempt receipt was deleted. Mini is free.

## Other overnight results and next strength work

- Corrected V11 direct-v2 spent its game compute, but its consumer falsely
  rejected uncapped house-rule utility outside `+/-1..3`. Preserve the raw and
  failed artifacts and repair validation without replaying games. Do not infer
  network activation because the historical runner did not record it.
- Teacher 143M-v2 completed capture and refused before diagnostics when the v11
  actor returned an action outside its canonical ballot. Preserve that packet.
  Version the actor/gate fix, prove zero off-ballot actions, and rerun only
  through a fresh 64-state freeze.
- Mini's next admitted compute is the six Direct-Q 32-iteration score-redacted
  preflights. They authorize a full bounded learning screen only if measured
  wall/storage and semantic receipts pass.
- Protected V11, structured bury and sampled-exact endgame code gates predate
  the manual production change. Before running them, explicitly name whether
  the reference is formal `mc-strong` or live report-LCB. For the product goal,
  report-LCB is the relevant bar; changing that parent must be versioned rather
  than implied.

## Fresh RLCB-C1 formally confirmed report-LCB

The clean follow-up did what the burned S0c pipeline could not: it answered one
narrow game question on new bytes. RLCB-C1 froze exactly three arms—live
`mc-s0-report-lcb`, `mc-strong`, and a current-policy null shifted by
60,000,011—over seeds 150,000,000–150,002,047. All eight 256-cluster shards
completed: 2,048 paired clusters and 4,096 mirrored records per arm. The stream
proof found zero null collisions against all 8,192 prohibited streams; accepted
doses were exact and every rejected/failed/short/zero-world counter was zero.

- report-LCB minus current: `+0.33837890625 +/- 0.06770579952`;
- current-null minus current: `-0.01904296875 +/- 0.06827022560`;
- all registered criteria: true;
- terminal decision: `CONFIRM_REPORT_LCB`;
- aggregate SHA-256:
  `83f5a9df2f1db1fa45d50fb005b941b776d9ecc2c9f8703d3d62efff8f5ef5ea`.

The supervisor itself refused after the shards because Claude's concurrent
`HANDOFF_REVIEW.md` addition made main dirty. No outcome had been parsed and no
aggregate existed at that point. The protocol explicitly permits standalone
artifact-only aggregate/verify. Codex temporarily stashed only that handoff
file, proved its SHA unchanged before/after, aggregated and independently
verified at exact pushed main `ced1033`, then restored the handoff byte for
byte. This was not a replay, retry, extension, or outcome-conditioned change.

RLCB-C1 closes the fresh one-round superiority claim. It does not revive S0c,
confirm adaptive allocation, prove multi-round progression, or retroactively
authorize the already-manual deployment. The important synthesized result is
now stronger than “two encouraging development blocks”: report-LCB has three
independent positive blocks, the final one with a clean null and formal
predeclared authority.

## V11 and Direct-Q reached honest stop decisions

The pushed `d1d2019` V11 artifact repair reopened the immutable 142M records,
including shard 5's retained `.FAILED` bytes, while changing only the invalid
bounded-utility consumer in memory. No game or source byte changed. V11 minus
current was `-0.141113 +/- 0.069823`, V11 minus its matched null was
`-0.109863 +/- 0.070111`, and null minus current was
`-0.031250 +/- 0.067878`. Exact dose and null checks passed, efficacy failed,
and `protected_composition_authorized=false`. Aggregate SHA-256:
`b7c90ba4c1a9bb421a4cfcc788dbf1eb44365868f65ee0eb58257b38205d21ad`.

Direct-Q's complete 144M screen produced a tempting gameplay result,
`+0.162760 +/- 0.058755`, but failed its conjunctive learning contract: seed 1
and both pooled attacker/defender held-out MSE LCBs failed. Aggregate SHA-256
`1fa6789eded784e03778f5ede841e45039579625477dbaa249d63c5ccc8ce791`
therefore records SELECT NONE and authorizes nothing. This is exactly why the
learning diagnostics were frozen separately from the gameplay report.

## Teacher-v3 and production latency advanced in parallel

Teacher-v3 repaired canonical actor actions, completed fresh capture,
diagnostics and an exact 64-state freeze, then versioned Stage-B observability.
Air launched eight compiled+strict Stage-B N=30 gold workers at exact
`1a2a713`; all remained live and progress-reporting at the close of this entry.
Separately, commit `c40a31c` froze the independent 64-state champion audit with
disjoint 32-world selection/report folds and downstream report-LCB continuation
at every information set. Stage-B outcomes cannot alter that audit.

Production profiling showed why report-LCB sometimes felt slow. In live room
HIEJ, 40 searched bot turns had search p50 1.133s, p95 1.858s and max 2.174s;
the old scheduler's visible inter-play values were p50 1.835s, p95 2.561s and
max 2.876s. Three sequential bots therefore feel like roughly 5.5 seconds.
The new speculative scheduler searches an isolated round/bot clone while the
room remains responsive, overlaps the 0.7-second pacing window, and discards
the action plus cloned RNG/counters if a seat is claimed or reconnected. A real
WebSocket witness now claims an on-turn bot while its worker is blocked and
receives private state before that worker is released.

## Synthesized lesson

S0 bought a real product mechanism and a painful measurement lesson. A fresh
disjoint report fold replicated a large gain twice; adaptive allocation did not
show a separable increment. S0c did not answer the terminal question because
its evidence chain burned before admissible parsing, so it remains closed. The
separate clean response worked: ship the simpler candidate with an explicit
rollback, preserve the S0 boundary, then confirm the policy on fresh streams
with a sane null. RLCB-C1 has now supplied that confirmation. Report-LCB is the
first policy in this program for which stronger play, sampler correctness,
exact work, null calibration and formal fresh evidence all line up.

## Latency package reached empty-room ship hold

The final speculative scheduler is clean and pushed at `ff784a8`. It snapshots
the live round and bot under the room lock, searches only those copies in a
worker, overlaps search with the existing 0.7-second pacing window, and commits
only if the game, round, phase, turn and seat controller are unchanged. A seat
claim or reconnect therefore wins immediately and discards the speculative
action plus cloned RNG/counters. X-ray uses the same isolation principle and no
longer holds the live room lock during search. The final audit also repaired a
failed-throw logging edge: the play event now retains both the policy's attempted
throw and the component the engine forced.

The focused matrix passed 61/61. A freshly compiled exact replay reproduced all
100 frozen live report-LCB decisions, with search p50 0.164s, p95 0.339s and max
0.379s; all latency gates passed and every uncontended projected turn remained
at the 0.7-second pacing floor. The broad temporary-worktree suite recorded 915
passes, 3 skips and 6 expected asset/provenance refusals, with no behavior-test
failure. Deployment did not proceed because production still had two connected
humans at 16:34: srig in HIEJ and Sarah Kim in ZTYH. This is an empty-room hold,
not a code-gate hold.

Release head `b315e91` additionally excludes the host native build cache from
the image context. A remote build-only run compiled the Linux extension from
source and pushed `registry.fly.io/shengji:latency-b315e91` with manifest
SHA-256
`dbc978028de1b9bd84dad00bb59e83f9ba6feac46e5d1ecc95ef3c2150c2426a`;
it did not restart or otherwise change the live version-16 machine.

## Teacher champion audit reached a real Air preflight pass

The independent 64-state audit was tightened before Stage B exposed any final
outcome. Commits `2a0541d` and `9b2c8ab` bind the downstream actor class,
heuristic rollout class, complete MC ballot, material AI/engine source hashes,
exact compiled engine and a bounded source transition rooted at the frozen
audit. The frozen state remains byte-identical at SHA-256
`d04d1c0fa507bab680da4d53eeb72325a97c8ca058aac0d01c16dfdcf44f7a34`.

This extra preflight bought a concrete correctness result. The first literal
ballot value had been transcribed incorrectly even though the unit test passed,
because the test mocked the live value to equal the same literal. Running the
real lock on Air exposed the mismatch (`11be...` versus the actual
`3710...`) before any audit label existed. The literal was corrected to live
ballot `mc_candidates@v1[c008dd47b0b7]`, and the regression now derives the
real generator ballot before exercising mutation falsifiability.

An untouched, separate Air audit worktree at exact pushed `f4f3dc0` now passes
19/19 focused tests. Its outcome-blind checks report no transition problems,
no execution-lock problems, exact live-lock equality, no frozen-state
self-validation problems and no frozen-state execution-lock problems. The
eight Stage-B workers continued in their original detached `1a2a713` worktree;
after roughly 2h10 all were healthy near one CPU and no final gold shard had
been published or opened. Production simultaneously remained on the safe
empty-room hold with one connected player, srig in HIEJ.

## Late provenance review repaired two terminal-identity gaps

Claude's 16:55 review correctly distinguished valid C1 numbers from a complete
supervisor chain. The original supervisor recorded all eight shard completions
and then terminally refused because unrelated `HANDOFF_REVIEW.md` was dirty;
the later standalone aggregate did not erase that event. Commit `57f4e1b` adds
a strictly artifact-only closeout. It pins original `ced1033`, exact refusal
transcript SHA `d3bb6aa9...1df8a`, all frozen shard/runtime/source bytes and
unchanged aggregate SHA `83f5a9df...f5ef5ea`. It cannot invoke the game runner
or supervisor, records zero games and zero retries, and grants no production
authority. Closeout plus independent verification passed at SHA
`06dd487de5389bb6ddbad38af39a1150fc98f52e6e6418c482e97ac8f3b7aae5`,
state `FORMAL_CONFIRMATION_CONFIRMED_ARTIFACT_ONLY`.

The same review showed that a bounded allowed-path transition was not an exact
terminal identity: an arbitrary future audit-only HEAD could satisfy it.
Teacher commit `f4f3dc0` therefore makes receipt creation require the exact
externally predeclared git and audit-script SHA. Air passes 19/19 tests and
exact preflight at git `f4f3dc0d...5c5349`, script SHA
`32a31bf7...c7bd9`; a fake future HEAD fails. This was completed before any
Stage-B final or champion-audit label existed.

## Final latency harness holds closed without changing runtime bytes

Claude's last release review identified six WebSockets manually entered by
tests and an evaluator cutoff test that mocked away `play_game`, the boundary
it claimed to protect. Commit `578b2c6` converts every such socket to
failure-safe `with`/`ExitStack` ownership; an assertion can no longer strand a
socket and hang teardown. It also drives the real
`evaluate -> play_game -> FullGameCutoff` path while shortening only the round
transition. Two earlier low-severity guards were made falsifiable at the same
time: disconnect must remove an already-ready seat from the quorum, and a seat
claim must clear the previous owner's latched bot announcement.

WebSocket+game passed 49/49. The expanded scheduler, X-ray, replay, invariant,
WebSocket and game matrix produced 92 passes, two optional skips and one
expected missing historical corpus asset, with no behavioral failure. A remote
build-only push created `registry.fly.io/shengji:latency-578b2c6`; its manifest
is exactly the prior runtime manifest `dbc97802...c2426a`, confirming the
ship-gate repair changed tests only. Production remained on version 16 because
srig was still connected in HIEJ at 17:15.

The release was then integrated onto authoritative `main` as an exact
cherry-pick series through `cd6789e`; a clean integration worktree reproduced
the same focused result. Main-derived image `latency-cd6789e` was built and
pushed at manifest `047bcfe4...5b300`. A runtime-tree diff against release
`578b2c6` is empty, so this becomes the preferred deploy tag while preserving
the reviewed branch as provenance.

That integration also exercised the C1 closeout verifier after unrelated
descendant commits and exposed a stability defect: it recomputed the creator
as current HEAD. Commit `76afab2` now reopens the exact historical creator git
and script digest recorded in the receipt. The original closeout bytes and SHA
remain unchanged and independently verify; the focused C1 suite is 19/19.
