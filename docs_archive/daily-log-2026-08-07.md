# Daily log — 2026-08-07

## S0 compute finished; the evidence pipeline did not

All eight 1,024-cluster S0c shards and the frozen aggregate completed on Mini.
The numerical outcome remains intentionally unread. The historical null reused
two evaluator opponent RNG streams at lag 17, so the ordinary iid interval was
not terminal authority. Before any corrected outcome parsing, the one-shot
audit successfully sealed exactly the packet, aggregate, eight manifests and
eight raw JSONLs:

- input seal SHA-256
  `b6a48e9dbabad008a15e3ace0b19fecff9304849435b5d9c4f69da30ddc29d10`;
- seal-attempt SHA-256
  `3da45785a7b7032785573bae4f1ba2e3b740f726d29e5e6efb46021511e3c1f8`;
- exact 18-input-set SHA-256
  `14a74a76b14bc6fd731f3de5cf332ee50060c18f7baba1ad77614766e35b1361`;
- evaluation-attempt SHA-256
  `97d3b22f656f9b43a8b34acf4085896706bb40b48c88f3b095cb08592725f9c5`.

The corrected evaluator then refused before decoding an outcome. A keepalive
supervisor restart had regenerated against moving repository provenance and
overwrote the terminal state with `BLOCKED`; the independently regenerated
packet no longer byte-matched the durable one. Because the evaluation attempt
was already durable and explicitly nonretryable, the old population could no
longer authorize either PROMOTE or a corrected negative result. This is an
evidence-chain failure, not evidence that adaptive or report-LCB lost.

## Report-LCB shipped as a separate product decision

Jerry chose the simpler replicated policy for production while keeping the
formal caveat. `mc-s0-report-lcb` adds a disjoint R=300 paired report fold to
ordinary N=30 selection and overrides candidate 0 only when the one-sided
Student-t lower bound is positive. Its two development results were:

- S0a: `+0.353 +/- 0.069` versus `mc-strong`;
- S0b: `+0.357 +/- 0.066` versus `mc-strong`.

The deployment preflight passed 84 search/protocol/ballot/websocket tests with
the compiled engine active. On the named difficult lead state, local median
decision time was 0.390s versus 0.127s for `mc-strong`; this was acceptable for
the strength-first product goal. Commit `74be565` documented the manual
decision and rollback, and Fly version 16 passed machine checks. Repeated live
health checks reported:

```json
{"ok":true,"rooms":0,"bot":"mc-s0-report-lcb","fast":true}
```

This deployment is not a retroactive formal S0 promotion. `mc-strong` remains
the immediate rollback, and a fresh collision-free report-LCB/current/null
confirmation remains the clean way to put the product choice on formal footing.

## Formal S0 closed outcome-blind

Commit `17f4085` introduced a structurally non-promoting closeout. It reopens
only the score-blind seal/attempt receipts, hashes the canonical input bytes
twice without decoding them, binds the exact supervisor refusal, writes its own
nonretryable attempt first, and can emit only `S0_COMPLETE_SELECT_NONE` with
`outcomes_parsed=false`, `promotion_admissible=false` and no deployment or
retry authority. The focused adversarial matrix passed 41 tests, including
mutated inputs and attempt-consumption on failure.

The one-shot closeout published successfully:

- closeout-attempt SHA-256
  `6104cbab343e29fcf1cc20ec0280b14374c88cb9a78bef169cf37e458a044ca5`;
- closeout SHA-256
  `ef0a3659859b38d0b9362376e5e403fecb625f59c475600ed09906ce695fde9a`.

Commit `ca556c2` made the already-frozen S0e-v2 parent lock terminal with
`authorized=false`. The independent verifier reproduced the exact lock and 51
focused S0 closeout/dependency/parent tests passed. The lock's historical
formal phrase “production remains mc-strong” describes what S0 was allowed to
authorize; it does not undo the separately approved production deployment.

After terminal verification, Codex required the loaded launch-service
namespace to equal exactly the eight inert S0c labels and required every PID to
be absent. It then booted out those eight labels and verified the complete
`com.shengji.s0mini.*` namespace empty. No evidence file, failed namespace or
attempt receipt was deleted. Mini is free.

## Other overnight results and next strength work

- Corrected V11 direct-v2 spent its game compute, but its consumer falsely
  rejected uncapped house-rule utility outside `+/-1..3`. Preserve the raw and
  failed artifacts and repair validation without replaying games. Do not infer
  network activation because the historical runner did not record it.
- Teacher 143M-v2 completed capture and refused before diagnostics when the v11
  actor returned an action outside its canonical ballot. Preserve that packet.
  Version the actor/gate fix, prove zero off-ballot actions, and rerun only
  through a fresh 64-state freeze.
- Mini's next admitted compute is the six Direct-Q 32-iteration score-redacted
  preflights. They authorize a full bounded learning screen only if measured
  wall/storage and semantic receipts pass.
- Protected V11, structured bury and sampled-exact endgame code gates predate
  the manual production change. Before running them, explicitly name whether
  the reference is formal `mc-strong` or live report-LCB. For the product goal,
  report-LCB is the relevant bar; changing that parent must be versioned rather
  than implied.

## Fresh RLCB-C1 formally confirmed report-LCB

The clean follow-up did what the burned S0c pipeline could not: it answered one
narrow game question on new bytes. RLCB-C1 froze exactly three arms—live
`mc-s0-report-lcb`, `mc-strong`, and a current-policy null shifted by
60,000,011—over seeds 150,000,000–150,002,047. All eight 256-cluster shards
completed: 2,048 paired clusters and 4,096 mirrored records per arm. The stream
proof found zero null collisions against all 8,192 prohibited streams; accepted
doses were exact and every rejected/failed/short/zero-world counter was zero.

- report-LCB minus current: `+0.33837890625 +/- 0.06770579952`;
- current-null minus current: `-0.01904296875 +/- 0.06827022560`;
- all registered criteria: true;
- terminal decision: `CONFIRM_REPORT_LCB`;
- aggregate SHA-256:
  `83f5a9df2f1db1fa45d50fb005b941b776d9ecc2c9f8703d3d62efff8f5ef5ea`.

The supervisor itself refused after the shards because Claude's concurrent
`HANDOFF_REVIEW.md` addition made main dirty. No outcome had been parsed and no
aggregate existed at that point. The protocol explicitly permits standalone
artifact-only aggregate/verify. Codex temporarily stashed only that handoff
file, proved its SHA unchanged before/after, aggregated and independently
verified at exact pushed main `ced1033`, then restored the handoff byte for
byte. This was not a replay, retry, extension, or outcome-conditioned change.

RLCB-C1 closes the fresh one-round superiority claim. It does not revive S0c,
confirm adaptive allocation, prove multi-round progression, or retroactively
authorize the already-manual deployment. The important synthesized result is
now stronger than “two encouraging development blocks”: report-LCB has three
independent positive blocks, the final one with a clean null and formal
predeclared authority.

## V11 and Direct-Q reached honest stop decisions

The pushed `d1d2019` V11 artifact repair reopened the immutable 142M records,
including shard 5's retained `.FAILED` bytes, while changing only the invalid
bounded-utility consumer in memory. No game or source byte changed. V11 minus
current was `-0.141113 +/- 0.069823`, V11 minus its matched null was
`-0.109863 +/- 0.070111`, and null minus current was
`-0.031250 +/- 0.067878`. Exact dose and null checks passed, efficacy failed,
and `protected_composition_authorized=false`. Aggregate SHA-256:
`b7c90ba4c1a9bb421a4cfcc788dbf1eb44365868f65ee0eb58257b38205d21ad`.

Direct-Q's complete 144M screen produced a tempting gameplay result,
`+0.162760 +/- 0.058755`, but failed its conjunctive learning contract: seed 1
and both pooled attacker/defender held-out MSE LCBs failed. Aggregate SHA-256
`1fa6789eded784e03778f5ede841e45039579625477dbaa249d63c5ccc8ce791`
therefore records SELECT NONE and authorizes nothing. This is exactly why the
learning diagnostics were frozen separately from the gameplay report.

## Teacher-v3 and production latency advanced in parallel

Teacher-v3 repaired canonical actor actions, completed fresh capture,
diagnostics and an exact 64-state freeze, then versioned Stage-B observability.
Air launched eight compiled+strict Stage-B N=30 gold workers at exact
`1a2a713`; all remained live and progress-reporting at the close of this entry.
Separately, commit `c40a31c` froze the independent 64-state champion audit with
disjoint 32-world selection/report folds and downstream report-LCB continuation
at every information set. Stage-B outcomes cannot alter that audit.

Production profiling showed why report-LCB sometimes felt slow. In live room
HIEJ, 40 searched bot turns had search p50 1.133s, p95 1.858s and max 2.174s;
the old scheduler's visible inter-play values were p50 1.835s, p95 2.561s and
max 2.876s. Three sequential bots therefore feel like roughly 5.5 seconds.
The new speculative scheduler searches an isolated round/bot clone while the
room remains responsive, overlaps the 0.7-second pacing window, and discards
the action plus cloned RNG/counters if a seat is claimed or reconnected. A real
WebSocket witness now claims an on-turn bot while its worker is blocked and
receives private state before that worker is released.

## Synthesized lesson

S0 bought a real product mechanism and a painful measurement lesson. A fresh
disjoint report fold replicated a large gain twice; adaptive allocation did not
show a separable increment. S0c did not answer the terminal question because
its evidence chain burned before admissible parsing, so it remains closed. The
separate clean response worked: ship the simpler candidate with an explicit
rollback, preserve the S0 boundary, then confirm the policy on fresh streams
with a sane null. RLCB-C1 has now supplied that confirmation. Report-LCB is the
first policy in this program for which stronger play, sampler correctness,
exact work, null calibration and formal fresh evidence all line up.

## Latency package reached empty-room ship hold

The final speculative scheduler is clean and pushed at `ff784a8`. It snapshots
the live round and bot under the room lock, searches only those copies in a
worker, overlaps search with the existing 0.7-second pacing window, and commits
only if the game, round, phase, turn and seat controller are unchanged. A seat
claim or reconnect therefore wins immediately and discards the speculative
action plus cloned RNG/counters. X-ray uses the same isolation principle and no
longer holds the live room lock during search. The final audit also repaired a
failed-throw logging edge: the play event now retains both the policy's attempted
throw and the component the engine forced.

The focused matrix passed 61/61. A freshly compiled exact replay reproduced all
100 frozen live report-LCB decisions, with search p50 0.164s, p95 0.339s and max
0.379s; all latency gates passed and every uncontended projected turn remained
at the 0.7-second pacing floor. The broad temporary-worktree suite recorded 915
passes, 3 skips and 6 expected asset/provenance refusals, with no behavior-test
failure. Deployment did not proceed because production still had two connected
humans at 16:34: srig in HIEJ and Sarah Kim in ZTYH. This is an empty-room hold,
not a code-gate hold.

Release head `b315e91` additionally excludes the host native build cache from
the image context. A remote build-only run compiled the Linux extension from
source and pushed `registry.fly.io/shengji:latency-b315e91` with manifest
SHA-256
`dbc978028de1b9bd84dad00bb59e83f9ba6feac46e5d1ecc95ef3c2150c2426a`;
it did not restart or otherwise change the live version-16 machine.

## Teacher champion audit reached an apparent Air preflight pass — retracted

The independent 64-state audit was tightened before Stage B exposed any final
outcome. Commits `2a0541d` and `9b2c8ab` bind the downstream actor class,
heuristic rollout class, complete MC ballot, material AI/engine source hashes,
exact compiled engine and a bounded source transition rooted at the frozen
audit. The frozen state remains byte-identical at SHA-256
`d04d1c0fa507bab680da4d53eeb72325a97c8ca058aac0d01c16dfdcf44f7a34`.

At the time, this preflight appeared to expose a mismatch (`11be...` versus
`3710...`) and the literal was changed to ballot
`mc_candidates@v1[c008dd47b0b7]`. A later fresh-process audit proved that
reading wrong: `3710...` came from a process-local ballot cache populated
before the exact native binary was installed. The fresh identity was the
original `11be...` / `mc_candidates@v1[568848979a2d]`. This entire apparent
pass is therefore superseded by the 18:00 repair below.

An untouched, separate Air audit worktree at exact pushed `f4f3dc0` passed
19/19 focused tests only in that cache-tainted setup. A fresh interpreter later
reported `continuation execution ballot drift`, so `f4f3dc0` may never create
an audit receipt. The eight Stage-B workers continued in their original
detached `1a2a713` worktree;
after roughly 2h10 all were healthy near one CPU and no final gold shard had
been published or opened. Production simultaneously remained on the safe
empty-room hold with one connected player, srig in HIEJ.

## Late provenance review repaired two terminal-identity gaps

Claude's 16:55 review correctly distinguished valid C1 numbers from a complete
supervisor chain. The original supervisor recorded all eight shard completions
and then terminally refused because unrelated `HANDOFF_REVIEW.md` was dirty;
the later standalone aggregate did not erase that event. Commit `57f4e1b` adds
a strictly artifact-only closeout. It pins original `ced1033`, exact refusal
transcript SHA `d3bb6aa9...1df8a`, all frozen shard/runtime/source bytes and
unchanged aggregate SHA `83f5a9df...f5ef5ea`. It cannot invoke the game runner
or supervisor, records zero games and zero retries, and grants no production
authority. Closeout plus independent verification passed at SHA
`06dd487de5389bb6ddbad38af39a1150fc98f52e6e6418c482e97ac8f3b7aae5`,
state `FORMAL_CONFIRMATION_CONFIRMED_ARTIFACT_ONLY`.

The same review showed that a bounded allowed-path transition was not an exact
terminal identity: an arbitrary future audit-only HEAD could satisfy it.
Teacher commit `f4f3dc0` made receipt creation require an exact externally
predeclared git and audit-script SHA, closing the future-HEAD hole. Its own
ballot identity was later found cache-tainted, however, so its apparent
preflight is retracted and the commit is not receipt-authorized. The exact-
predeclaration mechanism itself survives in `182d1df` below. Both discoveries
occurred before any Stage-B final or champion-audit label existed.

## Final latency harness holds closed without changing runtime bytes

Claude's last release review identified six WebSockets manually entered by
tests and an evaluator cutoff test that mocked away `play_game`, the boundary
it claimed to protect. Commit `578b2c6` converts every such socket to
failure-safe `with`/`ExitStack` ownership; an assertion can no longer strand a
socket and hang teardown. It also drives the real
`evaluate -> play_game -> FullGameCutoff` path while shortening only the round
transition. Two earlier low-severity guards were made falsifiable at the same
time: disconnect must remove an already-ready seat from the quorum, and a seat
claim must clear the previous owner's latched bot announcement.

WebSocket+game passed 49/49. The expanded scheduler, X-ray, replay, invariant,
WebSocket and game matrix produced 92 passes, two optional skips and one
expected missing historical corpus asset, with no behavioral failure. A remote
build-only push created `registry.fly.io/shengji:latency-578b2c6`; its manifest
is exactly the prior runtime manifest `dbc97802...c2426a`, confirming the
ship-gate repair changed tests only. Production remained on version 16 because
srig was still connected in HIEJ at 17:15.

The release was then integrated onto authoritative `main` as an exact
cherry-pick series through `cd6789e`; a clean integration worktree reproduced
the same focused result. Main-derived image `latency-cd6789e` was built and
pushed at manifest `047bcfe4...5b300`. A runtime-tree diff against release
`578b2c6` is empty, so this becomes the preferred deploy tag while preserving
the reviewed branch as provenance.

That integration also exercised the C1 closeout verifier after unrelated
descendant commits and exposed a stability defect: it recomputed the creator
as current HEAD. Commit `76afab2` now reopens the exact historical creator git
and script digest recorded in the receipt. The original closeout bytes and SHA
remain unchanged and independently verify; the focused C1 suite is 19/19.

## Production latency hardening shipped as release 17

Jerry explicitly authorized interrupting the remaining HIEJ game to deploy the
reviewed latency fix. Fly release 17 now runs exact main-derived image
`registry.fly.io/shengji:latency-cd6789e`, manifest SHA-256
`047bcfe4d4573961734a5536ad549605fd0df5e1477d7480cdf322282955b300`.
Fly status binds that digest to the running machine; health reports
`rooms=0`, `bot=mc-s0-report-lcb`, and `fast=true`. Release 16 is the immediate
scheduler/runtime rollback; `mc-strong` remains the separate policy rollback.

The post-deploy exercise crossed the real public paths rather than relying only
on unit tests. An on-turn bot seat was claimed in 20ms and resumed with its new
token in 17ms. The live semantic log records the losing speculative search as
`acted=false` and `stale_discarded=true`; all 42 timing records were offloaded
and snapshot-isolated. A real eight-candidate X-ray search took 1.53s while 25
concurrent WebSocket peeks stayed responsive at p50 12ms and max 19ms. Search
p50/p95/max were 1.136/1.857/3.104s, while full-turn p50/p95/max were
1.138/1.858/3.106s. The near-equality directly shows that long searches no
longer pay the old additional 0.7-second sleep.

## The Teacher terminal handoff was made mechanically explicit

At 17:48 all eight frozen Stage-B workers were still live, with no final gold
artifact and `3,072/16,384` (18.75%) outcome-blind fold worlds reported. That
counter establishes liveness only; state ballot sizes and continuation costs
make it unsuitable as a linear ETA.

The executable handoff now records the exact one-shot Stage-B gate command and
a stricter publication boundary. Eight final filenames alone are insufficient:
all label workers must have exited, no partial may remain, the producer must
still be clean at exact `1a2a713`, the state SHA must match, and the terminal
gate path must not exist. This matters because a worker creates the final hard
link before its last provenance recheck. The gate's exit 4 is a legitimate
terminal FAIL/INCONCLUSIVE, not permission to rerun. Only a sealed PASS may
cross into the independently pinned audit receipt and eight 32/32
champion-continuation shards.

## Main's Teacher spec was reconciled with the frozen experiment

A source-level audit found that authoritative main still carried the refused
143M-v2 text in `TEACHER_V1_SPEC.md`; the current 149M-v3 status and report-LCB
audit contract existed only on the pinned Teacher branch. Commit `d25e74a`
restores that already-predeclared contract to main and updates its operational
status to Stage-B N=30 attribution running. It records the 149M seed range,
Stage-A and Stage-B state hashes, the closed first attribution attempt, the
fresh v2 receipts, the separate 64-state 32/32 report-LCB audit and its exact
git/script/state locks. This changed documentation only: neither frozen
worktree, receipt, state, dose, threshold nor running outcome was touched.

## Fresh-process audit repaired a stale ballot-cache lock

At 18:00, a brand-new Python process reopened the supposedly clean `f4f3dc0`
audit and returned `continuation execution ballot drift`. Exact source and
compiled-engine hashes still matched; only the ballot differed. The cause was
the ordinary class/config ballot cache: the earlier preflight had populated it
before installing the exact native binary, then called the purportedly live
lock without clearing it. That froze cached source digest `3710...` and ballot
`c008dd47b0b7`; a receipt process starts cold and correctly derived `11be...`
and `568848979a2d`, so it would always refuse.

Pushed repair `182d1df` restores the cold-process literal and makes
`live_continuation_execution_lock()` clear the cache before deriving identity.
Its regression deliberately poisons the cache first, then proves the audit
boundary ignores it. On Air, exact native bytes pass all 19 focused tests and
a separate fresh interpreter reports empty transition, exact-predeclaration,
execution-lock, frozen-state-self and frozen-state-lock problem lists. The new
receipt identity is exact git `182d1df21697cedd722edfd3215ea1e2a7dd8753`,
script SHA-256
`57796fda247a4152a58bb98508d24ae1063f7e2c843ccf436b8b111f7c887ead`.
The state, dose, threshold, policy and output names did not change; no audit
label or Stage-B final existed. Superseded `f4f3dc0` is permanently barred from
receipt creation.

## Teacher remained live; child-publication readiness was tightened

At 18:08, all eight exact `1a2a713` Stage-B gold workers remained live at
86--89% CPU. Outcome-blind fold counters reached `3,292/16,384` (20.1%) with
no final gold shard. This is liveness evidence, not a linear ETA.

The conditional audit has the same post-link boundary as the producer labels:
receipt, label and gate code expose the final filename before completing the
last provenance recheck. The executable handoff now requires creator exit
`0`, a regular final, no surviving `.partial`, exact SHA-256 and a successful
reopen before any child stage. A filename alone never supplies launch
authority.

## The next Lane-C learner received a no-run feature gate

`SUPHX_MICRO_SPEC.md` now defines an honest test of Suphx-style oracle guiding:
train one policy with elementwise perfect-feature dropout from keep probability
one to zero, then compare it with equal-work immediate-removal and oracle-logit
distillation controls. It explicitly separates this mechanism from the invalid
historical oracle-scalar residual recipe and grants no training authority.

Implementation began only at the information boundary. New
`shengji.rl.suphx_micro` keeps banker-known burial in the legal input, exposes
other live hands and nonbanker-hidden burial only as perfect features, consumes
equal named mask RNG work at both endpoints and produces identical public
tensors across hidden-world swaps when the mask is zero. Its focused gate is
12/12. A separate four-head policy/value skeleton now routes attacker/defender
and lead/follow decisions to disjoint parameters and accepts only the masked
perfect tensor. Named initialization leaves global Torch RNG unchanged;
zero-mask hidden-world swaps give bit-identical logits/value, while the full
mask demonstrably consumes hidden ownership. The combined focused gate is
19/19 and the encoder/Direct-Q/synchronous-resume-adjacent matrix is 80/80. No
actor, learner, registry entry or run exists.

The next mechanics layer then added a frozen categorical actor and one exact
on-policy actor-critic update. Mask and action sampling use separate named RNG
streams; each row records its complete ballot, mask, uniform action draw,
behavior log-probability and direct role-signed terminal return. A first test
found and repaired a real Float32 contract mismatch (`log(softmax)` versus
`log_softmax`) before any run. The learner now refuses a stale actor, updates a
checkpointed entropy controller per role/surface and publishes the exact next
candidate. Interrupting after a full-information batch and resuming into a
zero-information batch reproduces next-batch bytes plus learner, optimizer,
replay, RNG, controller and candidate state. Focused mechanics are 45/45 and
the broad adjacent matrix is 122/122. There is still no launch packet, evidence
asset or training authority.

## Evening verification separated runtime provenance from regressions

The first ordinary full-suite pass reported three red tests after the Suphx
mechanics landed. Isolation showed that none was a behavior regression:

- the two RLCB-C1 assertions were reopening a receipt whose ballot identity is
  intentionally compiled-runtime-specific from a pure-Python test process;
  both pass under the exact `SHENGJI_FAST=1` / strict-void runtime;
- the retired corrected-S0 parent intentionally detects one descendant
  registry SHA drift introduced when C1 later registered its null policy. Its
  immutable SELECT-NONE authority was not rewritten or reopened.

The tests now state those two boundaries literally. The focused C1/S0 matrix
passes 31/31 in both ordinary and compiled-strict modes, and the pre-repair
ordinary Mini baseline passed 948 with 27 skips in 5m28s. The green S0 test is a
proof of the historical introduction blob plus exact fail-closed drift, not a
claim that `verify_terminal_lock()` currently returns PASS.

## Independent review caught and closed a causal Suphx stream defect

Claude's 18:51 review found that the first Suphx collector derived its game
shuffle from `ActorBatchIdentity.seed`, which itself includes the actor
checkpoint digest. Once treatment and control actors diverged, they would have
received different deals and defeated the promised paired causal comparison.
Before any training, the schedule gained an explicit deal-stream root. Game
seeds now depend only on that root and batch/round sequence; actor identity,
arm, mask and action streams cannot enter. A real two-actor witness proves
different checkpoint digests reopen the same initial deal, observation,
perfect tensor and ballot while mask and action draws remain distinct. The
learner refuses any sample whose deal root differs from its frozen schedule.

The same review clarified reward semantics. Suphx still uses the legacy
attacker-point bracket capped at `+/-3.5`; its schema, field and documentation
now say that explicitly and state that it is not uncapped
`RoundResult.level_change`. No reward recipe was selected from outcomes.

The repaired actor contract/module SHAs are `d9c9a80e...9938a` /
`245a6b0f...1562a`; learning contract/module SHAs are
`1edc7609...3a9bb` / `2f42cccf...369d5`. Focused Suphx mechanics pass 48/48
in compiled-strict mode, the broader actor/learner/resume/DouZero-adjacent
matrix passes 132/132, and the final full ordinary suite passes 951 with 27
skips in 5m28s.
There is still no CLI, frozen O0 population, launch packet, registry policy or
training authority.

At 19:25 the frozen Teacher-v3 Stage-B job remained outcome-sealed: all eight
real workers were live at 86--89% CPU, progress was
`672/908/236/688/316/796/616/600` or `4,832/16,384` (29.5%), and no gold
final, partial or gate existed. The one-shot Stage-B and conditional champion
audit rules remain unchanged.

## The public-only lower-rate transition became executable without a run

While Teacher-v3 continued on Air, the Suphx waiting lane closed its next code
boundary on Mini. `SuphxSchedule` now refuses increasing privilege schedules.
The lower-rate transition accepts only a cryptographically valid exact-resume
checkpoint whose `gamma=1 -> 0` parent is fully exhausted and whose terminal
candidate was explicitly adopted. It verifies that actor bytes equal the
checkpoint learner, then starts an all-zero-mask segment at exactly one tenth
the parent learning rate.

The child preserves the complete model state, including all four entropy
controller buffers, while explicitly creating a fresh empty Adam optimizer,
empty replay ring and named learner RNG. Its schedule binds a fresh causal deal
root and proves the finite parent/child deal sets do not overlap. The transition
receipt records zero generated games, zero updates and no automatic training;
construction publishes only the exact initial candidate. A second test runs
one child iteration, checkpoints it, and reproduces the next batch, candidate
and full bundle state after exact resume.

The new actor contract/module SHAs are `25926963...bcc7f0` /
`70c99215...8a6e0`; learning contract/module SHAs are
`8683e364...adff29` / `3c6b7660...06df85`. Suphx focused mechanics pass 52/52
under compiled-strict routing, the broader adjacent matrix passes 136/136, and
the full ordinary Mini suite passes 955 with 27 skips in 5m35s.
This is still a mechanics gate only: no score-redacted O0 timing preflight,
frozen DEV/REPORT assets, launch packet, training job or strength result exists.

## Teacher audit capacity was measured without reading outcomes

A separate Mini-only timing probe used one fresh `timing_probe_v1` world for
each of the 64 already-frozen champion-audit states. It timed candidate zero,
printed no utility or action choice, read no Stage-B label outcome and created
no evidence artifact. Mean/max live ballot sizes were 6.09/14. Scaling each
probe by its ballot size and the frozen 32+32 outer worlds projects about 37.1
CPU-hours. Under the immutable eight-way state partition, rough Mini shard
times are `2.27/6.09/2.03/4.01/5.49/6.78/4.61/5.87` hours, for a 6.8-hour
critical path. This is noisy one-world capacity planning rather than an Air
ETA or strength claim, but it confirms that a Stage-B PASS should hand all
eight Air cores directly to the audit.

A second real compiled probe exercised all 14 candidates on two independently
named non-evidence outer worlds. It completed 1,604 downstream decisions,
including 1,271 full report-LCB searches and 1,009,950 inner candidate
rollouts, in 201 seconds. The exact tensor and continuation-telemetry
validators both returned no problems. It used no future champion fold, parent
choice or Stage-B outcome and published no artifact; its purpose was to catch
a runtime-only nested-search failure before the terminal transition.

The next preflight targeted information leakage rather than work accounting.
Across all 64 frozen audit states and all 12 phase/role/lead-follow cells,
swapping cards only between non-acting hidden hands left the report-LCB action,
semantic decision record, sampled-world count and counters identical; 56 cases
performed a real search. A second sweep swapped an unseen kitty card with
another hidden hand for every non-banker state and passed all 47 cases,
including 41 searches. Since current main carries the exact locked
`mcbot.py`/`memory.py` hashes, two bounded regressions now preserve those
boundaries. They pass 10/10 focused and 63/63 with adjacent sampler, memory and
invariant tests in both ordinary and compiled-strict routing.

Claude's 19:49 review independently closed the Suphx causal deal-stream and
reward-semantics HOLD and accepted the lower-rate public transition as sound
no-training mechanics. The remaining Suphx boundary is operational: O0 assets,
score-redacted runtime gate, CLI and exact launch packet still precede compute.

## Suphx O0 runtime admission passed without opening a learning result

The next waiting-lane boundary is now pushed at `b22ee8e`. A stdlib-only
launcher rejects experimental sampler/ballot flags before behavior imports;
the runtime then requires compiled+strict routing, a clean material tree and
exact hashes for the policy, learner, ballot, SmartBot controls, engine and
native binary. The preflight runs exactly three disposable rounds and updates
at each endpoint (`gamma=1`, `gamma=0`) from equal seeded model bytes and an
actor-independent shared deal stream. It deletes every temporary candidate
and structurally forbids rewards, chosen actions, losses, values, entropy,
gradients and terminal model identity from its artifact.

Focused falsification is 13/13. The full Suphx, Direct-Q and synchronous-resume
adjacent matrix is 158/158 in ordinary mode and 158/158 under compiled+strict
routing. The exact Mini run took 2.665 seconds and its artifact reopened at
SHA-256 `4f0c3dd542634b66fd0826a8caef5dc21c7a8b083f96804d1f2f9bbe653ee434`.
All six updates, 508 samples, four role/surface cells and the fixed causal-deal
digest reconciled. The conservative serial formula supports its capped
recommendation of 64 iterations per arm.

This is capacity and mechanics evidence only. The artifact explicitly leaves
O0 launch, O1, training and production authority false and retains no learned
checkpoint. Before any O0 run, a separate reviewed packet must freeze the DEV
diagnostic population, three seed/arm identities, action-spread and oracle-
learning stop rules, exact outputs and one-shot gate. O1 remains conditional on
O0 first proving that the privileged policy can learn without collapsing.

At 20:15, Teacher-v3 remained outcome-sealed and healthy on Air: all eight
workers were live after 5h31m, no gold final/partial/gate existed, and
outcome-blind progress reached `5,908/16,384` fold worlds (36.1%).

## The detached Teacher launch exposed an exit-status gap before completion

The frozen transition correctly required more than filename appearance: each
creator was supposed to exit zero because `teacher_v1_label.py` hard-links its
final before its last provenance check. Inspection while all eight gold jobs
were still live and zero finals existed found that their manual detached launch
did not persist wait statuses. Python PIDs `73123..73130` have PPID 1 and the
namespace contains no `.exit` receipts, so a literal status cannot be recovered
after they disappear.

This was not papered over. Before any result existed, the active handoff
predeclared the only executable equivalence available from unchanged code: the
recorded PID no longer runs its exact command; its regular log contains no
`REFUSING:` and ends with the exact flushed
`wrote <expected-output>: 16 records, digest ...` sentinel; the final is
regular; no partial remains; and the unchanged gate loader fully reopens the
artifact. That sentinel executes only after final linking, post-link
parent/runtime revalidation, exact artifact reopen and partial removal, and is
the last successful statement in `main()`. Claude must accept this equivalence
before the gate or reject the transition as unable to meet its literal rule.
At 20:28 all eight workers remained live with zero gold final or partial, so
the decision remains outcome-blind rather than a rescue chosen after results.

## Stage-B readiness became executable without opening outcomes

Commit `3d8bee0` pushed a stdlib-only readiness checker and falsification suite
for the predeclared detached-creator boundary. It pins the exact producer git,
source and parent hashes; rejects a dirty producer, live gold worker, missing or
extra final, surviving partial, pre-existing gate, refusal log or malformed
terminal success sentinel; and never imports the evaluator or opens a gold JSON
artifact. Deliberately opaque non-JSON final bytes in its tests prove that the
checker is testing publication mechanics rather than peeking at results. The
focused plus adjacent Teacher matrix passed 134/134 under both ordinary and
compiled-strict routing.

A streamed live invocation on Air at 20:34 created no remote file and returned
exit `4` / not ready with the expected mechanical facts: all eight original
Python workers remained live, all eight regular finals were absent,
`outcomes_opened=false`, `artifact_created=false`, and both gate and audit
authority false. At 20:36 the same sealed run had reached outcome-blind fold
counts `796/1228/628/788/356/840/972/728`, or `6,336/16,384` (38.7%), with all
eight workers still live. Claude's explicit acceptance of the predeclared
success-sentinel equivalence remains required before the checker may be used
to admit the one-shot gate.
