# Shengji daily log — 2026-08-08

## Opening state at 00:00 EDT

T1 enters the day with its production and closed-screen lanes preserved.
Fly release 17 remains healthy on exact `mc-s0-report-lcb` with compiled
activation; the first ordinary post-fix human room showed full-turn
p50/p95/max `0.904/1.716/1.907s`. RLCB-C1 is confirmed artifact-only,
V11 direct-v2 selected none, Direct-Q selected none, and formal S0 remains its
nonretryable SELECT-NONE closeout.

Teacher-v3 Stage B remains the live Air strength job. At 23:57 on August 7,
shard 1 became the first clean terminal publication after 33,126.2 seconds:
its process exited, the exact success sentinel was flushed, one regular final
exists and no partial remains. Its outcomes were not opened. Seven workers
remain live and the outcome-blind fold counters are
`1248/2048/1156/1076/824/1104/1260/1352`, or `10,068/16,384` (61.5%). The
reviewed readiness checker recognized exactly that one-final/seven-worker
state, returned exit `4`, opened no outcome, created no artifact and granted no
gate or audit authority. All eight shards must satisfy the same boundary
before the one-shot Stage-B gate; only PASS launches the already-frozen
64-state report-LCB continuation audit.

Suphx O0 is the independent Mini lane. The 23:52 review passed exact
`7b15338`'s chronological hidden-world mechanics and held freeze only because
material `SUPHX_MICRO_SPEC.md` retained the stale 22/22 focused-test count.
Exact pushed material snapshot `9aabf0b` corrects that count to 25/25, and the
compiled+strict focused suite passes 25/25. A final explicit independent PASS
for that snapshot remains required. The exact O0 root is absent; no packet,
admission, training, evaluation or gate exists. Even after implementation
PASS, packet bytes require a separate SHA-bound review before the six 64-update
arms may run on Mini.

Air therefore remains committed to seven surviving Stage-B workers while Mini
is intentionally available for O0 immediately after its two review boundaries.
The next transitions are mechanical rather than discretionary: finish all
Teacher shards and run the one-shot gate, and obtain the exact O0 material PASS
before its one allowed freeze.

## T1's closed evidence chains reopened from their authoritative runtimes

At 00:06, three fresh artifact-only verifications reproduced the already-
terminal T1 lanes without generating a game, learner update or new evidence
artifact:

- RLCB-C1 returned aggregate
  `83f5a9df2f1db1fa45d50fb005b941b776d9ecc2c9f8703d3d62efff8f5ef5ea`,
  closeout
  `06dd487de5389bb6ddbad38af39a1150fc98f52e6e6418c482e97ac8f3b7aae5`
  and state `FORMAL_CONFIRMATION_CONFIRMED_ARTIFACT_ONLY`.
- Air's exact clean `d1d2019` V11 repair reopened aggregate
  `b7c90ba4c1a9bb421a4cfcc788dbf1eb44365868f65ee0eb58257b38205d21ad`
  with `verified=true`, `protected_composition_authorized=false` and no
  production promotion.
- Direct-Q was recomputed from a temporary exact clean `b365120` worktree with
  the frozen native-binary hash. It returned `verified=true`,
  `passed_learning_screen=false` and `production_promotion=false`. The
  temporary worktree and its binary symlink were removed afterward; the
  evidence namespace was untouched.

This satisfies the current-state reopening portion of the T1 completion audit
for V11-v2, Direct-Q and RLCB-C1. Teacher-v3 remains the unfinished objective
requirement; production latency remains a live operational monitor.

## Production latency hardening reopened on current main

Fly's live machine still reports release 17, exact image
`registry.fly.io/shengji:latency-cd6789e`, digest
`047bcfe4d4573961734a5536ad549605fd0df5e1477d7480cdf322282955b300`,
one shared vCPU, `mc-s0-report-lcb`, compiled activation and a passing health
check. Recent logs contain uninterrupted health checks and no new human game
traffic, so they add availability evidence but no new latency sample.

The current-main scheduler, X-ray, game, invariant, replay-benchmark and
WebSocket regression surface passes 95/95 from the standard `server/` working
directory. Running that same matrix from repository root exposed one test-only
CWD assumption: `test_acting_team_sign_is_correct_for_both_roles` opened the
registered `rl_data` sources relative to the shell rather than the known
server root. Its path resolution now anchors relative sources to `server/`;
the complete matrix passes 95/95 from repository root as well. Runtime code and
all experiment artifacts are unchanged.

## Suphx O0 material passed and the packet was frozen

At 00:49, independent review explicitly passed exact material snapshot
`9aabf0b` for its one allowed packet freeze. The material set has no later or
dirty delta, and the compiled-strict focused suite remains 25/25. The exact
freeze command then generated the 128-state DEV asset twice—once for publication
and once for independent semantic reopening—and published `launch_packet.json`
SHA-256
`6d4e6772e94df292fe0a7b72735ea3995e4f6098cca9e5c37ab12268bed1ed65`.

The namespace contains only the frozen spec, DEV asset, three initial
checkpoints and launch packet. No admission, training, evaluation or terminal
gate exists. Mini remains free pending the separately required machine-readable
packet-hash review; the material PASS and packet freeze alone authorize no
learning, O1, strength claim or production change.

## Suphx O0 ran to its exact terminal gate

At 01:05 the immutable review source contained the exact machine-readable PASS
marker for packet `6d4e6772…1ed65`; those bytes did not name a reviewer. The
source contained exactly one marker and hashed to
`0450fc45…d8789`; the immutable copied review and packet admission published as
`review_admission.json` SHA-256
`67f79f29bfec4428485800875fe62d842ee25b2f03f95a41b0e99664c23e590b`.
Its authority was narrow: O0 training true, O1/strength/production false.

All six frozen Mini arms—three exact seed pairs times oracle/public—completed
64 one-round updates apiece. Each exercised the registered 32-update
runner/learner teardown and exact resume. All six 64-row ledgers, manifests,
midpoint/terminal checkpoints, actor adoption chains, causal deal sequences,
finite models and four role/surface totals reopened successfully. The first
one-shot `launchctl submit` wrappers attempted to respawn after success; every
second invocation immediately refused to overwrite its already-final JSONL.
The six services were then unloaded. This produced no replay or artifact
mutation and is an orchestration lesson: future one-shot launchd jobs need an
explicit non-restarting wrapper.

The three frozen DEV evaluations then completed with exit zero and no stderr.
The one-shot terminal gate recomputed every diagnostic and semantically replayed
all raw comparison rounds before publishing gate SHA-256
`592a009aaf6fbd6680b6d9bab5e9738832050d1654b71dc6f2e19612d0bd407c`.
A separate `verify-gate` repeated that work and returned
`{"verified":true,"verdict":"SELECT_NONE"}`.

The result is informative but not a pass:

- oracle terminal minus exact initialization: mean `+0.335938`, one-sided LCB
  `+0.273991`;
- oracle terminal minus equal-seed public terminal: mean `+0.072917`, LCB
  `+0.002548`;
- per-seed oracle-minus-public means:
  `+0.343750/-0.207031/+0.082031`.

Seed 1 therefore failed the preregistered requirement that every frozen seed
show a positive oracle-public mean. Every other conjunction passed: both
aggregate LCBs, every-seed oracle-initial positivity, exact work/resume,
finite models/controllers, all role/surface cells, entropy floor, legal hidden-
hand and burial oracle sensitivity, exact public invariance and the exact-zero
same-model two-flip null. The policies nevertheless retained normalized
entropies very close to one, and the ensemble oracle-public LCB barely cleared
zero; those are diagnostics of weak/unstable oracle use, not alternate routes
to PASS.

O1 is not authorized, and this exact O0 run may not gain iterations, seeds,
checkpoints or tuned DEV rules. The correct next step is a fresh preregistered
mechanism question that explains how reward credit, feature scaling, update
dose or the policy objective should create a robust oracle advantage. This
does not establish that Suphx-style learning as a family fails.

A read-only post-terminal checkpoint trace narrows that design question. All
24 seed/arm/surface entropy coefficients monotonically reached zero: lead heads
at iterations 10–12 and follow heads at 48–53. Yet terminal DEV normalized
entropy stayed between about `0.999997` and `1.000000`. The run was not an
inert-network accident: oracle perfect-encoder parameters moved about 25–29%
in relative L2; other major parameter groups moved roughly 18–39%; and greedy
actions changed from initialization on 53–60% of oracle DEV states and 52–62%
of public states. The public perfect branch moved only about 2%, consistent
with zero inputs affecting biases but not using hidden cards. The immediate
question is therefore not simply “more updates”: determine whether the
nonnegative entropy bonus is intentionally only a minimum-entropy controller,
whether tiny logit rankings plus deterministic greedy evaluation are too
fragile, and whether arm-separated action streams with only three fixed seed
pairs made the oracle comparison too noisy. This remains diagnosis, not an
alternate pass or authority claim.

At 01:18 Teacher-v3 remained outcome-sealed on Air with one final and seven
healthy workers. Corrected sequential fold counters were
`1636/2048/1396/1464/1004/1304/1564/1524`, or `11,940/16,384` (72.9%). No
Stage-B outcome file was opened and no gate or audit authority exists yet.

## Teacher-v3 Stage B passed; audit publication failed closed and was repaired

At 07:28 the reviewed outcome-blind checker returned exit zero: all eight gold
finals were regular, every detached worker was gone, the producer remained
clean at exact `1a2a713`, no partial or prior gate existed, and the checker
reported `outcomes_opened=false` / `artifact_created=false`. This authorized
the one registered Stage-B gate invocation. It exited zero with terminal PASS,
no problems and gate SHA
`f607b48986aaa8b05194f88e8638540bc5c9360f09f3c28a7565d8d8cac89694`.
Across 128 states, cheap-minus-gold mean regret was `-0.002686`; its one-sided
upper bound was `0.019548`, comfortably below the frozen `0.10` limit. This
establishes the Stage-B attribution screen, not champion strength.

The conditional audit transition then caught another real one-shot bug before
spending audit compute. Controller `1c97928` copied and rehashed all 20 exact
parents, but the receipt child exited 3. It had hard-linked the completed
receipt to its final name and deliberately retained the same-inode `.partial`
as a refusal marker during post-link checks. Its own verifier then called the
ordinary `load_pinned()` reader, whose correct contract is to reject every
adjacent partial. The final and partial hashes were identical; no receipt exit
zero, preparation manifest, audit label, rollout or audit gate exists. The v1
root at exact `182d1df` is preserved unchanged as failed evidence and may not
be retried or adopted.

Pushed audit repair `1866132` fixes the same latent defect in receipt, label and
terminal-gate publication through one helper. It permits the adjacent partial
only when final and partial are regular non-symlinks to the same device/inode,
then reopens the final bytes and requires their exact SHA before revalidating
parents/runtime; a separate same-byte partial is rejected. The frozen
state-set publisher was already safe and is unchanged. Pushed controller
`c9a5c3f` requires repaired audit git `1866132`, script SHA
`c7b47a7a...d6cb` and new run ID
`teacher-v3-report-lcb-audit-v2-149m`, so the failed v1 checkout cannot enter.

Fresh Air roots were created without touching v1. Their evaluator/controller/
adjacent tests pass 21/21, 21/21 and 119/119. Exact native and frozen-state
SHAs remain `ef7c1618...66b4d` and `d04d1c0f...f7a34`; a read-only preflight
bound all 20 Stage-B parents with no problem and created no artifact. The v2
receipt and eight 32/32 labels remain held for an explicit independent review;
Air is intentionally idle at that boundary.

## Adversarial review found and closed two launch-hardening gaps

Claude's consolidated 102-commit review reproduced every closed result from
its frozen evidence and found no production rollback condition. It did expose
a critical prospective scheduler test hole: no test compared the speculative
snapshot/worker decision with direct `decide_play` on identical state. A
single extra snapshot RNG draw changed 3/12 MC witnesses and the deployed
report-LCB seed-3 witness while the old release gate stayed green. Pushed
`edc923f` adds named MC and report-LCB equivalence witnesses and proves each is
non-vacuous by requiring a one-draw perturbation to change the action. The
scheduler/replay focused surface passes 26/26; production runtime bytes were
not changed.

The same review added two Teacher supervisor falsifiability gaps: a label child
could exit zero without a final, or with a surviving partial, and the guards
were not mutation-covered. Both refusal paths now have end-to-end tests that
assert no gate. More importantly, the first audit-v2 review correctly returned
HOLD: controller `c9a5c3f` hash-bound the receipt but never parsed its authority
fields, so a receipt and preparation coherently rebound around the old v1 run
ID could pass preflight. No v2 receipt or label had been created, so no compute
was lost or restarted.

Controller `edc923f` now independently opens the receipt and requires exact
schema, `complete=true`, run ID
`teacher-v3-report-lcb-audit-v2-149m`, evaluator git and evaluator-script SHA.
The preparation manifest must bind the same identity. Four coherent mutations
of schema/completion/run/predeclaration, with both external hashes rebound,
all refuse before progress or children. Air was advanced cleanly to exact
`edc923f`; 27/27 controller tests pass there. Its outcome-blind preflight
reports zero problems, all 20 exact parents and `artifact_created=false` under
preparer SHA `084c7e98...8434` and supervisor SHA `59610ef0...bb06`. The
unchanged 64-state audit remains review-held; Stage-B evidence and the repaired
evaluator are unchanged.

## Audit-v2 passed review and launched; strategy audit narrows the next work

The revised exact review passed for evaluator `1866132`, evaluator-script SHA
`c7b47a7a...d6cb` and controller `edc923f`. The one authorized preparation
exited zero. Independent reopen confirmed receipt SHA
`ce51b826d4f04549b961f795868cc4c6c5f90124a8552ce76fe2d3ab0bd471d0`,
preparation SHA
`7f89a86c2e0803d83473d8ccca978dd99dd010e467761d0d4429a3598c166605`,
exact v2 schema/run/execution identity in both artifacts, receipt-child exit
zero, all 20 copied parents, no partial or descendant and an empty supervisor
preflight. At 08:16 Air supervisor PID 95339 launched all eight frozen 32/32
label workers (PIDs 95345--95352). At the first liveness check all eight were
running near 82--85% CPU and score-free fold progress had begun. No partial
outcome was read; the supervisor alone owns the terminal gate transition.

Claude's four-day strategy synthesis gets the central priority right:
report-LCB is the confirmed champion; V11 direct is stopped; Teacher should
become a counterfactual oracle rather than a champion clone; and Direct-Q/O0
should merge into a small mechanism battery. The near-term strength lane is
structured search, while Teacher is the medium-term flywheel that can improve
search labels, proposals and leaves.

Four qualifications were added to the live backlog. First, the demonstrated
report-LCB mechanism is fixed-challenger paired re-evaluation on disjoint
worlds plus a conservative positive LCB—not generic extra compute. Second,
Stage B's cheap≈gold result does not establish cheap-label fidelity on the
mined high-SE/disagreement tail; Stage C must escalate hard states to gold or
exact-late labeling and separately gate hard-tail regret. Third, v11pair is a
proposal-diversity hypothesis, not a confirmed strength role, and needs a
same-budget random-diversity control. Fourth, an O0-v2 experiment should use
CRN and at least eight training seeds as common design, then isolate entropy
sharpening and dose factorially rather than changing CRN, controller, dose and
target in one uninterpretable bundle.

The structured-search execution detail is also narrower than the synthesis
implied. Current S3a is a 512-state, state-level mechanism screen whose PASS
only authorizes designing a later full-game duel; it is not itself a strength
screen with an automatic 8,192 confirmation. S3b already has a 2,048-screen /
8,192-confirm protocol, but both scripts derive `mc-strong` from the formal S0
SELECT NONE packet. Before either spends strength compute, a new reviewed
version must bind the actual live parent `mc-s0-report-lcb` explicitly.

## T2 was named and the working documentation was compacted

T1 now has one remaining terminal dependency: the already-running Teacher-v3
audit. Production/release 17, RLCB-C1, V11 direct-v2, Direct-Q and Suphx O0 are
all terminal. Any valid PASS/FAIL/INCONCLUSIVE from the sealed audit closes T1;
the result need not be favorable and may not be retried.

T2 is **First live-champion challenger**. Its first gate is a reusable parent
contract that binds exact `mc-s0-report-lcb` and independent RLCB-C1 authority,
making stale formal-S0 `mc-strong` unreachable. S3a then runs only its existing
512-state structured-bury mechanism screen; a PASS authorizes duel design, not
promotion. S3b first runs its two-cluster score-free Mini throughput preflight,
then conditionally its 2,048 complete-round screen and untouched 8,192
confirmation. Teacher Stage C remains conditional on the current audit and a
separate hard-tail-regret contract.

The documentation now has explicit ownership rather than repeated current
truth. `README.md` carries a two-minute plain-English state and glossary;
`AI_POLICIES.md` owns one canonical terminal-results table and callable
policy/toggle conclusions; `BACKLOG.md` owns only milestones/order/gates;
`RL_PLAN.md` owns design rationale, model lineage and the data contract.
`HANDOFF_ACTIVE.md` was reduced to the live audit, T2 implementation packet and
Claude requests. `JOBS.md` was reduced from 810 lines to the live job plus
terminal stubs after its exact old body was preserved at
`docs_archive/jobs-through-2026-08-08.md`, original-body SHA-256
`26beff936f6c0744b220fc79e233163c8f09acde8a13adcba5450327ad132252`.

The closed DEV-512 `BALLOT_PLAN.md` moved to
`docs_archive/ballot-plan-through-2026-08-05.md`. `TEACHER_V1_SPEC.md` remains
top-level only while its v3 audit is live; its historical filename is now
explicitly documented. `SUPHX_MICRO_SPEC.md` remains evidence-bound because
the terminal O0 verifier names that exact material path. Claude acknowledged
and froze an exact review-ledger cutoff: the first 2,595 lines moved byte-for-
byte to `docs_archive/handoff-review-through-2026-08-07.md` (159,855 bytes,
SHA-256 `c2036a1446823486ca38076d8a44d531d756123e19c8277d18a77ce7c9d6e06f`).
The active ledger fell from 3,044 to 449 lines while retaining all three live
machine-readable markers, the current strategy review and the acknowledgment.

## T2.0 live-parent code closed locally and entered independent review

Pushed commit `05ea1d1` replaces the obsolete formal-S0 reference in both S3
consumers with one reusable `live-champion-parent-v1`. The authenticator does
not reinterpret S0c. It independently reopens the complete RLCB-C1 artifact
closeout (`06dd487d...b7aae5`), aggregate (`83f5a9df...f5ef5ea`) and freeze
receipt (`02c286ed...39d0`), then checks exact report-LCB policy-contract,
source/registry/ballot and compiled-binary identities against the current
executable. It also records release-17 deployment identity as an operator
attestation while explicitly leaving strength authority with RLCB-C1.

S3a and S3b now use fresh v2 schemas. S3a preserves the exact 512-state 136M
geometry, structured/legacy-four/random controls, R=120 disjoint report fold,
equal work and state-only `AUTHORIZE_DUEL_DESIGN` boundary. S3b preserves the
2,048-screen/8,192-confirm geometry, one-round signed-level utility, exact-use
counters and champion-matched null. Its runner now registers only live
report-LCB; `mc-strong`, adaptive and unknown references refuse before work.

Mutation coverage targets stale champion, self-consistent source/registry
rewrite, policy-contract drift, mismatched confirmation and any formal-S0
fallback. Final focused acceptance was 27/27 replay-heavy S3a plus 45/45
parent/S3b/structured-bury/RLCB-support tests. A clean pushed verification
reopened the raw confirmation and emitted parent output SHA-256
`5f9ddbfb358008706376a1820c52fe4cde53570e8b54ecee951d042b6c298402`.
No compute or production change was authorized; an exact independent review
packet was posted in `HANDOFF_ACTIVE.md`.

Fleet use is now explicitly two-queue. Air remains dedicated to the sealed
Teacher audit; at 09:32 all eight workers plus the supervisor were live with
zero finals and regular heartbeats. Mini ran the replay-heavy correctness
matrix. After external PASS, its first compute is only S3b's two-cluster
score-free throughput preflight under hard operational ceilings: screen 200
fleet-hours / 30 max shard-hours and confirmation 800 / 120. While review or
compute runs, implementation continues on S3a outcome-free sizing, both
Teacher terminal adapters, O0-v2 CRN/logit-margin infrastructure and a shared
progress/experiment receipt. Utilization means useful admitted work, not an
unreviewed strength run merely to fill cores.

## S3a sizing became runnable without consuming strength evidence

Pushed `66d6836` adds a separate `s3a-bury-throughput-preflight-v1` rather
than changing the already-posted live-parent packet. It executes the exact S3a
v2 producer on only two reserved timing states, 151,000,000--151,000,001,
outside the registered 136M strength population. Complete action and outcome
records exist only in memory and are discarded; the published receipt admits
only authenticated timing, work and capacity fields.

The preregistered projection is twice the observed seconds per state times 512
full-screen states or 64 states per shard. Hard placement ceilings are `400`
fleet-hours and `60` max shard-hours. Hash, parent, runtime, ancestry,
arithmetic and forbidden-field mutations refuse; 5/5 focused tests pass. A
preflight PASS can decide only where the screen fits—it cannot launch or pass
the screen. The exact review packet is in `HANDOFF_ACTIVE.md`; no timing or
strength job has run.

This closes the first parallel implementation item while Air remains occupied
by the sealed Teacher audit. After independent review, S3b and S3a timing will
run alone in separate Mini windows; Teacher terminal adapters and O0-v2
infrastructure remain the no-idle coding path during both windows.

## Both Teacher outcomes now have a fail-closed next step

Pushed `c961c14` adds `teacher-v3-terminal-adapter-v1`. It reopens the exact
terminal gate and final supervisor JSONL, requiring their hashes plus exact
audit git, script, run, 32/32 folds, continuation, eight-label population,
verdict and return-code agreement. Unknown verdicts, surviving partials,
mutated evidence and overwrite attempts refuse.

The two branches were committed before the live result. PASS emits only a
review-required hard-tail Stage-C design: fresh non-evaluation states,
disjoint selection/report folds, uncertainty/disagreement mining,
gold/report-LCB-or-deeper escalation, information-set-legal exact-late labels
and separate hard-tail regret. FAIL/INCONCLUSIVE permits only diagnostic cuts
over the existing frozen audit and forbids new states or worlds. Both branches
set compute, bulk labeling, training, production promotion and audit retry to
false, so the adapter cannot accidentally turn a favorable result into a
2,048-state launch.

Ten adapter falsification tests plus 27 preparation/supervisor tests pass on a
clean pushed tree, 37/37 total. The review packet is posted in
`HANDOFF_ACTIVE.md`. This is the second no-idle implementation item completed
while Air runs; O0-v2 common-random-number and logit-margin infrastructure is
next.

## O0-v2 separated CRN repair from policy sharpening

Pushed `8f3cca4` adds mechanics only for the next Suphx-style microbattery. A
keyed common-random-number stream shares deal, privilege-mask and action draws
between oracle/public arms only when they reach the same legal public decision
context. Arm, model, checkpoint, logits and hidden/perfect features cannot
enter the draw key; diverged public histories receive different keys. Training
seed remains an inference unit and the spec refuses fewer than eight unique
seeds.

Optional top-two logit-margin control is a separate factorial cell. Its
squared target increases a below-target margin and decreases an above-target
margin; forced actions contribute exact zero. The control CRN cell has no
margin loss. Both preserve the old 64-iteration dose and clipped acting-team
reward target, explicitly forbidding simultaneous dose, target, privilege or
optimizer changes.

Thirteen focused tests and the existing Suphx actor/learner/O0 screen matrix
pass. This module has no collector, runner, evidence population, CLI, gate or
launch authority. Fresh packet integration remains; no O0 compute was started.

## T2 live-champion parent passed independent review

At 09:54 Claude passed exact commit `05ea1d1`, ordered six-file material
SHA-256 `66be133c4e4caab127fd68efbb0ed91952ad9047762ca331215cad5ee535e17c`.
The review reproduced parent output `5f9ddbfb…8402`, reopened the real RLCB-C1
closeout, passed 27/27 S3a and 54/54 parent/S3b/support tests, and mutation-
tested anchor drift plus formal-S0/`mc-strong` re-entry.

The review separately preserved S3a's 512-state/R=120/state-only boundary and
S3b's 2,048/8,192, two-cluster score-free and one-round boundaries. It conveyed
no strength or production authority. The exact marker is preserved in
`HANDOFF_REVIEW.md`. T2.0 is closed; only the predeclared score-free S3b Mini
preflight is now admitted.

Claude initially passed the two remaining implementation reviews, then a
bounded wrong-artifact probe correctly superseded both markers with HOLD.
S3a's top-level receipt whitelist still accepted arbitrary nested
`work_totals` fields. The Teacher fixture itself named gate label digests
`1..8` and terminal supervisor digests `11..18`, yet both validators accepted
them. No corresponding packet may be used under the initial markers.

The repairs were implemented without opening an outcome or starting compute.
S3a now requires the exact nested work/arm/fold schemas, non-boolean integer
types, equal candidate work, exact sampler counter equalities and fixed report
world count. The Teacher adapter now requires the exact gate input item schema,
canonical ordered shard indices and exact ordered equality with the terminal
supervisor's eight label digests. The focused repair suite passed 23/23 and the
broader live-parent/S3a/Teacher boundary matrix passed 85/85 in 128.04 seconds.
Repair commit `2de0824` was pushed with exact material hashes and a bounded
two-marker re-review request. Independent re-review remains required.

The first literal S3b preflight invocation at 09:56 refused before any round
because those concurrent review appends made the tree dirty after the earlier
clean check. It exited 3 at the initial cleanliness guard, created no output or
partial, and consumed no timing or strength evidence. The unchanged command
remains authorized after the review append is committed and pushed.

## S3b v2 score-free preflight terminally held

After the review bytes and repair packet were committed and pushed, the exact
predeclared S3b command ran from a clean Mini tree at
`cd44ea8a6fefb8fba258d01bcca4bed98169a217`. It entered the exact-treatment
arm on seed 141,000,000 but never printed the first `1/2` cluster completion.
The shared per-world exact session reached its frozen cumulative node cap and
raised `ExactEndgameBudgetExceeded: exact endgame exceeded max_nodes=250000`;
the process exited 1.

The runner intentionally uses an OS-unlinked temporary score sink. Both final
and `.partial` throughput receipt paths were absent after exit, so no score,
raw record or timing projection survived. This is not strength evidence and
does not estimate capacity, but it is a clean falsification of v2's required
zero-overflow feasibility condition. The 2,048 screen is unauthorized. Do not
retry the seed, raise the v2 cap or introduce a heuristic fallback; any
narrower threshold, solver optimization or different bound is a fresh v3
mechanism packet.

## Teacher terminal handling frozen before the verdict

While all eight Air workers remained live and before any terminal artifact
existed, the operator path was fixed in `JOBS.md`. Exact gate and supervisor
finals will be hashed on Air, mirrored byte-for-byte to one Mini namespace and
reopened through the repaired adapter from a clean detached `2de0824`
worktree. Only after that identity chain passes may the branch be read. PASS
routes to hard-tail Stage-C packet design; FAIL/INCONCLUSIVE routes to frozen-
evidence diagnosis. Both branches deny compute, bulk labels, training,
promotion and retry.

## Both T2 evidence repairs passed independent re-review

At 10:24 Claude independently passed both exact `2de0824` repairs. For S3a,
the prior nested `{"strength_score":123.0}` injection now refuses, guard
neutralization makes the negative tests fail, and material
`fb0fa7ba…6e16` is exact. For the Teacher adapter, valid-but-different
terminal digests `11..18` now refuse against gate inputs `1..8`; extra fields,
reordered shards and guard neutralization are covered, with exact material
`ccb73bb7…e6d`. The focused 23/23 and broad 85/85 counts reproduced. S3a's
two-state score-free Mini sizing run is now admitted; the Teacher adapter waits
for the sealed audit's terminal finals.

## S3a v1 publication refused; fresh v2 repair coded

The reviewed S3a v1 command ran once from clean head `b3ac6fd`. Both hidden
timing states 151,000,000–151,000,001 completed, but the final verifier then
mistook the authenticated runtime source digest key
`runtime_identity.digests.cards` for a persisted card outcome and exited 3.
No final, partial, action, score or raw record survived. The states are retired
and will not be replayed.

The repair bumps the receipt to v2 and moves to fresh seeds
151,000,002–151,000,003. It excludes only the equality-bound runtime and live-
parent identity trees from the recursive outcome-key scan; every dynamic
receipt surface remains recursively scanned, with exact top-level/nested work
schemas still enforced. A real-shaped runtime containing `digests.cards`
passes, identity drift refuses, and an actual `caps.cards` injection refuses.
Focused tests pass 11/11 and the broad boundary matrix passes 86/86. The repair
requires a new independent PASS and fresh output predeclaration before running.

Repair commit `68d930f` was pushed with exact ordered material SHA-256
`4385661a…3aa`, focused 11/11 and broad 86/86. The bounded review request asks
Claude to reproduce the original realistic `digests.cards` false positive,
falsify both fixed-identity exemptions, recheck dynamic `caps.cards` and nested
work injection, and prove the fresh v2 schema/seeds. Output
`s3a-bury-v2-throughput-mini-v2.json` and the unchanged `400/60` caps were
predeclared before review. No v2 run is authorized without the new marker.

## Teacher refusal diagnostics separated fact from hypothesis

The audit-v2 failure originally looked like a simple sampler underfill, but
the live champion actually has exact accepted-work enabled and its old generic
message combined four distinct cases: a true short report, a complete report
that required failed/rejected attempts, fixed-policy drift, and counter/work
bookkeeping drift. Commit `b7534ee` split those paths without changing which
continuations are accepted. It also exhaustively types the five reachable
searched-play exits and keeps the diagnostic score-free. Claude independently
passed material `8ede4d35…43e7` at 14:36.

The surviving conclusion is deliberately narrower: audit-v2 was an
operational refusal and produced no ML verdict. The next Teacher attempt must
first choose and synthetically prove a versioned estimand—most likely exact
accepted report-world dose with retries fully reconciled, while any true
underfill/cap hit still refuses. No consumed state may be replayed and no fresh
evidence run is authorized by the diagnostic PASS.

## O0-v2 now has one shared public decision key

Commit `dd730a8` added a runner-facing projection built only from immutable
round-derived public/legal inputs. Oracle and public arms cannot pass arm,
model, logits, mask, observation or perfect-information tensors into that
endpoint. The outcome-free gate requires all 8 training seeds crossed with 64
iterations and exact 100% coupling at the shared first public context; later
keys may diverge after the policies choose different actions.

Claude passed exact material `639c259b…a0494b` at 14:39 after reproducing the
privileged-plane decoupling mutant, missing/duplicate/outcome-field refusals,
legitimate later divergence and a non-vacuity mutation. This permits merging
the guard and designing a separately reviewed runner packet only. It does not
authorize training or O1.

## S3a structured-bury sizing passed by a wide margin

After Claude passed the final fixed-`claim_boundary` repair at `c784e6d`, the
literal fresh two-state Mini command ran once from clean `79ab7d2`. Receipt
`cf770277…5c431` reopened under the exact verifier. Both states completed in
0.999 seconds; the frozen 2x projection is 0.142 fleet-hours and 0.0178
max-shard hours versus caps of 400 and 60. Selection accepted 220/220 worlds,
report accepted 240/240, every arm consumed 792 candidate-worlds, and there
were no failed, rejected or impossible worlds. No action, score or raw record
was retained.

This is a capacity result, not a strength result. It clears Mini placement and
makes the next action concrete: freeze and independently review the exact
512-state command, 136M population, outputs, estimand, gate, stop rule and
controller before consuming any screen outcome.

## S3b's failure points to a narrower v3 resource contract

Eight additional outcome-free synthetic four-card roots each solved below the
250,000-node ceiling (17,040–84,682 nodes, 0.188–0.838 seconds, zero caps).
That makes a single pathological root less likely. The v2 preflight instead
shared one cumulative exact-session budget across several candidate
frontiers, so the likely repair is a per-frontier ceiling plus a distinct
cumulative total-work guard—not simply increasing the consumed v2 cap. This
is only a mechanism diagnosis; any S3b v3 needs a fresh packet and review.
