# Daily log — 2026-08-10

This is the dated execution record. `AI_POLICIES.md` owns terminal policy
conclusions, `RL_PLAN.md` owns the durable research strategy, `BACKLOG.md` owns
what is next, and `JOBS.md` owns live compute.

## Plain-English milestone ledger

| milestone | why / strategy, in plain English | progress and learning | what is left |
|---|---|---|---|
| **S4 point-banking whole-game screen** | The rollout policy normally spends the cheapest winning card. Test whether banking a 5/10/K while retaining a higher winner improves complete rounds, not only hand-picked late tricks. | **SCREEN PASS.** All 2,048 paired clusters completed under exact `cad3992`. Treatment-minus-live report-LCB was `+0.086914 +/- 0.056166` signed levels, LCB `+0.030748`; treatment-minus-null was identical and matched-null-minus-champion was exactly zero. This is the first evidence that the narrow point-banking change survives natural complete-round traffic. | Preserve aggregate `3c7f27b8…4268`, final `e188f7e8…0f2b` and progress `97d910c1…c0fb`. Prepare and externally review a fresh 8,192-cluster confirmation packet later. Do not launch confirmation, promote or deploy under the current goal. |
| **H0 human/V11 proposal diagnostic** | Human and V11 moves should earn entry by beating production under equal common-world work, not by source reputation or imitation. | The one reviewed execution completed 555/557 rows and exactly 705,750 candidate-world rollouts, then terminally refused. Two seven-card follow throws had 12 live-ballot actions but only three generic analysis-enumerator actions, falsifying the controller's ballot-membership assumption. No aggregate utility was published. The guards prevented 555 partial outcomes from becoming a misleading result. | Close the namespace permanently: no retry and no partial-outcome use. Admit no human-derived proposal rule to Stage C. Keep the two failures as exact ballot regressions and input to dedicated throw sourcing. |
| **Teacher Stage-C capture v3** | Build 2,048 fresh training states that keep broad routine play but spend extra work on uncertainty, model disagreement, point play, bury and small endgames. The population must be reproducible even when rejected deals never become selected rows. | Capture source `0b697b6` and packet `d58a9308…c91` now replay-authenticate all 750,000 scan dispositions, recompute exact argmax/margin eligibility and separately cap capture plus terminal replay. Focused tests pass 33/33 and the compiled strict-void cross-lane battery passes 134/134. Mini is clean, compiled and staged; zero states/worlds/outcomes exist. | Obtain the fresh exact external v3 marker—the older v1 PASS is superseded. Then admit once, run 24 fixed shards at eight-way concurrency, terminally replay every disposition and freeze exactly 1,024 DESIGN / 512 CALIB / 512 untouched REPORT states. Any refusal consumes the namespace; do not retry. |
| **Teacher Stage-C labels** | Turn the 2,048 states into better-than-heuristic supervision with disjoint selection/report/audit worlds and finite work, then train ranking and calibrated outcome surfaces that search can actually consume. | Pushed source `c98b608` implements real admit/run-shard/aggregate commands, 16 fixed 128-state shards, per-shard one-shot slots, exact hidden-world disjointness and refusal-prefix accounting. An explicit sampler sequence now rejects a fully rehashed report-before-selection forgery. Focused tests pass 24/24 and the full compiled strict-void cross-lane battery 133/133. No state set, packet, label world or outcome exists. | Pass source-readiness review. After capture, independently review the frozen state set, run the mandatory bounded no-retained-outcome capacity preflight, freeze/review the exact label packet, then execute and aggregate once. Only a passing terminal label gate may open the fixed ≥8-seed training packet. |
| **S6 dedicated 甩牌 sourcing** | KESP showed that search cannot choose a strong throw if the ballot never presents it. Treat whole-suit evacuation and risky-but-plausible component bundles as explicit proposals whose risk is priced by search. | Commit `bdac287` records three witnesses: a top/near-boss bundle despite retained lows, a boss-component bundle despite ruff risk, and whole remaining plain-suit evacuation. Current Stage-C V11/quota/random diversity may discover some by chance but does not guarantee the third. | Freeze exact replay fixtures and negative triggers, implement a dedicated source with dose counters, and define an equal-count/equal-work matched state screen. Keep S6 separate from the already-frozen Stage-C population so its exam does not move mid-review. |
| **Fleet / overnight execution** | Spend compute on the shortest path to a stronger learned challenger while doing independent code and packet work during long runs. | S4 released Mini. Mini and Air are free; the exact capture-v3 Mini worktree, Python 3.14.3 and compiled fast extension are ready. Air has no strength job and must not duplicate one-shot evidence. | Immediately launch capture on Mini after the exact v3 PASS. While it runs, finish label/state-set/capacity packet preparation, the S4 confirmation packet and S6 fixtures. If capture and all label gates finish, run 16 label shards, then split at least eight fixed training seeds across available machines. REPORT stays sealed. |

## Durable strategic synthesis

S4 is tangible strength progress: a human-observed point-banking weakness became
a narrow continuation change, passed exact-state tests, and then posted a
positive lower bound over fresh complete rounds. It still needs confirmation;
the screen is not a production decision.

H0 is a useful negative rather than discarded work. It did not prove human data
unhelpful. It proved that this exact all-rows counterfactual contract made a
false ballot-membership assumption. Because the run stopped without aggregate
utility, it cannot support or reject any human proposal source. Stage C therefore
continues conservatively with frozen V11, structured and matched-random proposal
sources, while the two throw failures become ballot-engineering regressions.

The T4 critical path is now concrete: capture and review 2,048 states; capacity-
test, freeze, review and run finite labels; train at least eight seeds; select
only on DESIGN/CALIB; open REPORT once; and place the one passer inside
report-LCB with the incumbent fallback and same-work null. Reviews are gates in
that sequence, not substitutes for producing the dataset, model and fresh
whole-game strength result.

## Authority boundary

No Stage-C capture may start from the superseded v1 review. No label work may
start before state-set review, capacity PASS and packet-specific PASS. No REPORT
row may be opened during training selection. No S4 confirmation, promotion or
production change is authorized by today's screen.
