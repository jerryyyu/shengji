# Daily log — 2026-08-12

Last updated: 23:25 EDT. This is a plain-English chronology; canonical policy
verdicts live in `AI_POLICIES.md` and the active queue lives in `BACKLOG.md`.

## Why today matters

The live champion still depends on Monte Carlo search. Today's goal is to turn
three promising, narrow mechanisms into search improvements while the first
learned-model-inside-search policy receives a complete-round test. The process
must preserve trustworthy evidence without leaving Air idle behind Mini's long
T4 run.

## Milestone ledger

| milestone | why we are doing it | progress today | what remains |
|---|---|---|---|
| **T4 mid/late learned-search hybrid** | Test whether model proposals improve search while the incumbent remains fallback. | Mini is healthy with zero terminal shards. Seven shards are at `matched-null 100/512`; one is finishing treatment at `500/512` (38.9% of sequential round work overall). Outcomes remain sealed. | Finish all three arms, review the score-free final, aggregate once and obtain terminal review. |
| **S4 future point-banking confirmation** | Resolve two positive but individually inconclusive estimates with future-only evidence. | Cloud is healthy at `2,058/8,192` clusters (`25.1%`) of look one. The retired 300-billion packet remains quarantined and no outcome has been opened. | Finish look one; stop PASS or continue mechanically under the reviewed rule, then obtain score-free and terminal reviews. |
| **S6 shuai-pai sourcing** | Make legal throws visible, then spend extra search only where an actor-visible shape has demonstrated value. | Claude's 12:10 review passed PR #50's exact Air packet `19f3b2a3…79dd0`. The proposed 7,168-cluster screen expects about 145 triggered clusters and has 98.7% planning power at the reused-state fitting effect; only a fresh screen can validate that transfer. Draft PR #65's Mini alternative was closed because safe cross-host supersession cost more complexity than the four-cluster timing test justified. At 17:19 Codex reverified the exact Air packet/runtime and installed detached `screen` queue `s6-preflight-queue-v2`; it sleeps without compute and can launch only after pair-aware publishes a complete score-free final, all pair workers exit and every S6 one-shot target remains unused. | Let the fail-closed queue run the reviewed Air preflight; then obtain capacity review and freeze/review any fresh whole-game execution. |
| **Pair-aware rollouts** | Let simulations remember exhausted higher pairs so promoted low pairs are valued correctly. | Air is healthy with all eight shards at `160/896`; S6's reviewed queue is sleeping behind it. Claude PASSed PR #69 `ca1913f` at 22:36 after reproducing the v1 parent and attacker-only cap actions; a three-arm capacity design is now being built. | Finish the broad screen. Independently falsify/review the attacker-gated capacity design before deciding whether its economics justify another large run. |
| **Pair ballot retention** | Separate a rare ballot omission from broader pair valuation. | PR #61 artifact/evaluator PASSed at 21:53. Draft PR #72 at exact head `373de84` combines 1,023 defender rows over 990 deal clusters, excludes the lone attacker and binds exact membership/weights. Its `math.fsum` repair now produces byte-identical design bytes on Python 3.11/3.12/3.14. | Obtain external design review. Measure champion-natural dose before any whole-round claim; no preflight/run is authorized. |
| **S5 defensive point protection** | Determine whether the literal final champion still donates a point card after its report fold—not merely whether the rollout heuristic first prefers one. | Claude PASSed PR #70 at 21:45, authorizing one ten-witness × 32-seed final-champion diagnostic. It has not run and grants no treatment or strength authority. | Run once on the first noncontended qualified host. Build a narrow late-seat treatment only if the champion's final played action still donates points. |
| **Performance lane** | Make search cheaper without changing play or disturbing sealed jobs. | Cython `_current_winner` was slower and is dropped. Draft PR #71 `414fe29` makes heuristic leads cheaper and validates frozen H0 heuristic/actions/bury during construction/reopen. Independent audit reproduced a 16.22% lower median ARM round time and exact transcript SHA; x86 averaged 11.75% lower. Ninety-one strict audit tests and CI pass. | Claude independently reviews semantics, the H0 boundary and benchmark method. Merge only if that passes; this remains throughput, not strength evidence. |
| **MC hand-shape bury exploration** | Search the strategic hand left after burying—not just low cards or point cards. Candidate generation should expose every feasible suit void and preserve useful pairs, tractors, trump control and shuai-pai shapes. | Claude PASSed PR #51's source boundary, PR #52's deterministic outcome-blind 32+32 DEV journal at 23:06, and PR #54's actor-visible non-recursive all-boss/boss-or-near continuations at 23:09. Candidate zero and equal baseline work remain intact. No census, real rollout or fleet job ran. | Run the source census and one-state/one-world capacity on a qualified free host; use those numbers to choose—not automatically keep—continuation modes for the reusable pass. |
| **Docs and execution clarity** | Spend time on hypotheses and compute rather than reconstructing stale protocol history. | PRs #43/#57 merged the main cleanup. The active handoff now routes Claude to exact blockers and distinguishes request templates from signed column-1 markers. PR #64 reconciles backlog/jobs/daily truth. Transient monitor PR #68 was closed and its branch removed after the Pair-to-S4 handoff, so experiment-specific identities did not become permanent code. | Review/merge PR #64. Compact the live review tail only after preserving every unresolved request and marker needed for T4/pair/S4/S6 closeout. |

## Detailed chronology

<details>
<summary>Show 69 findings, repairs and incident lessons</summary>

These entries preserve what was known at each step. A later numbered entry can
supersede an earlier **Left** statement; the milestone table above is current.

1. **The pair observation exposed a world-model seam, not a root-ballot seam.**
   Production's root bot already uses public Memory and offered Jerry's `D5
   D5` in VXVS. Ordinary Monte Carlo continuations use `HeuristicBot`, which
   forgets exhausted higher pairs. Changing only that continuation was strongly
   positive in bounded exact endgames.
2. **Recurrence is not human-preference evidence.** The live-log trigger was
   common enough to matter, but humans and bots chose the promoted pair at
   nearly the same observed rate (~20%). The causal evidence is the fresh
   treatment/null exact screen; the census only sizes natural traffic.
3. **A good local continuation can still be diluted by conservative root
   search.** The pair rule activated in 17/24 natural searches but changed only
   one root move. That keeps the hypothesis alive while warning that a uniform
   whole-game screen may need substantial sample—or a tighter natural-dose
   estimand—to see it.
4. **S4 needs prospective accumulation, not retroactive pooling.** Its two old
   estimates can motivate a new sample size but cannot be combined after the
   fact as confirmatory evidence. The two-look successor fixes this before any
   fresh outcome exists.
5. **S6's first HOLD was useful and cheap.** Freezing a Mini-compatible packet
   for an Air job would have made the capacity result nonportable. The v2 packet
   now binds the machine, interpreter and branch-built compiled engine before
   any work begins.
6. **Two-tier rigor is now explicit.** The pair exact test was fast,
   reproducible exploration. Whole-game strength still requires fresh paired
   populations, adequate power, external review and literal live-champion
   comparison.
7. **S6 is a broad ballot expansion, not a rare exception.** On disjoint DEV
   seeds it added a move on 93.4% of natural leads. That meets the coverage
   request but may nearly double lead-search work; only the reviewed preflight
   can determine whether the full experiment is computationally sensible.
8. **S6's broadness is mostly generic whole-holding evacuation.** In bounded
   exact endgames those actions never beat the best existing live-ballot move
   in 128 balanced states. Boss/near bundles at least broke even directionally.
   The actionable split is “always source” versus “selectively spend search,”
   not removing shuai-pai from the ballot altogether.
9. **Pair-cap opportunities were small in count but stable at high N.** The
   repaired live diagnostic found eight such public-proof states, and all eight
   favored the low pair at every 300/1,000/3,000-world checkpoint. That is a
   strong mechanism clue, but shared rooms and multiple states per round make
   it unsuitable as a confidence interval or whole-game strength claim.
10. **Ballot visibility and search spend do not have to be the same thing.**
    S6 can keep all shuai-pai candidates inspectable while reserving a second
    expensive search for the 12.6% of broad triggers in the only stratum with
    positive exact evidence. That converts the broadness problem into a
    testable policy gate instead of deleting requested action coverage.
11. **A complete round does not always contain 100 play actions.** Singles do,
    but pairs, tractors and throws consume multiple cards at once. Catching
    this before packet review prevented a valid preflight from falsely
    refusing after hours of work; future whole-round validators must check
    complete four-seat trick groups rather than assume 25 single-card tricks.
12. **The broader pair-cap proof adds dose, but not monotonically.** Across 192
    natural roots it created two choices that v1 did not, but also undid one v1
    choice back to the champion. That is exactly why the next evidence must be
    complete-round treatment/null utility rather than counting activations as
    wins.
13. **Action count is not round completeness.** The first pair repair correctly
    allowed multi-card tricks but still accepted an impossible 84-card fixture.
    The durable contract is physical: 100 played cards, exactly 25 per seat,
    equal-width cyclic tricks and no card above two-deck multiplicity. When the
    flaw surfaced after admission, the run stopped at 1/4 and published no
    result; the consumed admission and progress line were preserved.
14. **Boss/near is cheap but not yet useful.** Its 87.37% search reduction did
    not retain the hoped-for DEV utility: 11 overrides tied and one lost two
    levels. This argues for diagnosing trigger quality and the losing witness,
    not spending immediately on a large confirmation or deleting broad S6.
15. **Exploration should preserve partial learning.** The S6 pilot retained
    completed rows only in memory until terminal publication. Future reusable
    DEV drivers should journal completed clusters, while confirmatory verdicts
    may still remain all-or-nothing and fail closed.
16. **The narrow S6 risk is failed-throw tail behavior, not every throw.** The
    only utility-changing override attempted `S4 S4 S6 S6`; the true opponents
    beat the bundle, so the engine forced only `S4 S4`, and attackers crossed
    from 75 to 100 points. Publicly proven all-boss gating removes that witness,
    but its ten observed overrides were all neutral. It is a safety refinement,
    not yet a strength hypothesis worth a large run.
17. **Pair-aware v1 changes often survive fresh high-N repricing, but not all
    metrics agree.** On all nine selected changes, level utility favored v1 on
    5–6 roots under two continuation models, opposed it on 1–2 and left two
    unresolved. One move gained roughly two points while losing roughly 0.13
    levels. Future screens must gate on level utility; points stay diagnostic.
18. **Raw pair v2 should protect v1 rather than replace it.** Its three
    differences from v1 contained two useful additions and one reversion that
    undid a roughly three-point v1 advantage. This is a composition problem,
    not evidence that broader pair tracking is uniformly better.
19. **S6's mismatch is not fixed by the existing level objective.** On the
    exact same report worlds, level-bracket scoring rejected seven neutral
    throws but still accepted the only two-level loss. It retained no wins.
    That closes a cheap full-pilot successor and points back to evaluator/search
    quality rather than another threshold on the same selected actions.
20. **The pair screen is now powered for the effect that motivated it.** A
    2,048-cluster run would be cheap but weak. The fixed 7,168 design is the
    largest one under both reviewed caps and has roughly 84% planning power at
    `+0.05`; its 80% minimum detectable effect is `0.04699` levels per round.
21. **Freeze authority and run authority remain separate.** The capacity PASS
    was enough to produce a byte-pinned 7,168-cluster proposal, not to spend
    it. Because the review prose also called 2,048 the bound, the next reviewer
    must explicitly accept or reject the exact powered size before gameplay.
22. **S6's value is concentrated in a sharper late-game shape.** The first
    post-hoc four-card data suggested full-hand boss/near throws, not partial
    bundles, carried the wins. A disjoint 128-state replication confirmed real
    exact action-set value: `+0.234` levels with LCB `+0.100`. The next problem
    is no longer sourcing; it is building an actor-information evaluator that
    can recognize those wins without seeing opponents' hands.
23. **The live search can recognize the full-hand opportunity, at least on the
    fitting population.** Four independent report-LCB streams per public state
    produced a `+0.307` exact level gain over the incumbent with LCB `+0.175`.
    The selector was not perfect—20 overrides were harmful—but beneficial
    overrides outnumbered them five to one and both roles stayed positive.
    Because these are the same opened states that selected the hypothesis,
    this clears fresh whole-game *design*, not strength.
24. **The selective S6 dose is neither vanishing nor broad.** It appeared in
    2.02% of 50,000 natural rounds and only in mid/late play. That is sparse
    enough to avoid the broad source's near-double-search cost, but frequent
    enough for a powered whole-game experiment if the capacity preflight
    confirms runtime and trigger counts.
25. **Bury strength is a post-bury hand-shape problem.** Point-bearing buries
    are a useful diagnostic stratum, but a strong ballot should explicitly
    include every feasible suit void and diverse candidates that preserve
    pairs, tractors, trump control and legal shuai-pai. MC must then evaluate
    those shapes with a continuation ballot capable of playing the preserved
    throw; otherwise it systematically undervalues the reason for the bury.
26. **A negative process filter is not fleet evidence.** A probe searched for
    `s6_full_hand` rather than the actual `s6_throw_full_hand...` command and
    falsely reported a healthy Air census idle. The original eight workers had
    remained detached and their logs were advancing. Eight unnecessary
    duplicate workers ran for roughly one minute before exact PID/namespace
    reconciliation stopped only them. No scored data or result was affected;
    [INC-12](../incidents/INC-20260812-12-fleet-monitor-false-negative.md)
    records the commands, impact and new absence-proof rule.
27. **Planning rates must name the estimator unit.** S6's source census counts
    one all-champion four-seat round, while its strength estimator sums two
    mirrored rounds per seed. The repaired packet now shows that treatment
    occupies all four seats once across those flips, caps multiple triggers at
    one affected cluster for planning, assumes no post-trigger trajectory
    equivalence, and makes measured run telemetry authoritative. This turns a
    plausible multiplication into a reviewable approximation.
28. **Fleet identity authority does not grant evidence-content authority.** A
    broad process inventory may read commands, working directories, file names,
    sizes and mtimes. The repaired monitor now tails only two exact reviewed
    score-free supervisor streams; every other recent log is metadata-only.
    This closes the sealed-content overreach found during the INC-12 repair.
29. **Joint bury exploration needs more than candidate zero as a reference.**
    The live ballot's first proposal is not the production bot's final searched
    lead. PR #51 therefore reports three explicitly descriptive optima: the
    existing lead menu under the incumbent bury, the pair/S6-widened menu under
    that same bury, and the full bury-plus-lead menu. This separates menu value
    from hand-shape value without pretending either raw winner is deployment
    evidence.
30. **“Include shuai-pai” must name when it can be played.** The first combo
    draft prices a throw attempted immediately after the bury; it cannot value
    one preserved for a later lead. PR #54 makes that difference measurable
    with separate baseline, public all-boss and boss-or-near continuation
    modes. They never call MC recursively, and each reusable journal namespace
    binds its mode and observed trigger dose, so later-throw value cannot be
    silently attributed to bury sourcing.
31. **Host pinning is also a scheduling constraint—but host mobility has a
    complexity budget.** Air's pair-aware rate projects roughly four days, so
    a Mini S6 fallback looked attractive. The existing Air controller cannot
    observe a documentary Mini supersession, however, and making it refuse
    would add roughly 500 lines of one-off cross-host retirement machinery.
    For a four-cluster, outcome-free timing test that is the wrong tradeoff:
    keep Air's reviewed packet singular and put Mini on other useful work.
32. **Exploration should fail partially, not forget completely.** The first
    bury scorer preserved underfilled worlds inside one state but had no
    durable multi-state driver. PR #52 now writes each completed DEV state as
    an immutable record under one exact manifest. This is deliberately less
    strict than confirmatory evidence—it preserves learning and supports
    resume—but it still prevents mixing changed code, runtime or work settings.
33. **Ballot availability and rollout valuation are separate pair problems.**
    Wide lead generation can create every legal pair, but the 14-action cap can
    still evict a pair before search sees it. The first 1,000-round census found
    only 14 affected early leads out of 4,000 and none in mid/late play. That
    makes guaranteed pair retention a plausible small source repair, while the
    late VXVS weakness still belongs to the active pair-aware valuation lane.
34. **The new x86 worker is a useful strength host, not interchangeable
    authority.** Reference/native legality and scoring matched, full-round
    histories agreed on fixed seeds, and 16 workers sustained about 8.9
    SmartBot rounds/second—roughly twice eight-worker throughput. Every sealed
    experiment still needs a controller and artifact chain pinned to this host,
    Python and native binary; an Air or Mini marker cannot be borrowed.
35. **Monitoring a new host does not authorize reading new evidence streams.**
    The fleet probe may report Cloud process identity, CPU, tmux, file size and
    mtime. Until the temporary census producer and schema are reviewed, its log
    and terminal JSON remain content-closed even though the intended job is
    score-free.
36. **Rare ballot gaps need affected-state evaluation before whole-game
    dilution.** Pair omission occurs on `0.0816%` of SmartBot-trajectory leads;
    that is source prevalence, not champion-natural dose. The useful next asset
    therefore oversamples those exact public states and reports separately
    whether a pair has value and whether the current selector can capture it.
37. **Attacker role is a plausible safety gate for broader pair tracking.** In
    the complete 192-root replay, the two favorable broad-v2 changes had
    attacker dose and the harmful reversion was defender-only. The bounded
    attacker gate retained both favorable roots and blocked the harmful one.
    That is a clean action-semantics result, not yet a utility claim.
38. **Profile reuse is safer than protocol cloning when the science is
    unchanged.** S4 C2 reuses the reviewed C1 game loop, exact-work checks and
    automatic transition through isolated modules, while byte-pinning both
    reused sources. This removes roughly 2,500 duplicated lines and a large
    future drift surface without mutating C1 in-process.
39. **Capacity provenance should show both old and derived status.** The spent
    S4 artifact remains honestly `HOLD` under its eight-shard envelope. C2 may
    derive packet-review readiness only after authenticating that exact result
    and admission and applying its pre-reviewed 16-shard envelope; it must not
    relabel or rerun the old evidence.
40. **A fresh native build is not the reviewed native binary.** Cloud's first
    unmocked C2 check refused a locally rebuilt extension because its digest
    differed. Installing the separately reviewed x86 binary restored exact
    policy, ballot and full-round identities. Host compatibility includes the
    byte-pinned build, not merely architecture and passing unit tests.
41. **A derived population is only as trustworthy as its raw-shard replay.** A
    self-hash proves that a merged file is internally consistent; it does not
    prove its claimed natural frequency or receipt lineage. The pair pipeline
    now reopens every source shard and rebuilds both selection and dose before
    any downstream evaluation can use the asset.
42. **A controller PASS does not prove its child can start.** S4's packet and
    supervisor recomputed exactly, yet every child rejected a stale profile
    path before gameplay. Large-run gates now need a real exact-host child
    receipt smoke and a post-launch worker-survival check, not only packet
    verification.
43. **Fail closed must also be transactional.** A correctly refused freeze can
    still strand a one-shot namespace if it copies authority before validating
    exact runtime flags. Recovery-v2 now performs all native/runtime/evidence
    checks before its first write, and a regression proves a refusal leaves the
    namespace absent.
44. **Session-only review cron is not launch-critical scheduling.** The S4
    packet request was ready at 15:00, but job `c591827b` produced no scheduled
    15:11 event; Claude's local timeline/state remained at 14:54 and the task
    declares itself non-durable. Cloud stayed reserved and idle with no
    evidence impact. [INC-14](../incidents/INC-20260812-14-claude-session-cron-missed.md)
    records the durable-scheduler and heartbeat follow-ups.
45. **A monkeypatch is not a reviewer safety boundary.** Claude tried to stop
    the real S4 launch before worker creation by replacing a wrapper-module
    global; the controller resolved a different global and started all 16
    workers. No outcome completed, but the immutable packet's history became
    false, so the full 300-billion interval was retired. Reviewers must use a
    validation-only command that cannot spawn gameplay, never a patched
    `launch()`. [INC-15](../incidents/INC-20260812-15-reviewer-witness-launched-gameplay.md)
    records the incident and durable boundary.
46. **Reusable exploration still needs internally reconstructable results.**
    The first pair affected-state aggregate accepted self-rehashed embedded
    scores and selector flags. PR #61 now records the champion's report-fold
    evidence and reconstructs its played reason, exact work, attacker/defender
    utility conversion, paired estimands, best inserted pair and selector
    telemetry. In plain English: the diagnostic can now tell whether adding a
    pair helped because the recorded simulations say so, rather than trusting
    a summary field. A final pre-run audit also caught that the evaluator had
    inherited the capture's false `score_free` claim; head `443b62d` now
    declares scored, diagnostic-only work truthfully. Fifty-five focused pair
    tests pass. **Left:** freeze and independently review the running score-free
    population, then review this exact evaluator head before any scored run.
47. **Affected-state weights are descriptive, not a natural-strength
    estimate.** The capture counts every search-reachable omission event to
    estimate the early/mid/late band mix, but retains only the first affected
    state per deal and band. That hybrid is useful for routing exploration; it
    is neither an exact natural-decision estimate nor whole-game dose. In plain
    English: the asset tells us whether the rare mistake can be costly when it
    happens. A small production-semantics denominator census or direct
    whole-game screen must tell us whether fixing it moves overall strength.
    **Left:** add that cheap dose check only if affected-state utility is
    positive—do not repeat the 12-million-deal capture just for a denominator.
48. **Quarantining a population does not require cloning the whole runtime.**
    The fresh S4 controller keeps the reviewed C1 game loop and C2 science as
    isolated imports, changes only the 360-billion schedule/namespace, and
    reopens the complete retired interval at the child boundary. This preserves
    the honest restart without adding another large independent supervisor.
    The exact controller and packet have now passed external review. A durable
    queue waits without CPU use behind Pair V3 and checks all capture shards,
    zero workers and every S4 hash before admission. **Left:** finish Pair V3;
    then the queue consumes the one admission and launches, or exits HOLD.
49. **Defensive point loss is real but narrow, and mostly not a sourcing gap.**
    The one score-free S5 census replayed 4,363 bot follows across 122 complete
    human-game rounds and found 58 strict hindsight DEV cases where an opponent
    owned the trick, the bot could not win, its play carried points and a
    strictly lower-point legal follow existed. Today's candidate-zero/rollout
    surface reproduces 16; all 16 are the exact intersection where candidate
    zero and the rollout heuristic match the historical point-bearing action.
    Ten occur in third/fourth seat, after the partner has already acted. Forty-
    two rollout choices already use fewer points, and 57/58 ballots contain a
    lower-point action. In plain English: the old global “bots feed double”
    story remains withdrawn, but a small continuation-choice weakness may
    survive. The census intentionally ran zero full champion searches, so it
    has not yet shown that report-LCB's final played move still makes the same
    mistake; search may already repair its bad first instinct.
    Artifact `efc82b8…bac3` reverified exactly and publishes zero worlds,
    rollouts, scores, raw cards or identities. **Left:** external artifact
    review, then a cheap private-safe same-state/multi-seed replay of the final
    champion decision on the ten late triggers. Implement the actor-visible
    treatment only if current search still donates points, retaining a matched
    null and fresh trigger-matched screen before any whole-game claim.
50. **Process substrings can be false positives as well as false negatives.**
    The first Pair-to-S4 queue would have treated the persistent tmux server as
    a surviving capture worker because tmux retained the original capture argv.
    A pre-transition audit saw the broad filter over-count while an executable-
    identity probe found the exact 16 Python workers. Queue v2 was staged and
    swapped without touching capture or S4 evidence; no compute was lost.
    [INC-16](../incidents/INC-20260812-16-s4-queue-process-filter-false-positive.md)
    records the fix. **Left:** after the Pair supervisor exits, verify the same
    predicate reaches zero while tmux remains, then confirm S4 admission and
    worker survival from score-free metadata only.
51. **A failed recipe should not erase a plausible research family.** Direct-Q
    and the O0 oracle-residual implementation selected none, but those results
    did not faithfully test every DouZero- or Suphx-style approach. The active
    backlog now preserves both as a research reserve with explicit re-entry
    conditions: Direct-Q needs changed role/credit assignment and multi-seed
    held-out learning; Suphx needs either a real privileged-policy curriculum
    or bounded per-hand public adaptation. In plain English: remember the ideas
    without rerunning the same failures or displacing better-evidenced work.
    **Left:** revisit only after a new small-domain learning contract exists;
    all 32 current worker slots already belong to T4, pair-aware evaluation
    and the reviewed S4 successor.
52. **A clean compute handoff can be automatic without weakening evidence.**
    Pair V3 finished all 12 million score-free deals with 16 complete shards,
    zero partials and zero surviving workers. Queue v2 then authenticated the
    exact worker identity, source, packet and review boundary, consumed S4's
    sole admission at 19:31 and launched all 16 fresh 360-billion workers.
    In plain English: Cloud moved directly from building the pair source asset
    to testing point banking without an idle gap or a human racing the process.
    Codex still has not opened any Pair shard or S4 outcome. **Left:** Claude
    reconstructs Pair V3 in reviewer scratch before one formal merge; S4 runs
    its predeclared 8,192-cluster look and automatically continues to 16,384
    only if the reviewed sequential rule requires it.
53. **Review authorization needs author provenance as well as exact bytes.**
    The Codex-authored S5 request accidentally placed its proposed marker at
    column one, so a correct-prefix scan found one apparent PASS even though
    Claude had not reviewed it. The preceding `Codex` heading exposed the false
    authority before code, evidence or compute changed. At that point S5 was
    still waiting for review; item 57 records the later authenticated PASS.
    [INC-17](../incidents/INC-20260812-17-request-template-looked-like-review-pass.md)
    records the near-miss. The durable rule is to authenticate uniqueness,
    independent-author context and exact fields together.
54. **The pair-cap follow-up needs distinct parent and incremental witnesses.**
    The old treatment/null flag turned both reviewed pair-aware v1 and the
    attacker-only extension on or off together. A later audit also showed that
    v3 returns early when v1 fires, so one decision cannot witness both. Item
    63 records the exact two-lead repair and current re-review gate.
55. **Transient monitors should expire with the job they name.** PR #68 safely
    exposed Pair V3 progress while its exact reviewed capture and first S4
    queue identity were live. Both identities are now terminal or superseded,
    so the PR and its remote/local branch were closed instead of merging stale
    special cases into the permanent fleet probe. Its commit remains
    recoverable from GitHub history. **Left:** generalize only reusable
    score-free contracts; do not accumulate one allowlist branch per run.
56. **Pair V3 is now a reproducible asset, but not yet scored evidence.**
    Claude rebuilt all 1,536 rows independently from the sixteen 12M-deal
    shards, and Codex's single formal merge is byte-identical at
    `6a3f8d9d…339ae`. In plain English: the rare pair-omission population is
    trustworthy without anyone selecting on its eventual utility. At 21:53
    the formal artifact and PR #61 evaluator also PASSed: population content
    may now be opened for DEV/CALIB capacity design only. The later capacity
    draft is on internal hold as recorded in item 61; scored execution and
    REPORT remain closed.
57. **S5 crossed its cheap design gate, not a strength gate.** Claude PASSed
    PR #70 at 21:45, authorizing one 320-decision replay of the literal final
    report-LCB action on ten late partner-already-acted witnesses. In plain
    English: this asks whether search already repairs the rollout's bad first
    instinct before we build another policy. **Left:** add the requested direct
    fixtures before later producer reuse, run once on the first noncontended
    qualified host, and advance only if the final action still donates points.
58. **Large experiments now need an economics argument, not just correct
    code.** Before another host-day, record natural dose, conditional effect,
    implied whole-game gain, minimum detectable effect, maximum fleet-hours and
    the decision unlocked. This routes the pair family instead of launching
    broad pair-aware, attacker-gated pair-cap and rare pair retention as three
    independent large bets. It also keeps S4/S5/`ANTICIPATE_FEED` separately
    attributable before any continuation portfolio. **Left:** apply this table
    at every future capacity-packet review.
59. **Performance work now has a physically separate lane.** A fourth worker,
    16-vCPU / 30-GiB x86, is reachable only through local alias
    `shengji-perf`; its address is not repository data. It starts with no
    strength authority. The permanent fleet probe now reports only host
    capacity, Python process identity/cwd/git and tmux on that worker—never
    evidence content. **Left:** pin the runtime, obtain a fresh post-3.62x
    profile, and prove exact histories/actions/RNG/report-LCB parity before
    claiming throughput.
60. **An apparently spare core is not permission to benchmark beside sealed
    evidence.** A low-priority single-core pytest/performance comparison was
    found on Mini at about 21:50 while T4 owned eight workers. It was stopped;
    every T4 worker remained healthy and there is no evidence-integrity claim.
    In plain English: use the new performance host instead of making isolation
    depend on guessed spare capacity. **Left:** keep full suites and timing runs
    off Mini/Air/strength Cloud until their scored supervisors seal.
61. **Pair-ballot capacity needs a defender-cluster estimator and champion
    dose.** The 1,023 defender rows span 990 deal clusters; one attacker row
    must not enter the primary. Draft `f5ee600` also lacks an explicit combined
    DEV+CALIB estimator, while `0.0816%` is SmartBot-trajectory prevalence, not
    champion-natural dose. **Left:** repair internally; no review or run.
62. **The compiled winner shortcut is slower in production context.** Correct
    ARM and x86 baselines put Cython `_current_winner` about 4% behind, and the
    earlier roughly 6x leaf result was invalid. The combined timing was
    confounded. **Left:** rebuild only the tractor prefilter and import cleanup,
    preserving the frozen H0 SHA and fixing static historical tests.
63. **PR #69 needs two leads because its branches are mutually exclusive.**
    v3 returns early when v1 fires, so one decision cannot witness both. Repair
    `ca1913f` centralizes the parent return and tests v1-only `DQ DQ` versus
    champion `SA`, then cap-only, on one policy. All 23 tests pass; replacing
    the v1 return with champion fails two. **Left:** bounded external re-review.

64. **The pair-cap action seam is now externally closed.** Claude reproduced
    both mutually exclusive decisions and every mutation at 22:36, confirming
    that the matched parent really is v1 and the incremental rule is attacker-
    only. This authorizes a three-arm capacity design—not gameplay. **Why:** the
    literal champion arm is needed to distinguish “better than parent” from
    “actually stronger than production.” **Left:** finish/falsify the capacity
    design and let the broad Pair result route whether the spend is worthwhile.
65. **Pair-ballot conditional learning is now correctly defender-only.** Clean
    head `ea8ea9d` combines all 1,023 defender rows across 990 deal clusters,
    excludes the lone attacker, and pins exact population/weight identity so a
    coordinated role, split or REPORT substitution cannot enter the result.
    The real reviewed bytes reconstruct exactly and 82 focused/parent tests
    pass. **Why:** learn whether the rare missing-pair source is useful without
    pretending a SmartBot census measures production dose. **Left:** finish the
    independent re-audit, then seek design review; measure champion dose before
    any whole-round claim.
66. **The first performance win survived semantics checks, and its audit paid
    for a correctness repair too.** The pair-count prefilter reduced 20 matched
    x86 whole-round times by 11.75%; independent ARM trials had a 1.198x median
    speedup, with exact histories/outcomes. Adversarial review found a
    pre-existing H0 controller path that could bless today's heuristic while
    naming the frozen historical one. Head `414fe29` now validates the frozen
    heuristic, action universe and bury source on controller construction and
    every reopen; 50 focused and 134 broader strict tests pass. **Why:** cheaper
    rollouts increase experiment throughput only if old evidence cannot be
    silently reinterpreted. **Left:** one final independent falsification, then
    publish for external review.
67. **That final performance falsification passed and PR #71 is now public.**
    A second audit reproduced a 16.22% lower median ARM whole-round time over
    alternating 2,000-round trials, with byte-identical complete transcript
    SHA; all independent H0 source drifts and guard-removal mutations refused.
    CI passes. **Why:** this is the rare enabler that gives every future CPU
    search more experiments per day without changing the bot. **Left:** Claude
    reproduces semantics and timing before any merge; no deployment follows.
68. **The bury-shape preparation stack is semantically closed.** Claude PASSed
    PR #52 at 23:06 and PR #54 at 23:09. The reusable journal is deterministic,
    DEV-only and outcome-blind; the continuation arms are actor-visible,
    non-recursive, candidate-zero preserving and equal in baseline work. Three
    PR #52 fixture requests are regression hardening, not blockers. **Why:** we
    can now spend the next cheap compute on whether hand shape and later throws
    matter rather than more protocol design. **Left:** source census and one-
    state/one-world timing on a qualified free host.
69. **Exact hashes require deterministic numerical reductions too.** The final
    Pair-ballot capacity audit found Python 3.11 and 3.14 differed only in four
    last-bit planning fields because built-in `sum()` changed algorithms. Head
    `373de84` uses `math.fsum` and exact-value regressions; independent local
    builds on Python 3.11.14, 3.12.12 and 3.14.3 now all produce internal SHA
    `cd8ada0d…4f9d` and file SHA `be21b547…f439`. **Why:** a scientifically
    correct design still fails operationally if a supported runtime cannot
    reproduce its bytes. Independent falsification passed and draft PR #72 is
    published at exact head `373de84`. **Left:** external design review; even a
    PASS authorizes only capacity-preflight implementation, not execution.

</details>

## Fleet snapshot — 23:25 EDT

Mini T4 is healthy with zero terminal shards: shards 0–6 are at
`matched-null 100/512`, while shard 7 remains at `treatment 500/512`. Air
pair-aware is healthy at `160/896` on every shard and its S6 queue is sleeping.
Strength Cloud S4 is healthy at `2,058/8,192` clusters (`25.1%`) of look one.
The separate 16-vCPU performance host is idle and has no strength authority.
All 32 strength cores remain occupied and outcomes remain sealed; live
identities belong in `JOBS.md`.
