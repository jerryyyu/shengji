# Daily log — 2026-08-13

This is the plain-English execution log for August 13. Terminal policy results
belong in `AI_POLICIES.md`; durable strategy belongs in `RL_PLAN.md`; active
queue truth belongs in `BACKLOG.md`.

## Current milestone ledger

| lane | why we are doing it | progress so far | what is left |
|---|---|---|---|
| **T4 learned mid/late search** | Test whether the Stage C model can improve the live Monte Carlo champion when used only where its held-out signal was strongest and every proposal is repriced by fresh search. | The sealed whole-game screen remains healthy on Mini at 5,596/12,288 sequential arm-rounds (45.54%); no outcome was opened. Read-only closeout PR #80 repaired its internal HOLD at superseding head `e0c23e6`; 13 focused tests and CI pass, external review is pending, and it cannot touch the run. | Natural completion; external review of #80; score-free supervisor review; one authorized aggregate; terminal external verdict. |
| **S4 point banking** | Repair a systematic rollout-world bias: simulated players often win cheaply even when banking a point card is strategically better. Two earlier fresh runs were positive, so a properly powered sequential confirmation can finally resolve the effect. | Sixteen strength-Cloud workers remain healthy. Counter-only progress is 4,279/8,192 clusters (52.23%) at the first look; outcomes remain sealed. | Automatic look-one stop/continue, score-free final review, then terminal aggregation/review. |
| **Pair ballot retention** | Keep a legal pair from being crowded out before search can evaluate it, especially in the defender-heavy early states where the census found omissions. | Claude PASSed PR #79 `6461c66` at canonical `1cf9521`. The complete reviewed score-free population and all 16 receipts were copied to Performance Cloud, exact design `be21b547…f439` was rebuilt, and packet `e054c5e5…e492` / internal `25b1888c…a14b5` was frozen with execution false. No admission, result or run exists. The parent stack also waits for PR #55 exact-head merge readiness. | Obtain the packet-specific review requested at canonical `023850d`; only a PASS permits one score-free capacity preflight. Separately review #55 before merging #55→#60→#61→#72. Champion-natural dose and scored testing remain later gates. |
| **Pair-aware rollout** | Teach simulations that a low pair can become powerful after higher pairs are exhausted, using only actor-visible history. | The powered broad screen remains healthy on Air with eight workers. | Natural completion, score-free final review, then terminal aggregate. The reviewed S6 preflight queue runs afterward only if every guard passes. |
| **S5 defensive point protection** | Determine whether the final champion—not just a heuristic proxy—still throws point cards into already-lost late-seat tricks when a lower-point legal ballot action exists. | The validation-only refusal repair PASSed after the request-template defect spent the one-shot admission. PRs #74/#76 are now closed instead of merging dead execution code; PR #70 retains the reusable diagnostic source. No result or retry exists. | Decide whether the mechanism justifies a fresh recovery design. Any run requires explicit retry authority and a new admission/result namespace; never reuse the spent chain. |
| **Performance** | Lower production latency and make every future strength experiment cheaper without changing actions, RNG, or game outcomes. | On top of prepared-world PR #77, PR #81 `c6c7126` measured 7.73% lower whole-round wall time from native cheapest-winner selection across six fresh pairs, then another 4.01% incrementally by counting lead pairs once per hand across three fresh pairs. A later native-lead prototype exposed a malformed-seat segfault at `0bd073c`; local repair `8e698e` passes 30 ARM and 30 x86 boundary/parity tests, and a repaired-head exact-final A/B is in flight. | Review #81 plus #75/#77. Finish and validate the repaired native-lead A/B before stating its return. Nothing deploys or enters frozen experiments before review. |
| **Bury shape + later shuai-pai** | Search the strategic hand left after burying—voids, pairs, tractors and later legal throws—rather than merely ranking eight individual cards. | Source, reusable DEV journal and actor-visible continuation semantics all passed review. PR #78 `f4b80f0` is execution-ready after review for a lightweight opened-DEV diagnostic: census all 512 states, retain the exact 32+32 slice, then compare baseline/`all_boss`/boss-or-near timing on one accepted common world for the widest state. It emits no score or strength claim, and no run exists. | Review #78, run the telemetry-only capacity measurement on the performance host when free, then choose an affordable reusable exploration pass from measured work/dose rather than guessing. |
| **Human H0 geometry repair** | Keep learning from human moves without turning partial exploratory coverage into a deployment claim. | PR #82 `82f19fd` reproduces the two failure shapes: production exposed 12 legal choices while the generic analyzer exposed only three. It replaces the false count invariant with direct engine legality and is score-free; it cannot read old utilities, train, launch or promote. | External adversarial review, then separately authorize any new opened-DEV geometry prevalidation. Any scored H0 successor needs a fresh population and design. |

## Overnight engineering lessons

- **Historical evidence and current code are different identities.** A
  behavior-preserving optimization legitimately changes source-derived ballot
  hashes. The right repair is to keep old confirmation receipts immutable and
  add a reviewed compatibility statement for current code—not silently rewrite
  history or weaken drift checks.
- **Architecture portability must be explicit.** The S5 diagnostic was correct
  but bound to an ARM native binary. The x86 wrapper preserves the historical
  parent, pins the new native/ballot contract, and replays the same score-free
  decisions before it can consume the original one-shot authority.
- **Profile hypotheses still need end-to-end timing.** Replacing `sorted` with
  `min` looked attractive in a profile but produced no whole-round gain and was
  dropped. Preparing a sampled world once targets measured duplicate work and
  produced a small reproducible exact-head gain while preserving transcripts.
  A larger cache result stayed unpublished when adversarial tests found stale
  values after legal public-object mutation and changed trump ordering; the
  safe repair was slower, so it was discarded rather than rationalized.
- **The fleet stays hypothesis-driven.** Mini, Air and strength Cloud remain on
  T4, Pair and S4. The dedicated x86 host performs bounded engineering only
  while S5 recovery authority remains closed.
- **Tests must not borrow scored-host cores.** A helper unintentionally started
  two pytest processes beside T4 on Mini. They were identified and stopped
  after roughly two CPU-core-minutes; no evidence was written, no outcome was
  opened and T4 stayed healthy. Broad tests now stay off live scored hosts.
- **A documented marker convention is not an executable gate.** PR #74
  repeated INC-17 because the wrapper could not distinguish an implementer's
  byte-perfect request from a reviewer attestation. The old admission is spent;
  recovery requires a distinct attestation prefix, pinned review provenance,
  one canonical admission path and explicit retry authority.
- **Close dead execution code, retain reusable science.** After the S5 refusal
  repair PASSed, PRs #74/#76 were closed while PR #70's diagnostic source was
  retained. The Pair stack remains open until PR #55's exact current head gets
  an explicit merge-readiness review.
