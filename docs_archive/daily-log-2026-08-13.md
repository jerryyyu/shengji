# Daily log — 2026-08-13

This is the plain-English execution log for August 13. Terminal policy results
belong in `AI_POLICIES.md`; durable strategy belongs in `RL_PLAN.md`; active
queue truth belongs in `BACKLOG.md`.

## Current milestone ledger

| lane | why we are doing it | progress so far | what is left |
|---|---|---|---|
| **T4 learned mid/late search** | Test whether the Stage C model can improve the live Monte Carlo champion when used only where its held-out signal was strongest and every proposal is repriced by fresh search. | The sealed whole-game screen remains healthy on Mini at the safe counter-only lower bound of 6,396/12,288 sequential arm-rounds (52.05%); treatment is complete, seven matched-null shards report 300/512 and one reports 200/512. No outcome was opened. The slow null tail projects near 16:59 EDT, leaving the champion arm a knife-edge 26.7-second average before the 20:46 cutoff. Claude exact-head PASSed PR #80 `e61975c`; 22/22 tests pass plain and fast, and the read-only helper is merged with no execution authority. | Natural completion or a concrete terminal HOLD; then score-free supervisor review, one authorized aggregate and terminal external verdict. |
| **S4 point banking** | Repair a systematic rollout-world bias: simulated players often win cheaply even when banking a point card is strategically better. Two earlier fresh runs were positive, so a properly powered sequential confirmation can finally resolve the effect. | All 16 strength-Cloud workers remain healthy. Reviewed score-free progress is 6,187/8,192 clusters (75.52%) at the first look; outcomes remain sealed. | Automatic look-one stop/continue, score-free final review, then terminal aggregation/review. |
| **Pair ballot retention** | Keep a legal pair from being crowded out before search can evaluate it, especially in the defender-heavy early states where the census found omissions. | Claude PASSed design-only PR #86 exact head `a43a17c` at canonical `fe6eb3b`, opening only a future controller-design review; the exact #55→#60→#61→#72 source stack is merge-ready in order. | Add the requested ancestry fixture during controller design. Champion-natural dose remains a later gate; packet implementation/freeze/run, evidence access, scoring, REPORT, aggregation, retry, strength, training, promotion and deployment are closed. |
| **Pair-aware rollout** | Teach simulations that a low pair can become powerful after higher pairs are exhausted, using only actor-visible history. | All eight workers remain alive on Air; reviewed score-free progress is 2,064/7,168 clusters (28.79%), outcomes remain sealed and current pace remains on a substantive timeout trajectory. Contingency PR #85 exact head `111314f` PASSed external design-only review and grants no capacity screen, replacement launch, retry or extension authority. | Monitor without contention. On natural completion or terminal timeout/HOLD, follow score-free review and let the reviewed S6 preflight queue proceed afterward only if every guard passes. |
| **S5 defensive point protection** | Determine whether the final champion—not just a heuristic proxy—still throws point cards into already-lost late-seat tricks when a lower-point legal ballot action exists. | The validation-only refusal repair PASSed after the request-template defect spent the one-shot admission. PRs #74/#76 are now closed instead of merging dead execution code; PR #70 retains the reusable diagnostic source. No result or retry exists. | Decide whether the mechanism justifies a fresh recovery design. Any run requires explicit retry authority and a new admission/result namespace; never reuse the spent chain. |
| **Performance** | Lower production latency and make every future strength experiment cheaper without changing actions, RNG, or game outcomes. | PR #77 `0381081` measured 2.6234% lower wall time with a 1.5066% lower bound, missing its 3% retain gate. On that rejected parent, PR #81 `c6c7126` retained two candidates: native cheapest-winner at 7.7306% lower wall (lower bound 6.2489%) and one-Counter lead composition at 4.0124% incremental (lower bound 3.2857%), with exact normalized semantics. They must be split, rebased onto PR #71/#75 and remeasured without adding cross-baseline gains. PR #83 `69ff44e` measured 10.2014% lower wall time over three exploratory pairs; its raw artifacts are owner-writable and lack an immutable manifest. Performance Cloud is idle. | Retire #77 as-is; rebase and remeasure #81's retained candidates; review #83 as exploratory implementation evidence only. Nothing deploys or enters frozen experiments before review. |
| **Bury shape + later shuai-pai** | Search the strategic hand left after burying—voids, pairs, tractors and later legal throws—rather than merely ranking eight individual cards. | PR #78 exact head `8ab5db2` PASSed external review with its no-strength authority pinned. It censuses all 512 opened-DEV states, retains the exact 32+32 slice, then times three arms on one accepted common world for the widest state. It emits no score or strength claim, and no run exists. | Separately authorize the telemetry-only capacity measurement when the performance host is free, then choose an affordable exploration pass from measured work/dose. |
| **Human H0 geometry repair** | Keep learning from human moves without turning partial exploratory coverage into a deployment claim. | PR #82 exact head `a498bf5` PASSed external review and reproduces the two failure shapes: production exposed 12 legal choices while the generic analyzer exposed only three. It has no score or launch surface; the reviewer requested a fixture pinning its declarative authority map because flipping `scoring_authorized` currently leaves tests green. | Add the authority-map fixture, then separately authorize any opened-DEV geometry prevalidation. Any scored H0 successor needs a fresh population and design. |

## Overnight engineering lessons

- **Historical evidence and current code are different identities.** A
  behavior-preserving optimization legitimately changes source-derived ballot
  hashes. The right repair is to keep old confirmation receipts immutable and
  add a reviewed compatibility statement for current code—not silently rewrite
  history or weaken drift checks.
- **Architecture portability must be explicit.** The S5 diagnostic was correct
  but bound to an ARM native binary. The x86 wrapper preserves the historical
  parent, pins the new native/ballot contract, and replays the same score-free
  decisions before it can consume the original one-shot authority.
- **Profile hypotheses still need end-to-end timing.** Replacing `sorted` with
  `min` looked attractive in a profile but produced no whole-round gain and was
  dropped. Preparing a sampled world once targets measured duplicate work and
  produced a small reproducible exact-head gain while preserving transcripts.
  A larger cache result stayed unpublished when adversarial tests found stale
  values after legal public-object mutation and changed trump ordering; the
  safe repair was slower, so it was discarded rather than rationalized.
- **The fleet stays hypothesis-driven.** Mini, Air and strength Cloud remain on
  T4, Pair and S4. The dedicated x86 host performs bounded engineering only
  while S5 recovery authority remains closed.
- **Tests must not borrow scored-host cores.** A helper unintentionally started
  two pytest processes beside T4 on Mini. They were identified and stopped
  after roughly two CPU-core-minutes; no evidence was written, no outcome was
  opened and T4 stayed healthy. Broad tests now stay off live scored hosts.
- **A documented marker convention is not an executable gate.** PR #74
  repeated INC-17 because the wrapper could not distinguish an implementer's
  byte-perfect request from a reviewer attestation. The old admission is spent;
  recovery requires a distinct attestation prefix, pinned review provenance,
  one canonical admission path and explicit retry authority.
- **Close dead execution code, retain reusable science.** After the S5 refusal
  repair PASSed, PRs #74/#76 were closed while PR #70's diagnostic source was
  retained. PR #55's exact preservation delta then PASSed merge-readiness; the
  reviewed #55→#60→#61→#72 stack must merge in that order.
