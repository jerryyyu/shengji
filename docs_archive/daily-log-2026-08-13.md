# Daily log — 2026-08-13

This is the plain-English execution log for August 13. Terminal policy results
belong in `AI_POLICIES.md`; durable strategy belongs in `RL_PLAN.md`; active
queue truth belongs in `BACKLOG.md`.

## Current milestone ledger

| lane | why we are doing it | progress so far | what is left |
|---|---|---|---|
| **T4 learned mid/late search** | Test whether the Stage C model can improve the live Monte Carlo champion when used only where its held-out signal was strongest and every proposal is repriced by fresh search. | The sealed whole-game screen remains healthy on Mini. At midnight, reviewed counter-only progress was 4,896/12,288 sequential arm-rounds (39.8%); no outcome was opened. | Natural completion, score-free supervisor review, one authorized aggregate, and terminal external verdict. |
| **S4 point banking** | Repair a systematic rollout-world bias: simulated players often win cheaply even when banking a point card is strategically better. Two earlier fresh runs were positive, so a properly powered sequential confirmation can finally resolve the effect. | Sixteen strength-Cloud workers are healthy. At midnight, reviewed score-free progress was 2,337/8,192 clusters (28.5%) at look one; outcomes remain sealed. | Automatic look-one stop/continue, score-free final review, then terminal aggregation/review. |
| **Pair-aware rollout** | Teach simulations that a low pair can become powerful after higher pairs are exhausted, using only actor-visible history. | The powered broad screen remains healthy on Air with eight workers. | Natural completion, score-free final review, then terminal aggregate. The reviewed S6 preflight queue runs afterward only if every guard passes. |
| **S5 defensive point protection** | Determine whether the final champion—not just a heuristic proxy—still throws point cards into already-lost late-seat tricks when a lower-point legal ballot action exists. | PR #74's portability construction PASSed, but its request template self-authorized a partial 41.7-second attempt before review and consumed the one-shot admission. No result exists and no retry is authorized; INC-18 preserves the evidence. | Review the distinct-attestation repair. Any recovery additionally needs a new admission/result namespace and explicit retry marker; never reuse the old queue or admission. |
| **Performance** | Lower production latency and make every future strength experiment cheaper without changing actions, RNG, or game outcomes. | PR #71's lead prefilter measured roughly 12–16% whole-round improvement. Repair `093ec33` keeps historical RLCB evidence immutable. Stacked compatibility receipt PR #75 binds old/new identities and three fixed x86 rounds matched byte-for-byte after excluding only ballot fingerprints. A separate sampled-world preparation seam preserved nine whole-round transcripts/counters and measured 3.37% aggregate wall improvement. | External review of #71 then #75, followed by adversarial review of the second seam. Nothing deploys or enters frozen experiments before those gates. |
| **Bury shape + later shuai-pai** | Search the strategic hand left after burying—voids, pairs, tractors and later legal throws—rather than merely ranking eight individual cards. | Source, reusable DEV journal and actor-visible continuation semantics all passed review. A bounded score-free census/one-state capacity packet is being implemented; no real scoring ran. | Review and run the score-free capacity measurement, then choose an affordable reusable exploration pass from measured work/dose rather than guessing. |

## Overnight engineering lessons

- **Historical evidence and current code are different identities.** A
  behavior-preserving optimization legitimately changes source-derived ballot
  hashes. The right repair is to keep old confirmation receipts immutable and
  add a reviewed compatibility statement for current code—not silently rewrite
  history or weaken drift checks.
- **Architecture portability must be explicit.** The S5 diagnostic was correct
  but bound to an ARM native binary. The x86 wrapper preserves the historical
  parent, pins the new native/ballot contract, and replays the same score-free
  decisions before it can consume the original one-shot authority.
- **Profile hypotheses still need end-to-end timing.** Replacing `sorted` with
  `min` looked attractive in a profile but produced no whole-round gain and was
  dropped. Preparing a sampled world once targets measured duplicate work and
  produced a small reproducible aggregate gain while preserving transcripts.
- **The fleet stays hypothesis-driven.** Mini, Air and strength Cloud remain on
  T4, Pair and S4. The dedicated x86 host performs bounded engineering only
  while S5 recovery authority remains closed.
- **A documented marker convention is not an executable gate.** PR #74
  repeated INC-17 because the wrapper could not distinguish an implementer's
  byte-perfect request from a reviewer attestation. The old admission is spent;
  recovery requires a distinct attestation prefix, pinned review provenance,
  one canonical admission path and explicit retry authority.
