# Daily log — 2026-08-13

This is the plain-English execution log for August 13. Terminal policy results
belong in `AI_POLICIES.md`; durable strategy belongs in `RL_PLAN.md`; active
queue truth belongs in `BACKLOG.md`.

## Current milestone ledger

| lane | why we are doing it | progress so far | what is left |
|---|---|---|---|
| **T4 learned mid/late search** | Test whether the Stage C model can improve the live Monte Carlo champion when used only where its held-out signal was strongest and every proposal is repriced by fresh search. | Terminal aggregate `f30a77c7…e652` is `SELECT_NONE`. All integrity/exact-work gates passed, but treatment-vs-champion LCB was −0.00759 and treatment-vs-matched-null LCB was −0.03313. The merged helper recursively reproduced it and canonical review `a165274` terminally verified the result. | Closed: no confirmation, retry, promotion or deployment is authorized. |
| **S4 point banking** | Repair a systematic rollout-world bias: simulated players often win cheaply even when banking a point card is strategically better. Two earlier fresh runs were positive, so a properly powered sequential confirmation can finally resolve the effect. | Look one completed cleanly. Integrity passed but the early-efficacy boundary was not crossed, so the reviewed controller released tranche two. All 16 workers are healthy at 6,192/8,192 (75.59%); outcomes remain sealed. | Finish tranche two, run the pinned read-only verifier and obtain terminal external review. |
| **Pair ballot retention** | Keep a legal pair from being crowded out before search can evaluate it, especially in the defender-heavy early states where the census found omissions. | PR #86 is merged. Externally reviewed design-only PR #100 now specifies the future scored controller, and reviewed design-only PR #101 freezes a fresh 8,192-deal, 16-lane exact-champion natural split/role/band dose census; both are merged. | Build and review each implementation separately. No packet, census, scored execution, output access, REPORT, aggregation, retry, strength, training, promotion or deployment is authorized. |
| **Pair-aware rollout** | Teach simulations that a low pair can become powerful after higher pairs are exhausted, using only actor-visible history. | All eight Air workers remain alive at 3,648/7,168 (50.89%); outcomes remain sealed and timeout risk persists. PR #93's capacity attempt remains a reviewed over-cap negative with no retry. PR #96 exact `8a3ef59` passed source review; fresh score-free packet `b2d78d67…f69f92f` is frozen and verified on Performance Cloud, with packet review, admission, result and installed unit absent. | Preserve natural Air completion or terminal HOLD. Obtain packet review before one successor capacity execution. |
| **S5 defensive point protection** | Determine whether the final champion—not just a heuristic proxy—still throws point cards into already-lost late-seat tricks when a lower-point legal ballot action exists. | The validation-only refusal repair PASSed after the request-template defect spent the one-shot admission. PRs #74/#76 are now closed instead of merging dead execution code; PR #70 retains the reusable diagnostic source. No result or retry exists. | Decide whether the mechanism justifies a fresh recovery design. Any run requires explicit retry authority and a new admission/result namespace; never reuse the spent chain. |
| **Performance** | Lower production latency and make every future strength experiment cheaper without changing actions, RNG, or game outcomes. | The immutable six-pair batch completed: `111.464s -> 78.782s`, 29.3203% lower wall, 27.8619% paired lower bound, all six positive and normalized semantics exact. Claude independently reopened all 63 artifacts and VERIFIED/retain at `e5818ee`; PR #98 merged the byte-identical production runtime without harness/recovery code. A later three-pair current-main diagnostic suggested 30.36% but failed its strict normalizer on one explicit false flag, so it is not confirmation. | Use the faster runtime only in newly reviewed packets. V5 never reruns; obtain a fresh preregistered current-main confirmation before quoting a production-main percentage. |
| **Bury shape + later shuai-pai** | Search the strategic hand left after burying—voids, pairs, tractors and later legal throws—rather than merely ranking eight individual cards. | The sole V2 start refused before admission/gameplay because an ignored loadable `.pyc` remained in the checkout; no packet-review snapshot, records or final exists and V2 cannot retry. Optimized V3 PR #104 exact `a93c2f5` fixes Claude's sole route-level regression HOLD, keeps production bytes unchanged, passes 123/123 pure and compiled, and is staged on x86. | Obtain exact source re-review, freeze/review one fresh packet, run once serially, keep records sealed and review only the score-free final. |
| **Human H0 geometry repair** | Keep learning from human moves without turning partial exploratory coverage into a deployment claim. | PR #82 exact head `bf72dff` PASSed and merged. It reproduces the two failure shapes—production exposed 12 legal choices while the generic analyzer exposed only three—and pins the complete false authority map. | Separately authorize any opened-DEV geometry prevalidation. Any scored H0 successor needs a fresh population and design. |
| **Point-flow census** | Decide what humans and rollouts do only after giving both the same legal-opportunity denominator, before choosing S4/S5/feed rules. | PR #99 exact `0ee28a0` passed external descriptive-tooling review and withdraws the old 70%-versus-23% headline. On 165 private opened rounds, literal legal-opportunity rates are human 164/204 and rollout 14,664/14,666; inferred-strict rates are 32/46 and 302/989. Exact 150-row binding found 20 policy flips but only three toward the human action. | Choose a separately designed consumer, if warranted. This descriptive corpus grants no scored run, training, strength or deployment authority. |

## Overnight engineering lessons

- **Historical evidence and current code are different identities.** A
  behavior-preserving optimization legitimately changes source-derived ballot
  hashes. The right repair is to keep old confirmation receipts immutable and
  add a reviewed compatibility statement for current code—not silently rewrite
  history or weaken drift checks.
- **Architecture portability must be explicit.** The S5 diagnostic was correct
  but bound to an ARM native binary. The x86 wrapper preserves the historical
  parent, pins the new native/ballot contract, and replays the same score-free
  decisions before it can consume the original one-shot authority.
- **Profile hypotheses still need end-to-end timing.** Replacing `sorted` with
  `min` looked attractive in a profile but produced no whole-round gain and was
  dropped. Preparing a sampled world once targets measured duplicate work and
  produced a small reproducible exact-head gain while preserving transcripts.
  A larger cache result stayed unpublished when adversarial tests found stale
  values after legal public-object mutation and changed trump ordering; the
  safe repair was slower, so it was discarded rather than rationalized.
- **The fleet stays hypothesis-driven.** Mini finished T4 with a terminally
  reviewed `SELECT_NONE` aggregate. Air and strength Cloud continue Pair and
  S4. The dedicated
  x86 host performs bounded engineering only behind exact review gates.
- **Tests must not borrow scored-host cores.** A helper unintentionally started
  two pytest processes beside T4 on Mini. They were identified and stopped
  after roughly two CPU-core-minutes; no evidence was written, no outcome was
  opened and T4 stayed healthy. A later PR #94 broad suite similarly escaped
  its command session for under 45 seconds on one core; only its exact process
  tree was stopped and the complete suite moved to the idle x86 host. Broad
  tests now stay off live scored hosts.
- **Exercise the exact child and actual service inputs before one-shot use.**
  Earlier PR #89 rehearsals closed source staging and systemd provenance, but
  not the timed child flags: V4 proved that `-I` ignores the environment-only
  bytecode setting. V5 added `-B`, yet its first start found that actual inputs
  remained mode 0644 while rehearsal copies were immutable. Canonical review
  classified that pre-evidence refusal as unconsumed and authorized only the
  exact mode repair plus one start; the resulting immutable batch completed and
  verified. Future design freeze must apply the runtime gate to the actual
  inputs, not just copies, without assuming that this recovery precedent grants
  retry authority elsewhere.
- **A documented marker convention is not an executable gate.** PR #74
  repeated INC-17 because the wrapper could not distinguish an implementer's
  byte-perfect request from a reviewer attestation. The old admission is spent;
  recovery requires a distinct attestation prefix, pinned review provenance,
  one canonical admission path and explicit retry authority.
- **Close dead execution code, retain reusable science.** After the S5 refusal
  repair PASSed, PRs #74/#76 were closed while PR #70's diagnostic source was
  retained. PR #55's exact preservation delta then PASSed merge-readiness; the
  reviewed #55→#60→#61→#72 stack must merge in that order.
